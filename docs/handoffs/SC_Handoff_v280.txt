SC Handoff v280 — IDENT-3 Crew Scheduler consumes Team eligibility (PR, not merged) — 2026-09-16

SESSION SUMMARY
Slice 3 of Team → Crew identity unification. Crew Scheduler now reads Team-
controlled eligibility from the linked crew row. This is consumption only.
No production people were linked. No bulk backfill. No assignment identity
writes. Team UI, Field Command, PowerSync, and TF2 were not changed.

CHANGES IN THIS PR (NOT MERGED)
One filter helper: src/schedule/lib/scheduleCrew.js
- linked (team_member_id set): active iff archived !== true
- legacy unlinked (team_member_id null/blank): keep existing !archived
- Crew List Restore of a linked person is blocked; Team owns unarchive

Wired at the existing roster boundaries (Schedule, Home, Jobs, Daily,
capacity, leads, exports, Crew List). Job cells still render from
assignment crew_name, so a now-archived linked person already on a job
stays visible on that assignment.

DECISIONS
Do not hide all unlinked crew. Do not write assignments.team_member_id.
Do not unarchive linked crew from Scheduler. Weekly texts (CrewPhone /
Schedules) left on existing !archived until a later slice.

NEW/CLOSED BACKLOG ITEMS
IDENT-3 opened In Progress on this PR. IDENT-2 closed as merged PR #62
922b520. F61/B118/F56 leftovers not touched.

VERIFICATION
node src/schedule/lib/scheduleCrew.test.mjs passed (linked active, linked
archived hidden, legacy unlinked visible, mixed roster, assignments JSON
unchanged, no team_member_id writes, filter does not flip archived).
node src/lib/crewEligibility.test.mjs still passed.
eslint on the new helper + fixture script is clean.
npm run build passed.
Fixture preview (synthetic Eligible Linked / Ineligible Linked /
Legacy Unlinked): chips show Eligible Linked + Legacy Unlinked only;
Wednesday job bar title still lists all three names; 3/2 crew; zero
crew/assignments writes on load.

NOT TOUCHED
assignments.team_member_id. Team UI / crewEligibility writes. Field
Command. PowerSync. Invites. TF2. Roster backfill. Chris Berger / Chris Winn.

NEXT SESSION
Do not start from Field, PowerSync, or a Chris/backfill.
Merge IDENT-3 only after Chris accepts the Crew Scheduler preview.
Assignment identity migration is a later slice.

FILES TO KNOW
src/schedule/lib/scheduleCrew.js
src/schedule/lib/scheduleCrew.test.mjs
src/schedule/ScheduleLayout.jsx (Restore block for linked crew)
scripts/check-crew-schedule-eligibility.mjs
docs/BACKLOG.md (IDENT-3)

GIT STATE
Branch feat/crew-schedule-eligibility-roster. PR opened, not merged.
