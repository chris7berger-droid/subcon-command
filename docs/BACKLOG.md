# Sales Command — Backlog

**F63 — Launch UI · Crew Schedule VISUALLY ACCEPTED / LOCKED 2026-09-17:** Chris accepted implementation commit `87e99ad49d55b987f87becd401c1dc0b5793ef93` at this [exact preview](https://sales-command-j5d0vc9s5-chris7berger-droids-projects.vercel.app/schedule/schedule). The implemented REAL Crew Schedule is now the canonical launch visual/material reference for controlled propagation, not an AI mockup. **No further Crew Schedule UI changes or polishing without explicit authorization.** [Handoff v287](handoffs/SC_Handoff_v287.txt) records acceptance and supersedes pending-review status in v284–v286. Desktop Shell Features and separate dimensional Geometry A identity assets remain deferred follow-ups. F63 remains open for separately authorized propagation/assets/launch documents; acceptance does not authorize implementation, merge or production deployment.

**F63 — Calendar VISUALLY ACCEPTED / LOCKED 2026-09-17:** Chris accepted the canonical reskin on the [real Vercel Calendar preview](https://sales-command-3hil1ghj7-chris7berger-droids-projects.vercel.app/schedule/calendar), implementation `69ab28cf69b0631369461013b0b95c8ef7559588`. Retain `ui/calendar-canonical-reskin` as the accepted Calendar preview branch. This is the LOCKED Calendar launch-UI visual baseline; no additional UI changes or polish unless explicitly reopened. Existing Calendar structure and functionality were preserved; Crew Schedule remains LOCKED and untouched. Deferred F63 features/identity work were not implemented and remain deferred. Calendar has not been merged to main or released to production; main remains `b32b30ad9a01c17a0ff17fccae7960cfc6d41f1c`. [Handoff v288](handoffs/SC_Handoff_v288.txt) records the accepted state.

**WTC desktop layout — Closed 2026-09-17 (PR #65 merged `8a1bace`):** Chris accepted the preview and authorized merge. Warning, main panel and Summary share a 1400px maximum; desktop arrows retain 16px clearance. Materials columns redistribute width; QTY/TAX inputs are at least 80px, with a 1000px scrollable table minimum. Compact WTC containers (900px or less) put navigation above content. Build/local browser comparisons passed; original 38px material rows, values, totals, colors and read-only behavior preserved. No new lint diagnostics (23 errors / 2 warnings pre-existing). Vercel production `dpl_Pq4J9TzweRyW9F8KocCLdhHUy7jr` Ready; actual `www.scmybiz.com` browser checks passed at 1920/1440/1024px using intercepted fixtures with zero API writes. Completed review branch deleted. Handoff v283.

**IDENT-4 — T2 · In Progress (PR, not merged):** Team → Crew identity Slice 4 — write `assignments.team_member_id` on NEW Schedule assignment inserts from the selected crew row. Legacy unlinked crew still insert null. No historical backfill, no Chris link, no Team/Field/PowerSync/schema/TF2 changes. IDENT-3 is merged. Handoff v281.

**IDENT-3 — T2 · Closed 2026-09-16 (PR #63 merged `d8c9eb7`):** Crew Scheduler consumes Team-controlled eligibility. Linked crew are active iff `archived !== true`; legacy unlinked keep `!archived`. Existing assignments were not rewritten. Handoff v280.

**IDENT-2 — T2 · Closed 2026-09-16 (PR #62 merged `922b520`):** Team-controlled Crew Schedule eligibility. Team modal **Available on Crew Schedule**. ON creates/links/unarchives; OFF archives, does not delete. Handoff v279.

**PR61 — merged 2026-09-16 (`31caa32`):** /crew Midweek Update — today through Friday compact text (JOB #, coworkers, canonical `scheduled-off` as `(OFF — MAY CHANGE)`). Weekly/Sunday send unchanged. Desktop Schedules.jsx unchanged. No assignment or `crew_status` writes. Chris accepted the actual iPhone Vercel preview. Handoff v278.

**PR60 — merged 2026-09-15 (`3bc81ac`):** Field Command Crews reads Crew Scheduler `assignments` (no `job_crew`). Scheduled Off is stored `scheduled-off`, distinct from Call In `off`, with create/see/edit/remove. Chris accepted the Ready preview. Handoff v277. F56 leftover polish #2–#4 remain.

**B123 — T2 · Closed 2026-09-13 (PR56 merged `d12cfef`):** Crew Schedule weekday/date labels only — capacity-strip `MON 14` charcoal pills in `#79CDE0`, stacked Job/Mo–Sa `MON` over `09/14`, then ~25% larger equal-circle free/off badges. Chris accepted the preview. Week-nav, scores, bars, JOB, grid, and date logic unchanged. Handoff v276.

**B122 — T2 · Scrapped 2026-09-13 (Chris rejected):** Do not put a names × Mo–Sa crew grid on weekly texts, and do not drop the **With:** line for that. PR55 closed unmerged; branch deleted. Empty-day B121 and lead B120 stay. Handoff v275.

**B121 — T2 · Closed 2026-09-13 (PR54 merged `ac0b74b`):** Weekly crew texts omit days with no work — no weekday/date and no **No work assigned** in the preview or the shared text. Chris accepted the preview. Days with work unchanged. Handoff v274.

**B120 — T2 · Closed 2026-09-13 (PR53 merged `48342cf`):** Weekly crew texts (`/crew` and desktop Schedules) now use the same lead as Crew Schedule: trip lead if set, otherwise the job Lead. Unlinked / “trip not identified” days no longer print Confirm with office when the board already has a lead. Chris accepted the preview. Coworkers and start times unchanged. Handoff v273.

**F61 temporary phone sharing — Released 2026-09-10 (PR50, `4b01cda`):** Separate **https://scmybiz.com/crew** page is live; `/crew-texts` also works. Direct-link login, week/person selection, daily jobs/coworkers/leads/delayed starts, Share/Copy and refresh on return reuse existing Schedule data and authorization. Desktop shell/sidebar/crew board/existing Schedules page/CSS match pre-release main exactly. No Twilio, PDF rebuild, migrations or app data writes. Chris authorized launch after PR49 completed; updated candidate `541205f` passed build, focused lint and one deployed phone-flow check. Production deployment `dpl_2wLkiYyeexfBKEvZQStF6AeVzMRf` is Ready; actual public `/crew` opens standalone login on a phone viewport without Vercel approval. Authenticated flow and share payloads were tested with intercepted fixtures; native iPhone Messages delivery and user acceptance remain Chris’s device check. Handoff v265 records completed branch/worktree cleanup and terminal closeout; v264 retains release details.

**B116 — T2 · Closed 2026-09-10 (PR49 merged `c3d7506`, production Ready):** Sales Trips/direct Send to Schedule, daily Field SOW scope-note display, and job-number-only crew cards released. Trip summary SOW stays manual. Build and focused preview checks already passed; Chris inspected the crew cards and confirmed warning removal after the separate live Sales-link repair. B117 is deployed; B118 retains eleven explicit exceptions. Handoff v263 supersedes the earlier pending-release notes below. Completed app/DB branches cleaned up; canonical checkouts on main.

**B119 — T2 · Closed 2026-09-11 (PR51 merged `ea41bb9`):** Crew Schedule Job / Mo–Sa date bar stays put while jobs scroll. All Stages row removed so crew cards and the week strip sit under Weekly Crew Capacity. Day names and dates are larger on the capacity cards and the board header. Chris accepted the preview. No data or assignment changes. Handoff v268.

**B118 — T2 · Open (11 remaining exceptions; 49 repaired and accepted) — 2026-09-10: Historical Schedule → Sales reconciliation.** Chris clarified that the earlier trip conversion was expected to finish historical Sales links too. Read-only matching found 49 clear Schedule-to-Sales identities (14 links to existing Sales records, 25 archive Sales records serving 35 historical rows). Repair is committed in canonical `command-suite-db/scripts/repairs/historical_sales_links.sql` (DB PR5 merged `fee1210`; completed worktree/branch removed); repair applied live 2026-09-10 (DB implementation `0c49c1c`). Focused rollback/preservation/repeat rehearsal passed; independent live comparison preserves all 302 trips and 3,379 crew assignments plus every other Schedule field. Chris-authenticated reads see all 49 repaired Sales joins. Chris confirmed the warnings are gone on the actual Crew Schedule. These 49 are repaired; do not rerun. B118 stays open for the eleven exceptions. Preserve all existing trip/crew identities and Schedule details. Eleven rows excluded from this first map: three are the ALREADY-DECIDED 1111 Shop Work/overhead records (do not ask Chris to identify them again); eight have ambiguous/missing customer-job identities. Shop Work design is in `docs/plans/shop_work_overhead_type.md`; it explicitly keeps a real overhead Sales record and is currently marked not built. Do not manufacture customer Sales matches or silently hide warnings.

**B116 small requested follow-up — 2026-09-10:** Crew sidebar labels now show the job-number portion only, dropping the truncated job-name suffix before weekday bubbles. Existing tooltip, warnings and dots retained. One rendering-line change on PR49; build passes.

**B117 — T2 · Closed 2026-09-10 (DB MOB-3 / PR4):** Historical trips existed with valid crew links but were hidden by trip RLS requiring a Sales call_log link. Earlier stale-tab diagnosis was incorrect. 62 trips / 60 jobs affected. Chris approved live application; migration `20260910230000` now checks owning jobs.tenant_id. Standard deployment gates passed. Live authenticated verification now reads all three saved trips for K7/job1054 and Dave Lee/job1278, with their seven September7–13 crew assignments. No trip/crew records changed. Baseline and release notes synchronized. Chris asked to reload for visual acceptance. No extra browser fixture runs. Handoff v260 release addendum. B116/PR49 remains separate and unmerged.

**B116 testing follow-up — 2026-09-10 (`3b3b225`, PR49, not merged):** Schedule's Field SOW builder preserved transferred `scope_notes` but never rendered them. Added the missing read-only per-day display with line breaks preserved. Chris explicitly chose to keep the crew trip's Scope / SOW summary manual; it remains separate. Local and Vercel `6uw5sef7i` browser regressions verify distinct WTC notes display and survive a Schedule date save without changing the Sales source. Build/send/date checks pass; same six pre-existing FieldSowBuilder lint findings. Live read-only comparison confirms active test job 1284 (10176) Day 1 notes exactly match its source WTC. Read-only checks of Dave Lee 10252 and K7 6665 confirm September 7–13 crew assignments have valid saved trip links; advised reload because the screen appears stale (not browser-confirmed). No record edits or additional trip conversion. Handoff v259.

**B116 — T2 · In Progress — built and preview-verified 2026-09-10 (PR49, `bc84347`, not merged): Sales Trips terminology and direct Send to Schedule.** Authorized scope: retain WTC multiple-trip authoring and per-trip Field SOW; rename visible Sales Mobilization labels to Trips; remove routine send-review confirmation; preserve validation and single-trip defaults; stop copying customer-facing Sales SOW into new Schedule jobs. Existing trip/scope/crew records remain unchanged. No Schedule redesign, database migration, or Field phone sync work. Touches F42/F43 only in this narrow scope; their broader UX work stays deferred. Build, focused send/date regressions and deployed browser checks pass; no new lint findings versus main. Preview `gt2g5l9k1` inspected with synthetic proposals and all API calls intercepted. WTC multi-trip authoring/scope save, single/multiple/no-trip send, Schedule trip display and incomplete-selection blocking verified. Handoff v258.

**B115 acceptance — corrected 2026-09-10:** Chris tested production and confirmed “Test looks good.” The crew-to-trip conversion was applied; completed trip-fix branches were cleaned up. The earlier blanket completion claim did not establish the full historical result: missing Sales links remained, and intercepted browser checks missed authenticated trip visibility. B117 fixed access live; B118 Sales reconciliation remains in progress. Do not repeat the completed conversion. Handoff v257 now carries this correction; v261 records the prevention rule.

**B115 — T2 · Closed 2026-09-10 (PR48 `e0a77fd`; DB PR3 `9593b1c`):** Converted all remaining NULL-linked crew blocks after Chris accepted 10088: 190 saved trips / 183 jobs / 2,053 links. Independent full before/after comparison preserves all 3,325 assignment records except those links; every prior link/name, job, scope and crew record unchanged. Default titles count all literal trips oldest first; UUID links survive renaming. Job dates remain above Trips; approved “Please update trip title.” reminder retained. Browser-on-load writer removed. Latest-snapshot transactional rehearsal passed rollback, concurrency and repeat no-op; 190 recovery audits persisted, zero unlinked assignments remain. Focused browser regression/build passed; exact preview `48624b9` (5xju356v8) inspected with actual post-conversion snapshot, all network writes intercepted. Production `e0a77fd` succeeded. Existing non-NULL off-trip links (including 7215) remain outside scope. Handoff v257.

**F61 touch-up — PR #43 merged 2026-09-09 (`2416c91`), production deployment successful:** Weekly crew texts adds a week-preserving Back to Crew Schedule button, two-panel desktop layout and stacked phone layout. Sidebar route matching now distinguishes Crew Schedule from Schedules. Build, focused lint, and intercepted browser regression suite pass. Feature branch removed locally and remotely; release handoff v246 supersedes v245’s pending-review status.

**Trip/job handoff fixes — PR42 MERGED 2026-09-08, `a127944`, production verified:** Corrected Sales date authority, scrollable SOW, safe trip deletion, consecutive display numbering, trip-aware Planning DAYS/CREW, restored calendar, exact-trip Crew navigation and stable Back to Job. Consolidated handoff: `docs/handoffs/SC_Handoff_v244.txt`. This incorporates released weekly crew texts (F61); no superseded handoff overwrites.

**Follow-ups discovered, not closed by PR42:**
- Automatic refresh: Crew Schedule static jobs/trips lack return-to-tab/reconnect catch-up; Home/Jobs also lack catch-up after missed realtime events. Preserve drafts when implementing.
- Readiness/banner/list date readers still use job dates in places; audit them against mobilization-only scheduling. The DAYS/CREW display fixes do not change promotion gates.
- Historical bidding-date cleanup remains scoped: only 10227 was explicitly authorized and repaired. Do not bulk-clear job dates without provenance.
- Deletion assignment/pull-ticket prechecks are not atomic database constraints.
- Direct Edit/Delete controls on collapsed trip rows and original-date separation were discussed but not implemented after the user redirected work to date authority; current deletion is inside Edit trip.

**CODEX RELEASE COMPLETE — 2026-09-08:** Scheduling/trips merged via [Codex PR #38](https://github.com/chris7berger-droid/sales-command/pull/38), merge `555019c`, production verified. B104/B105/B106/MOBZ-2 moved to Completed Log. The merged Codex branch/worktree are retired; main is the next-session starting point. Authoritative handoff: `docs/handoffs/SC_Handoff_v235.txt`. B107/B108 remain explicitly deferred.

**Single source of truth for all outstanding work.** Update on every session
that completes, defers, or discovers an item. Status values: `Open`,
`In Progress`, `Blocked`, `Done` (move Done items to the Completed Log
at the bottom and out of the active table within a session or two).

Last updated: 2026-09-03 (Subcon merge **PHASE 5 — The name + addresses — PLANNED + audited** on `feat/schedule-merge-plan`, not built yet. Turned the §5 sketch into a build-depth spec (`docs/plans/schedule_into_sales_merge_PLAN.md` §5a–§5h), self-audit corrected 6 unverified assumptions, then /runaudit round 1 (3 agents) returned 11 caused-by + 2 adjacent, **0 security**. Chris ratified: **Option 1 — nothing deploys ahead of the merge** (the whole rename + link cleanup ships at the one flip; reverses the earlier "fix emails now" — those links point at `/sales`,`/schedule`,`/field` routes that only exist post-merge, and old `salescommand.app` already forwards). Round-1 amendment (§5 AMENDMENT AM-1…AM-7): login screen DOES render "Sales Command" (split-JSX heading `Login.jsx:127`, my "no login wordmark" claim was FALSE); rename ground-truth widened (`Settings.jsx:647` "Sub Con Command LLC", `LandingPage.jsx:363` + `FeatureDetailPage.jsx:447` "© Sales Command"); comment-sweep CUT (0 valid targets — hits are kept section-brand); auth-redirect gate tightened to exact `www.scmybiz.com` + hard reset/invite smoke. **B75 filed** (Stripe billing return params ignored, pre-existing), **B76 filed** (§5f vs code `sccmybiz.com` hard-route mismatch). **One open decision gating build — AM-7: does the real homepage `scmybiz.com/` (LandingPage, all "Sales Command") get renamed to Subcon Command, or stay?** "Command" suffix KEPT on all sections (amends Beat 8). Held — NOT merged.)
Last updated: 2026-09-02 (Subcon merge **PHASE 4 — AR Command moves in — BUILT** on `feat/schedule-merge-plan`, NOT merged (rollout = one entitlement flip at end). Cosmetic mount per plan §4b: `AR-Command-Center/src/{components,lib,pages,assets}` → `src/ar/*` (dropped dead `main.jsx`/`App.jsx`/`lib/supabase.js`); new `src/ar/ARLayout.jsx` mirrors Field/Schedule (`.ar-root` wrapper, scoped `.ar-root::before` crosshatch only, nested `/ar/:tab` routes, Upload gate above routes, index/`*`→triage backstop); `Dashboard.jsx` converted to URL-driven tabs (`useParams`+`navigate`, TAB_IDS guard on Directory chapter ids); host `App.jsx` `/ar/*` route under `GroupGuard app="ar"`; `nav.js` `AVAILABLE_APPS += "ar"` + `GROUPS[ar].items` filled + `home:/ar/triage`; green→teal token swap in `src/ar/lib/tokens.js` (exact hexes). AR data stays localStorage + Excel upload — live Supabase AR wiring OUT of scope. Zero new deps/migrations/grants. `npm run build` GREEN. **B73 filed** (AR print/export HTML still hardcodes `#5BBD3F` green). **All three code gates GREEN** (2026-09-03): /buildvsplan 7/7 PASS · /code-review 0 blockers (**B74** filed) · /security-review 0 exploitable-today (**S15** filed). **Preview smoke DEFERRED by Chris's call** — AR is underdeveloped + will be reworked, and the mount is cosmetic + entitlement-dark, so pixel-smoking it now has no value (deferred, not failed). Held — NOT merged, entitlement flip NOT done. **Pointer: at the suite go-live one-flip, HOLD AR out of the entitlement (light Sales+Schedule+Field only) until AR is actually built out** — "moved in" ≠ "turn on". Full detail: plan §4b + handoff v201.)
Last updated: 2026-09-02 (Subcon merge **PHASE 2 — Schedule moves in — BUILT** on `feat/schedule-merge-plan`, NOT merged (rollout = one flip at end). §2 safety sweep GREEN; sch-command/src → `src/schedule/*` mounted `/schedule/*` under the host shell via new `ScheduleLayout.jsx`; CSS fenced under `.schedule-root`; 7 collision fixes applied; supabase client/auth/login dropped to host; `lib/user.jsx` kept + fed host teamMember. Compiles clean, browser-smoked (Schedule Home/Jobs render inside host, Sales unaffected, 0 console errors). Deferred to next session: teal reconciliation (Beat 5 polish), Settings fold into unified /settings, pagination sweep, and the gates (/buildvsplan → /code-review → /security-review → preview). Entitlement HELD — HDSP tenant_config.apps stays ["sales"] until go-live flip. Full detail: `docs/plans/schedule_into_sales_merge_PLAN.md` §2b + handoff v195.)
Last updated: 2026-08-31 (retention-release send-screen fix — **B71 filed + fixed in code**. "Bill Retention" created the release invoice but navigated to its detail view, not the Send/Approve screen, so the release sat in "New" and never posted to QuickBooks (surfaced on invoice 10165 → release 10181, present in SC, absent from QB). Fixed in `src/pages/Invoices.jsx` (4 edits): Bill Retention now carries a one-shot router-state flag across the `InvoiceDetail` remount to auto-open the Send/Approve modal (`showPDF`) once data has loaded — no auto-push, user still chooses Approve→QB vs Send. Builds clean. **Pending browser smoke + merge**; the already-stranded 10181 must be posted by hand (`Send / Resend` → `Approve → QB`).)
Last updated: 2026-08-11 (Home → Follow-Up build session, ERD #45. Both migrations (runway cols + Admin/Manager tenant_config write-guard; outreach_log append-only log) authored, rehearsed, and **pushed LIVE to prod** via command-suite-db — note the DB password is stale, so they went in via the Management API + hand-written ledger (see handoff v183). Full app build shipped on `feat/home-follow-up-screen` (data layer, provider, 5 components, Home rewrite, refresh wiring, Settings save-guard); **/buildvsplan PASSED 0/0 in two terminals**. Build corrected two plan errors: archive source is `archive.legacy_records` via `archiveDb` (not `archive_records`), and `call_log_id` is INTEGER (not uuid). **UI rejected — "boring list, needs a major redesign"**; engine is done, VISUAL design is the next job → **F47** filed. **F48** filed (archive date fallback, deferred). Branch NOT merged; ERD #45 stays OPEN.)
Last updated: 2026-08-07 (invoice date/timezone — `invoices.sent_at` is a **date** column that every send path wrote a UTC instant into, so anything sent after 5pm Pacific was stamped **tomorrow** and fell out of the date filter, in SC and in QuickBooks. Fixed at the shared helper: `utils.tod()` now returns the local wall-clock date, new `dayDiff()` for date-vs-date day counts. Same root cause corrected the list's **aging** column (one day hot) and the **pay-app** send + printed invoice date. Prod data repaired for #10153/#10154 in both systems. **No new open rows** — but note invoices sent after 5pm on earlier days remain a day late and are undetectable, since only the date is stored. No migration.)
Last updated: 2026-07-28 (proposal-PDF rounding — **root cause of the 6/26 exact-penny work found and fixed**. `ProposalPDFModal` was computing the customer-facing total by hand instead of via `calcWtcPrice`, so the PDF printed the raw pre-ceil sum while the invoice billed the ceil'd figure — customers paid what the proposal said and came up cents short. PDF total + per-work-type lines now use `calcWtcPrice`; Subtotal derives from the rounded total so Subtotal − Discount = Total on the page; cents print whenever the price has cents (PDF + public signing page) regardless of the job's `show_cents` checkbox. With the PDF honest, exact-penny pricing became a **closed window** — `EXACT_PRICING_END` = midnight Central 2026-07-29; new work rounds UP again, and the 6/26–7/29 cohort keeps penny pricing permanently so no signed contract moves. **B61 retired** (desired behavior inverted). **F41 filed** (deferred per-job exact-penny switch). Plan amended: `docs/plans/exact_penny_pricing.md` "Amendment 2026-07-28".)
Last updated: 2026-06-26 (exact-penny-pricing loop R3 — filed **B52** (multi-invoice cumulative 1¢ drift, ADJ-D3) + **B53** (override-sister mispricing ignores per-GC markup, ADJ-D4) as pre-existing pricing bugs surfaced by — not caused by — the exact-penny audit. Plan `docs/plans/exact_penny_pricing.md` converged after 3 rounds; **build complete on `feat/exact-penny-pricing` (calc core + `pricing_anchor_at` migration/clone RPC + ~15 call-site rewires + line-sum reconciliation), pending /buildvsplan + /code-review + /security-review + migration push**. B52/B53 remain open as separate follow-ups.)
Last updated: 2026-06-01 (Loop #30 — per-invoice retention release. Shipped the release path: migration `20260601120000` (`invoices.retention_release_of` FK + `retention_released` flag), a "Bill Retention $X" button on the invoice detail that spawns a release invoice for the held amount (conditional UPDATE→INSERT→checked-revert), the one-line `retention_released` copy onto the void-replacement insert (double-bill guard), and a `qb-sync-invoice` gate that emits a single positive retention-item line for release invoices (hard-fails if no retention item configured). **Touched F6** (per-invoice done, per-job/cumulative still open). **F34 already filed** (full block/cascade void UX, deferred). Plan + 3-round audit at `docs/plans/retention_invoice_process.md`.)
Last updated: 2026-05-28 (Loop #28 — billing-contact-not-syncing-to-customer-detail. Ships round-0 §2 simple fix in `NewInquiryWizard.jsx` only. Audit round-1+2 found 23 valid findings but most were pre-existing wizard patterns surfaced by, not caused by, the fix. Scope-cut decision: ship the simple mirror, file the valuable durable findings as backlog rows. **Filed:** B40 (UPDATE silent failure), F31 (cross-surface symmetry sweep), F32 (DB-level Billing Contact uniqueness), F33 (picker unlock affordance), S4 (customer_contacts tenant_id consistency trigger), S5 (send-invoice/send-pay-app/qb-create-job tenant filter on contact reads), R4 (extract pickBillingContact). Plan + audit deliberation preserved at `docs/plans/billing_contact_wizard_existing_customer.md` §11.)
Last updated: 2026-05-21 (Loop #23 — **B33 closed, merge SHA `896b09b` (PR #26).** Invoice pay links swapped from Checkout Sessions (24h cap) to Payment Links (live until deactivated). 3 deactivation triggers wired (paid via webhook, void + pullback via new `deactivate-payment-link` edge fn). Audit M3 fix: deactivate prior link on resend. Side find: HDSP `STRIPE_WEBHOOK_SECRET` had been wrong since 2026-04-06 (SCC v55 setup overwrote it) — fixed via `supabase secrets set`. 2 historical silent-paid invoices (#10011, #10023) backfilled in DB + QB.)
Last updated: 2026-05-19 (Loop #18 — **Step 11 code complete.** CallLog multi-GC count chip, Proposals sister differentiator, WTCCalculator sync wiring. F17 ready for smoke.)
Last updated: 2026-05-19 (Loop #16 — **Step 9 wired.** calc.js extended, F18 schema migration, clone RPC updated, wizard CTA live. F18/F19 closed.)
Last updated: 2026-05-15 (Loop #8 — **B27 closed.** WTC MaterialPicker three-bug fix: save-button feedback (`✓ Saved` toast), picker refresh after `saveCustomToCatalog` (was stale until page reload), search now matches supplier (was name + kit_size only). Plus side-find stale-closure fix on `saveCustomToCatalog` (concurrent field edits no longer reverted), plus added pencil-icon inline edit modal on tenant-owned dropdown rows so price changes don't require a Settings context switch.)
Last updated: 2026-05-15 (Loop #7 — bug-hunt session. **B25 + B26 closed.** B25: silent save-fail on duplicate job_number (commit `9787a29`). B26: sticky-tag bug in `CallLogDetail.handleSave` — regexed `CO\d+` out of old display and pasted back, cementing phantom CO tags into rows that never were COs (e.g. job 6618 signed by customer with no parent in DB). Real-world rebuild done in same session: parent 6618 (id 3601) inserted via SQL, child 3512 properly linked as CO. Code fix: rebuild display from canonical columns (job_number + co_number + job_name), never regex old display.)
Last updated: 2026-05-14 (Three events today. **B21 closed** — public invoice page 400 for 100% of live invoices since 2026-05-02; RLS `i.job_id::int` cast → `invoices.call_log_id` FK + rewrote policies. Migration `20260514130000`. **B22 closed — C1 fix live + verified in prod.** Sister-aware `mark_proposal_signed` + `'Signed'` status shipped (migration `20260514000000` + `proposal-signed` edge fn + 10 client surfaces); both multi-GC and single-GC smoke passed on TEST customers. **Two latent signing bugs surfaced during C1 smoke (NOT caused by C1):** B23 (Pull Back doesn't clear `signing_token_consumed_at` → re-sign forever-broken on pulled-back proposals) + B24 (`PublicSigningPage` shows success even when DB rejects the sign). Both T2 — pick up next loop.)
Last updated: 2026-08-07 (**O11 closed** — the migration rehearsal gate was passing everything. Its baseline had drifted 5 weeks from prod and `--verify` compared that stale copy to equally stale constants, printing "matches prod" without ever querying prod. Fixed in `command-suite-db` (merge `9c396d8`): baseline re-dumped, constants re-measured, `--against-prod` made non-optional on every run, and a permanent canary added so a blind gate fails loudly instead of quietly. **F44** filed separately — T&M rate cards should not count as contract dollars, split out of the T&M billing plan.)
Last full sweep: 2026-05-08 — every Open row re-evaluated against current code; B6 caught as already-shipped during this sweep.

## Tier definitions

The `Tier` column replaces the prior H/M/L Pri field. Tier captures
severity × likelihood-soon × cost × strategic leverage as one decision:

- **T0** — Drop everything. Active prod breakage, in-flight security incident.
- **T1** — This session. High-severity-and-likely × low-cost. Quick wins on customer-facing surfaces.
- **T2** — This sprint. High strategic leverage, or unblocks T1/T2 work.
- **T3** — When convenient. Low-severity bugs, refactor, polish.
- **T4** — Only if forced. Items needing a re-trigger before they can move (missing artifacts, blocked on external).

Dependency chains are surfaced with `Blocks: <ID>` / `Blocked by: <ID>`
as the first line of the Notes column.

---

## Active

### Security

| ID    | Tier | Status      | Item                                                                                              | Source                          | Notes                                                                                                                                                                                       |
|-------|------|-------------|---------------------------------------------------------------------------------------------------|---------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| MOBZ-1 | T2 | On branch (not merged) | Manual Combine duplicates + History-from-crew-days (Schedule) | mobilization_model build 2026-09-08 | **Branch `feat/mobilization-combine` — preview only, NOT on scmybiz.com yet; merge = go-live (Chris's call).** DB migrations are LIVE (command-suite-db `20260908120000/130000/140000/150000`: mob_type/note + append-only scope-versions, `assignments.mobilization_id`, `jobs.merged_into_job_id`, `combine_jobs()`). Combine tool folds duplicate cards into one job (per-card Add to Main / Don't Add → Save; earliest = Main, rest fold in as trips); History panel now shows trips derived from crew days (was `job_changes`); `loadJobs` hides merged rows. Code review 2026-09-08: **0 ship-blockers** (→ MOB-3 cleanups). **TWO real combines already run in prod:** job 1224 "Contract flooring" (9424 Double R Blvd), job 1234 "Agru" (AGRU 230 Lyon Dr). Plan: `sch-command docs/plans/mobilization_model.md` (auto-cleanup dropped → manual Combine amendment). Cross-repo follow-ups in **command-suite-db BACKLOG: MOB-1** (assignments fail-open policy drop, needs cross-app security gate) + **MOB-2** (scope-versions grants vs `restrictive_combos=1` invariant + baseline/EXPECT catch-up — safe today, decision pending). |
| MOB-3 | T3 | Open | Combine/History code cleanups (non-blocking, from pre-merge review) | code-review 2026-09-08 | All **Low, 0 ship-blockers**. (1) extract shared `MONTHS`/`fmt`/`rangeLabel` date helpers out of `CombineDuplicatesModal` + `HistoryPanel` into one util; (2) `deriveJobTrips` — dedup dates defensively so an array caller can't inflate `dayCount` (live callers pass Sets, so no live bug); (3) guard against one mob labeling multiple trips in `deriveJobTrips` (cosmetic — labels only); (4) surface/`console.error` `logJobChange` failures in `combineJobs` (audit-log only; the combine itself is unaffected). ~1h. |
| H5    | T2   | Closed 2026-05-20 | Token expiry / single-use (`signing_token_expires_at`)                                            | Deep audit 2026-04-30           | **Blocks: F7.** Migration A deployed to prod 2026-05-11 02:09 UTC (PR #20 → squashed `a73ce87`); Vercel + `proposal-signed` edge fn deployed; prod smoke P2–P4 passed (PostgREST cache reloaded clean, existing Sold revisit renders Accepted, new send + sign on TEST customer flipped status/approved_at/consumed_at/signature/call_log.stage atomically — all four with identical `02:13:02.712165` timestamp — and edge fn returned HTTP 409 `ALREADY_SIGNED` for the consumed token re-attempt). Token expiry + single-use are LIVE; the security gap (forever-valid signing URLs) is closed at the DB layer. Stays In Progress until O3 drops the 1-arg compat wrapper + legacy anon-insert policy at the ≥48h gate. Plan + smoke at `docs/plans/h5_signing_token_expiry.md`. |
| H11   | T2   | Closed 2026-05-20 | `send-proposal` trust boundary — body-trusted email + unescaped HTML interpolation                | Found 2026-05-09 (during C9 fix) | **Blocks: F7.** `supabase/functions/send-proposal/index.ts` trusts body-supplied `customerEmail`, `signingUrl`, `companyName`, `companyTagline`, `companyPhone`, `repEmail`, `repName`, `proposalNumber`, `jobName`, `emailIntro` and interpolates several into HTML without `escapeHtml()`. Same shape as audit C8 on `send-invoice`. Vectors: (a) low-priv tenant member redirects customer email to attacker via `customerEmail`; (b) stored-XSS-via-email if any of customerName/jobName/emailIntro contains markup. Preferred fix shape: accept `proposalId`, server loads tenant/proposal/customer/rep, server builds signing URL from `proposals.signing_token`, escape all HTML (mirror the `escapeHtml` helper now in `send-pay-app`). ~half day. |
| B13   | T2   | Closed 2026-05-20 | `invoices.viewing_token` — no expiry, indefinite public exposure of pricing + payment context     | Found 2026-05-10 (H5 scoping)   | **Blocks: F7.** Same forwarded-link-forever class as H5 but on `invoices.viewing_token`: public invoice URL stays valid indefinitely; a leaked link continues to expose customer + pricing detail. Add `invoices.viewing_token_expires_at` + auto-fill trigger; backfill anchored on `COALESCE(sent_at, created_at)` + tenant `proposal_validity_days`; mirror strict predicate across `invoices_public_view_token`, `invoice_lines_public_read_token`, `proposals_public_view_token`, `call_log_public_view_token`, and the viewing branch of `customers_public_read_token`. No single-use semantic — customers retrieve invoices repeatedly. Re-issue path via `send-invoice` resend (same shape as H5's `handleSend` refresh on send). Same two-migration compat-safe deploy pattern as H5. ~half day. |
| S2    | T2   | Closed 2026-05-20 | Schedule daily alarm on `SELECT count(*) FROM private.v_orphan_auth_users`                        | S1 review 2026-05-08            | **Blocks: F7.** Steady-state guard for the S1 fix. Currently 0 orphans; becomes load-bearing the moment F7 multi-tenant onboarding lands. Likely path: new edge function querying the view + `cron.schedule` via `pg_net.http_post` (matches existing Resend-using fns). Service_role can read `private` schema. View moved from `public` to `private` on 2026-05-20 (S3, migration `20260520120000`) to clear advisor findings. ~30–45 min. |
| S3    | T0   | Closed 2026-05-20 | Move `v_orphan_auth_users` from `public` to `private` schema to clear `auth_users_exposed` + `security_definer_view` advisors | Supabase advisor email 2026-05-17 | **Closed same session (Loop #19).** Both ERROR-level advisor findings on Command Suite DB pointed at `public.v_orphan_auth_users` (created by S1, migration `20260509120000`). `auth_users_exposed` fired because views in PostgREST-exposed schemas inherit `authenticated`'s default SELECT grant chain even after `REVOKE`; `security_definer_view` fired because the original view ran as creator (postgres role can read `auth.users`) instead of caller. Fix: created `private` schema (not in PostgREST's exposed list — `supabase/config.toml` has no `[api]` block, so defaults `public`/`graphql_public`/`storage` apply), recreated view there with `WITH (security_invoker = on)`, granted SELECT only to `service_role`, dropped `public.v_orphan_auth_users`. No app code touches the view (grep verified). Migration `20260520120000_orphan_auth_view_to_private.sql`, rollback `supabase/rollbacks/20260520120100_…sql`. S2 path updated from `public.` to `private.` in same edit. |
| S4    | T2   | Open        | `customer_contacts.tenant_id` consistency trigger — TENANT_MISMATCH RAISE pattern                  | Loop #28 audit 2026-05-28        | **Blocks: F7.** BEFORE INSERT OR UPDATE trigger on `customer_contacts` that joins `customers` by `customer_id` and raises `EXCEPTION 'TENANT_MISMATCH'` if `customer_contacts.tenant_id != customers.tenant_id`. Mirrors `delete_customer` pattern at `supabase/migrations/20260430120000_customer_delete_merge.sql:136-138` (RAISE, not derive — derivation would silently rebind shadow rows). Same trigger shape as H4 on `proposal_signatures` (`20260508120000_proposal_signatures_tenant_id_trigger.sql`). Closes the cross-tenant orphan row class for `customer_contacts` durably; lets readers stop carrying belt-and-suspenders tenant filters (see S5). ~1h including migration + scratch test. |
| S5    | T3   | Open        | `send-invoice` + `send-pay-app` + `qb-create-job`: tenant filter on `customer_contacts` reads     | Loop #28 audit 2026-05-28        | Defense-in-depth. Currently rely on upstream `customers.tenant_id` filter; explicit `.eq("tenant_id", caller.tenantId)` on `customer_contacts` reads is belt-and-suspenders pre-S4. Mirrors C9 fix pattern at `supabase/functions/send-pay-app/index.ts` (2026-05-09). **AUDIT 2026-06-10:** scope corrected — `qb-create-job` reads from the `customer` object, **not** `customer_contacts` (no contact read in that fn), so **drop it from scope**. Two real sites, line #s drifted: `send-invoice/index.ts:87-96`, `send-pay-app/index.ts:190-197`. Note: the comment at `send-pay-app/index.ts:178` claiming `customer_contacts` has no `tenant_id` is **false** — the column exists (`sql/rls_child_tables.sql:143`), so the `.eq("tenant_id", …)` fix is feasible (fix that comment too). Once S4 lands, these become redundant — but they ship faster (~30 min/fn) and de-risk the F7 onboarding window before S4 reaches prod. |
| S6    | T1   | Closed 2026-06-09 | [Sev 7 · Like 6 · Eff 1h] `check-orphan-users` is unauthenticated + service-role + Resend send loop (CRITICAL in audit) | Weekly auto-audit 2026-06-01 (C2) | **Top-urgent — single-tenant-relevant TODAY.** `supabase/functions/check-orphan-users/index.ts:8` runs with NO auth and NO `[functions.check-orphan-users] verify_jwt` entry in `config.toml`, so a single unauthenticated `curl` invokes service-role `get_orphan_auth_user_count()` and fires a Resend email per request → DoS / spend-abuse / Resend-quota drain + a permanent service-role RPC oracle. Carry-over from 2026-05-25 (was H-C2), escalated to CRITICAL. **Fix:** add `[functions.check-orphan-users] verify_jwt = true` to `supabase/config.toml` AND a timing-safe `CRON_SECRET` header check at handler top; invoke only from a scheduled service-role job. Full detail: `audit-reports/2026-06-01.md` §C2. **Closed (code) 2026-06-09 on `feat/t1-security-remediation`:** `config.toml` `[functions.check-orphan-users] verify_jwt=true` + handler length-guard+`timingSafeEqual` `x-cron-secret` gate + cron-reschedule migration (`20260609120100`) adding `x-cron-secret` via `current_setting('app.settings.cron_secret', TRUE)`. **DEPLOYED 2026-06-09:** gate live, `verify_jwt` enforcing — **unauthenticated curl → 401 confirmed, the vulnerability is closed.** Secret stored in function-env (`supabase secrets set CRON_SECRET`) **and Supabase Vault** — the planned `app.settings.cron_secret` GUC was **platform-blocked** (custom GUC needs superuser; SQL-editor/migration role isn't), so the cron reads `x-cron-secret` from Vault instead (migration file updated to as-executed). **PENDING:** confirm the daily 6am-UTC cron still reaches the function (`cron.job_run_details`) — the editor showed `app.settings.supabase_url` unrecognized, which may mean the alarm's pre-existing URL/key GUCs don't resolve (a pre-existing issue, independent of this fix). |
| S7    | T1   | Closed 2026-06-09 | [Sev 8 · Like 5 · Eff half-day] `.vercel.app` substring CORS allowance bypasses auth across ~20 money/admin edge functions | Weekly auto-audit 2026-06-01 (C1) | **Highest-leverage systemic fix — relevant TODAY.** `origin.endsWith(".vercel.app")` in `send-invoice:15` + 19 siblings (qb-*, stripe-webhook, invite-user, reset-password, etc.) lets an attacker host JS on any `*.vercel.app`, issue credentialed cross-origin POSTs, and have the victim's browser attach the logged-in admin JWT → every "authenticated" check becomes CSRF-bypassable. Persistent since 2026-05-17. **Fix:** replace substring match with an exact-host allowlist; deliver preview-env tokens server-side, not via origin matching. Also strip `http://localhost` CORS (audit C17) in the same pass. Detail: `audit-reports/2026-06-01.md` §C1/A1. **Closed (code) 2026-06-09 on `feat/t1-security-remediation`:** centralized `supabase/functions/_shared/cors.ts` (`buildCorsHeaders`), exact-host allowlist + `PREVIEW_ORIGINS` (full `https://` origins) + empty-origin guard; dropped `.vercel.app` substring and `http://localhost` across all 20 functions; webhooks keep `stripe-signature`; `reset-password` keeps its 4 Command-Suite origins via `extraOrigins`. C17 folded in. **DEPLOYED + VERIFIED 2026-06-09:** all 20 fns live (canary send-invoice → batch 17 → 2 webhooks); header smoke confirmed — allowed origins echo, `.vercel.app`/localhost reject, reset-password keeps Schedule Command origins. |
| S8    | T1   | Closed 2026-06-09 | [Sev 8 · Like 5 · Eff half-day] Stripe webhooks lack `event.id` idempotency → replayable double-billing | Weekly auto-audit 2026-06-01 (C5/C6) | **Money impact, relevant TODAY.** `stripe-webhook/index.ts:87` and `scc-stripe-webhook/index.ts:76` have no `event.id` dedupe. Stripe delivers at-least-once, so a replayed `checkout.session.completed` re-marks an invoice Paid, re-invokes `qb-record-payment` (duplicate QB Payment — `qb_payment_id` overwritten with no guard), and re-sends receipts → customer billed twice in QB. **Fix:** insert `event.id` into a `processed_stripe_events` PK table at handler top; abort 200 on conflict (both webhooks). Detail: `audit-reports/2026-06-01.md` §C5/C6. **Closed (code) 2026-06-09 on `feat/t1-security-remediation`:** new `processed_stripe_events` table (migration `20260609120000`, `event_id` PK, `status` claimed/done, `tenant_id` nullable/NULL) + two-phase claim→`markDone()`-before-every-2xx in both webhooks (`'claimed'` left on 4xx/5xx → Stripe retries). **Note:** S8 is an at-least-once guarantee (no silent lost payment); a concurrent/retry race can still double-charge until **ADJ-1** makes `qb-record-payment` idempotent — ADJ-1 is the very next loop. **DEPLOYED 2026-06-09:** `processed_stripe_events` table applied (via SQL editor + `migration repair --status applied` — `supabase db push` was blocked by sibling-repo ledger divergence, NOT a ledger rewrite); both webhooks live; health smoke OK (bad-sig→400, no crash on S8 path). **Stripe replay smoke (first delivery→`done` / replay→`{duplicate:true}`) still PENDING** (needs a real Stripe-signed event). |
| L15   | T3   | Open        | `qb_connection` stores refresh tokens in plaintext                                                | Original audit 2026-04-26       | Move to Supabase Vault. Low likelihood (single tenant, prod is locked down) but real debt; defer until Vault wiring is needed for another reason. |
| S10   | T3   | Open        | [Sev 5 · Like 3 · Eff half-day] `job-attachments` DELETE policy is bucket-wide → cross-tenant object deletion vector | invoice-attachments Round-2 audit 2026-07-02 (adjacent) | The `job-attachments` storage DELETE policy (migration `20260420120000`) is not tenant/path-scoped: any authenticated user can `storage.remove()` any object in the bucket. The F39 invoice-attachments feature stores a client-set `storage_path` and deletes by it — a crafted `storage_path` could target another tenant's object. Pre-existing infra, surfaced (not caused) by F39. **Fix:** tenant/path-scoped storage RLS on `job-attachments` (restrict DELETE to objects under the caller's tenant prefix), authored in `command-suite-db`. Also the mitigation the F39 remove-orphan sweep depends on (plan §4.2). Not fixed in the F39 loop. **SCOPING NOTE (2026-07-06 — deferred to its own /decide→plan session, heavier than the T3 line implies):** the "tenant prefix" framing doesn't hold — object paths in `job-attachments` encode ENTITY ids, not tenant (`invoice-attachments/{invoiceId}/…`, `pay-app-waivers|pay-app-completed|pay-app-templates/{payAppId|customerId}/…`, `pay-app-sov/…`, `proposal-{id}/…`, `{jobId}/…`, `{proposalId}/contract-…`), so there's no tenant segment to scope on. True tenant-scoping would need a per-prefix path→entity→tenant JOIN in the policy (fragile) OR a path-scheme migration (big). Likely cleaner fix: **owner-based DELETE policy** (`bucket_id='job-attachments' AND owner = auth.uid()`) — fully closes the cross-tenant `storage.remove` vector (you can only delete objects you uploaded); confirmed NO server-side/service-role uploads to this bucket (owner is reliably the uploader's auth.uid). Tradeoff to weigh: same-tenant cross-user delete would no-op → benign orphan file (bounded). Bucket is SHARED across Sales/Schedule/Field → cross-repo check + 6-gate RLS deploy required (CLAUDE_RLS.md); authored in command-suite-db. |
| S11   | T3   | Open        | [Sev 4 · Like 3 · Eff half-day] Invoice attachments (lien releases / release waivers) live in the PUBLIC `job-attachments` bucket — reachable by URL with no auth | F39 invoice-attachments security review 2026-07-02 (L1) | Persisted invoice docs are protected only by `crypto.randomUUID()` path entropy — anyone with the URL reads them, no auth. Matches the existing `job-attachments` public-bucket pattern and the plan's §4.1 **accepted v1 design**, but F39 newly places legal/financial documents there (the earlier waiver was the same pattern; this generalizes it). **Fix (if deemed sensitive):** move to a private bucket + signed URLs — the send-invoice edge fn already fetches server-side via service context, and the historical list could sign on demand. Plan §4.1 documented this as the "stronger alternative not chosen for v1." Not a blocker; hardening debt. |
| S12   | T6   | Closed 2026-07-22 | [Sev 9 · Like 5 · Eff half-day] `team_members` write policies allowed intra-tenant **self-elevation** (a Sales Rep flips own `role` → Admin, unlocking every role-gated policy) — audit finding **B11** (distinct from the pay-app "B11" at row 87, an unrelated ID reuse) | Weekly auto-audit 2026-06-28 (B11, CRITICAL; carried from 2026-06-01 / 2026-06-22) | **Fix authored + deployed in `command-suite-db`** (migration `20260722120000_team_members_role_lockdown.sql`, plan `docs/plans/b11_team_member_role_lockdown.md` LOCKED). §4a `team_members_update` → self-or-admin; §4b `insert/delete` → admin/manager only; §4c `guard_team_member_privilege_cols()` BEFORE trigger blocks a non-admin changing `role`/`apps`/`active` on their own row (gates on `auth.uid() IS NOT NULL` so service_role's `deactivate-user` still works; `onboarded` left open so new-hire WelcomeScreen is unaffected). **DEPLOYED to shared prod `pbgvgjjuhnpsumnowuym` 2026-07-22; §8 smoke-verified against the live DB with real GoTrue-minted JWTs — 12/12 tests pass** (non-admin role/apps/active/insert/delete all blocked by guard P0001 or RLS 0-rows; onboarded self-write succeeds; admin change/self-demote/add/delete succeed; service_role deactivate succeeds; trigger present in `pg_trigger`). Throwaway test users used + cleaned up (tenant back to 5 members, 0 residue). |
| —     | T4   | Open        | Triage remaining 13 Medium + 9 Low audit findings                                                 | Deep audit (branch was `claude/sweet-johnson-vvCCt`, deleted by v93 cleanup) | T4 because the audit report itself needs to be retrieved from PR/cache before triage can start — branch was deleted with other claude/* branches. Find the artifact, then re-tier. |
| ADJ-1 | T2   | Open        | [Sev 7 · Like 4 · Eff half-day] `qb-record-payment` is not idempotent — duplicate QB Payment on replay/double-invoke | Round-1 audit of `docs/plans/t1_security_remediation.md` (2026-06-09) | **S8 does NOT close this.** `qb-record-payment` overwrites `qb_payment_id` with no guard; reachable via Stripe webhook replay (S8's two-phase dedupe stops the *webhook* re-running, but not a second invocation from any source) AND the two UI paths `src/pages/Invoices.jsx:1124` and `:1160`. Same class as audit C16. **Fix:** guard on existing `qb_payment_id` / dedupe by Stripe payment id before recording the QB Payment. |
| S13   | T3   | Open        | [Sev 8 · Like 1 · Eff half-day] `jobs` table has a permissive **"Authenticated users can do everything" ALL policy** that OR-overrides its tenant-scoped `jobs_select/insert/update/delete` policies → `jobs` has no real tenant isolation | T6 /security-review of `fix/pullback-resend-teardown` (2026-08-26) | **Blocks: F7. Non-exploitable TODAY (1 tenant — `tenant_config` count = 1) and NOT introduced by PB-1** (surfaced while confirming the PB-1 rollback `jobs` DELETE couldn't be steered cross-row). Once a second tenant onboards, any authenticated user could SELECT/UPDATE/DELETE any tenant's `jobs` rows because the permissive ALL policy is OR'd with the tenant predicates and wins. Same multi-tenant-isolation class as S4/S5 (all F7-gated). **Fix:** drop/replace the catch-all ALL policy on `jobs` with tenant-scoped-only policies (authored in `command-suite-db`; 6-gate RLS deploy). Do NOT gate the PB-1 merge on this. Note the `job_wtcs` child table IS properly tenant-scoped on all four verbs (verified 2026-08-26) — the gap is on `jobs` itself. |
| S14   | T3   | Open        | [Sev 8 · Like 1 · Eff half-day] `daily_log_entries` authenticated SELECT policy is `USING (true)` — no tenant isolation (the table has **no `tenant_id` column** to scope on) | T6 /security-review of `feat/schedule-merge-plan` Phase 3 Field web (2026-09-02) | **Blocks: F7. Non-exploitable TODAY (1 tenant — `tenant_config` count = 1; table has 3 rows, all the one tenant) and NOT introduced by this branch** (surfaced while confirming the Field-web reads scope correctly — Phase 3 added `src/field` Today + Daily Logs screens as the first Sales-side authenticated *readers* of `daily_log_entries`, but the exploit path is direct PostgREST, independent of the UI). Policy `daily_log_select_authenticated` = `USING (true)`, so once a second tenant onboards any authenticated user reads every tenant's daily-log notes. anon has **0 policies** on the table (RLS on → not anon-exposed). Same multi-tenant class as S13/S4/S5 (all F7-gated). **Fix:** `daily_log_entries` anchors `job_id` on `call_log.id` — scope via `EXISTS (SELECT 1 FROM call_log c WHERE c.id = daily_log_entries.job_id AND c.tenant_id = get_user_tenant_id())`, OR add a `tenant_id` column + backfill + consistency trigger (mirror S4). Authored in `command-suite-db`, 6-gate RLS deploy. Do NOT gate the Phase 3 merge on this. |
| S15   | T4   | Open        | [Sev 3 · Like 2 · Eff half-day] AR Command live-backend hardening — three defense-in-depth notes from the Phase 4 /security-review (T6), ALL gated on AR's future live-Supabase phase (none exploitable today) | T6 /security-review of `feat/schedule-merge-plan` Phase 4 (AR moves in) 2026-09-02 | **Repo: sales-command (+ command-suite-db when AR gets a backend). 0 exploitable-today — Phase 4 is a COSMETIC MOUNT: AR data is entirely client-local (localStorage `ar7-*` + the user's own Excel upload), no DB/RLS/edge-fn/grant/token/dep added, and `GroupGuard app="ar"` is fail-closed (renders for nobody unless "ar" is in AVAILABLE_APPS AND tenantApps AND memberApps).** Three notes to carry into the AR-backend phase, none reachable by an unauthorized actor now: **(1)** `src/ar/lib/exportUtils.js:65` `w.document.write(h)` builds print HTML where `bodyHtml` interpolates uploaded AR data unescaped (only `title` is `esc()`'d) — worst case is **self-XSS in your own print preview** (source = your own upload, rendered in your own window; pre-existing code moved wholesale, not introduced here). Escape the interpolated data if AR ever renders another actor's data. **(2)** Client-side `GroupGuard` is the ONLY mount control — there is no server-side AR authz because there is no server-side AR data yet; real authz must land WITH the live-Supabase backend (a client guard is not a boundary). **(3)** AR data persists in browser localStorage unencrypted — same-origin/same-user only today; revisit sensitivity when AR holds live A/R data. Do NOT gate the Phase 4 merge on any of these. |
| ADJ-2 | T3   | Open        | [Sev 5 · Like 4 · Eff 1h] `reset-password` trusts caller `Origin` to choose branding + redirectUrl | Round-1 audit of t1 security plan (2026-06-09); audit C12 | S7 does **not** close this (CORS gates no execution). **AUDIT 2026-06-10 — premise corrected:** the framing "trusts caller `Origin`" is mechanically wrong. `reset-password/index.ts:66` references a bare `origin` identifier that is **never bound from `req`** — it resolves to the Deno worker **global**, not the caller's `Origin` header. So this is a **latent wrong-branding bug** (branding derived from the wrong source entirely), NOT an attacker-controllable input. Re-scope before building. **Fix (unchanged):** resolve branding/redirect server-side from the tenant. |
| ADJ-3 | T4   | Open        | `check-orphan-users` has no CORS check at all                                                      | Round-1 audit of t1 security plan (2026-06-09) | **Tracking only — NOT a defect.** It's a cron target, not browser-called, so it correctly has no CORS handling. Recorded so a future reader doesn't "add CORS for consistency." No action. |
| ADJ-4 | T4   | Open        | [Sev 2 · Like 3 · Eff 1h] Missing `Access-Control-Allow-Methods` header on CORS responses (audit C18) | Round-1 audit of t1 security plan (2026-06-09); audit C18 | Opportunistic only. Zero-cost fold into `supabase/functions/_shared/cors.ts` (`Access-Control-Allow-Methods: "POST, OPTIONS"`). **AUDIT 2026-06-10:** precondition satisfied — S7 has shipped, `_shared/cors.ts` is live and the shared resolver, so this is now an actionable one-line add (`buildCorsHeaders` returns only Allow-Origin + Allow-Headers today). Still not worth a standalone pass; fold into the next `_shared/cors.ts` touch. |
| S9 | T3 | Open | [Sev 3 · Like 9 · Eff 1h] orphan-user alarm cron has never fired — missing `app.settings.supabase_url` GUC (+ new-key auth) | T1 deploy 2026-06-10 (Loop #33) | **Pre-existing, surfaced during S6 deploy — NOT caused by S6.** Cron `check-orphan-users-daily` fails at `current_setting('app.settings.supabase_url')` (unset; one-arg form raises) BEFORE auth is evaluated — confirmed via `cron.job_run_details` 2026-06-10 06:00 UTC: "unrecognized configuration parameter". The original `20260520095500` job used the same line, so the alarm has never fired since 2026-05-20 (S2 marked Closed but non-functional). S6 security gate is unaffected/verified (unauth→401). **Fix (own mini-loop):** hardcode the non-secret URL (`https://pbgvgjjuhnpsumnowuym.supabase.co`), source `service_role_key` from Vault (GUC unsettable — see custom-GUC-blocked memory), and resolve verify_jwt vs new-format `sb_` keys — likely set `check-orphan-users` `verify_jwt=false` and rely solely on the handler `x-cron-secret` gate (already the real gate). Monitoring-only; zero money/customer impact. |
| S13 | T2   | Open   | [Sev 5 · Like 3 · Eff ~2h] `proposal_wtc` carries a TABLE-level anon SELECT grant — an invoice-link holder can query our cost basis and margin directly | /security-review, 2026-08-10 (T&M billing build) | **Repo: command-suite-db (grant) + sales-command (verify readers).** **Pre-existing — NOT introduced by the T&M build**, but that build's public-page allow-list only removed the cost columns from the DOM; it did not close the hole. Verified against live prod: `proposal_wtc` has a **table-level** anon SELECT grant, with RLS `proposal_wtc_public_view_token` scoping rows through the invoice-line join to `request_viewing_token()`. So RLS correctly stops cross-customer walking — a holder sees only their own job's work types — but within that scope every column is readable, including `burden_rate`, `ot_burden_rate`, `pw_rate`, `pw_ot_rate`, `markup_pct`, `materials` and `discount`. **Anyone holding an invoice link can hit PostgREST directly with the public anon key and read what the job costs us and what we made on it.** This is the true H6 class (cost basis over the wire) — the same reason `PublicSigningPage` was moved behind a `SECURITY DEFINER` RPC and told never to re-import calc helpers. Client-side allow-lists cannot fix it; the client is not the boundary. **Fix:** harden `proposal_wtc` the way `invoices` already is — `REVOKE SELECT ON TABLE public.proposal_wtc FROM anon`, then `GRANT SELECT(col)` individually for the display set only: `id, proposal_id, work_type_id, is_rate_card, rate_class, rate_amount, locked_line_total, sales_sow` plus whatever the signing-page RPC needs. **Sequencing note:** the REVOKE is an ABSENCE, so it will need a hand-added line in `supabase/baseline/prod_public_schema.sql` exactly like the invoices one (no dump emits an absence), a second entry in `anon_hardened_allowlist.txt`, and `anon_hardened_pending.txt` staging — the rehearsal gate currently assumes `invoices` is the ONLY hardened table (`scripts/rehearse/anon_hardened_allowlist.txt:4`), so that assumption has to be widened first or the gate will fail on drift. **Then grep every anon reader** (`PublicInvoicePage.jsx`, `get_public_proposal_view`) and confirm none needs a revoked column. |

### Bugs

**B114 — T2 · Closed 2026-09-10 (PR47, `00050f0`; DB PR2 / migration 20260910200000): Require titled trips and explicit crew links going forward.** Chris requested preventive enforcement after B113: nonblank trip titles, every new crew assignment linked to a real trip, and creation/import/send paths checked before writes. UI validation plus database backstop. Existing historical unlinked crew remains intact for explicit repair; no bulk date guesses. Focused browser regressions, DB title/link/deletion tests and six concurrency cases passed. Vercel preview ds17qqe04 inspected; production deployment succeeded. All 3,315 full crew rows had identical before/after fingerprints. Legacy crew import stops before its first write because the source has no trip links.

**B113 — T2 · Closed 2026-09-10 (PR #46, `d2ee2af`; DB PR #1, `8bde5a1`): Trip-owned crew and empty-trip deletion.** Job 10262's Sep 14–16 trip shows zero crew but deletion is blocked by nine unlinked Sep 17–19 job assignments. Chris authorized preserving Daniel Luna, Jacob Medina and Jorge Ramirez on Sep 17–19 and deleting the empty trip. Remove date-inferred trip ownership; trip deletion checks the saved assignment trip FK, whole-job deletion still checks all crew. Crew Schedule new assignments must select a saved trip. Original job dates remain reference data rather than an extra synthetic staffing row on Crew Schedule, including parent-only jobs such as 10088. Its eight Sep 14–17 crew days and four older crew days were read-only verified; no live 10088 changes authorized or made. Database safeguards, six concurrency cases and final Vercel preview passed; production deployment succeeded. Live 10262 repair preserved nine IDs/names/dates, trip c0232d75-ac61-4462-8ef0-deb3dac47248. Broader imported/historical link cleanup and database-wide mandatory-trip enforcement remain separate; do not bulk-repair job 7215 or other jobs.

**Crew backend follow-up — Open, narrowed after B115 on 2026-09-10:** B114 enforces new titled trips/crew links and blocks legacy import before writes. B115 completed the authorized historical NULL-link conversion with exact preservation. Remaining separately authorized work: review existing non-NULL links whose crew dates fall outside their trip (known 7215 Sep 28–29 assignments linked to Sep 21–22 trip), and any importer/legacy-client workflow changes Chris elects to pursue. Do not resume those automatically. SOW-1 separately covers cross-app SOW and phone sync. Earlier B113/B114 entries describe their release scope, not the later B115 authorization.

**B112 — T2 · Closed 2026-09-09 (PR #45, production `e943998`): One allocation model for Crew Schedule board, crew cards, and capacity header.** Chris reproduced Jacob's September 14 double booking hidden by job-date filtering and 7215's September 28 crew linked to a September 21 trip. Board now retains all weekly assignments, including inactive/off-date jobs and unavailable-job issue rows; crew cards, conflicts, capacity headcounts/detail and trip summary badges use the same rows. Count each active person once per day, preserve time-off precedence, flag ambiguous/off-date links without rewriting crew data. Tests: focused model regression, real board fixture, capacity-summary browser regression, build. Merged to main; completed feature branches removed. Handoffs v251–v254. PR #45 includes requested crew-card polish: compact job rows beside wrapping names, larger job numbers, weekday initials over bubbles, first-name alphabetical order within each group. **Scope boundary:** this fixes read/display consistency, not all assignment writers or a mandatory trip FK. Existing bad links are not repaired automatically; no new migration or production data edits. App deployed successfully after Chris approved release; live site serves index-Dmi34yjq.js.

**B111 — T2 · Closed 2026-09-09 (app `dfded07`, PR #44; DB `677efa0`, release baseline `1746417`): Crew-first job/trip deletion.** Add Delete trip inside the expanded Crew Schedule trip; reuse one preflight + two-confirmation flow in both trip editors and whole-job deletion. Never clear crew automatically: assigned crew or unlinked crew days hard-block deletion until manually removed/reassigned. New command-suite-db migration guards soft/hard job deletion and trip deletion, rejects assignments to hidden jobs, checks recorded work/pull tickets/scope dependencies, and writes deletion audits atomically. Actual-work checks are conservatively job-wide because Field has no trip FK; phones with unsynced work cannot be certified remotely. Existing scope versions and tagged SOW days block deletion rather than being discarded. Existing historical ghosts are not cleaned up by this build. Branch `codex/crew-first-deletion` in both repos; DB counterpart DEL-1. See handoff v248 for exact paths, review boundaries and verification. SOW-1 implementation, B107/B108 printing, and broad scheduling date-authority changes remain separate.

**B111 review-blocker correction — 2026-09-09, independently cleared and deployed:** Fixed the database bypass for crew whose `job_id` is NULL but whose trip belongs to the job. Deletion now recognizes the trip relationship; inserts/updates require a matching parent job. Authenticated database regressions and all six concurrency scenarios pass. App implementation unchanged by the correction. Re-review found no blockers. Migration 20260909160000 applied through the standard gates after live fingerprint verification, then PR #44 merged and Vercel production succeeded. Post-release baseline, deletion regressions, and six concurrency scenarios pass. See handoff v250 for release verification and accepted limits.

**SOW-1 — Open, investigation saved 2026-09-09:** Reconcile the WTC → proposal → Schedule job/trips → Field phone SOW pipeline. Confirmed separate scope/date paths, stale PRT reader and unsafe fallback, phone job-resolution ambiguity, and missing trip/crew sync. See [read-only investigation and proposed repair sequence](audit/sow_pipeline_2026-09-09.md). References existing Field B2/FE1 and prior DMS plans; not a new approved build plan. Implementation and live data repairs are not authorized; resume with Chris.

| ID  | Tier | Status | Item                                                                          | Source           | Notes                                                                                                                                                       |
|-----|------|--------|-------------------------------------------------------------------------------|------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------|
| B107 | T2 | Open — separately deferred by Chris 2026-09-08 | [Sev 6 · Like 6 · Eff half-day] **Week Schedule print excludes Scheduled and In Progress jobs.** | Independent review of 69b158b..0483c1d | `src/schedule/lib/exports.js`, `printWeekSchedule`: only Ongoing/On Hold pass its status filter, unlike Crew Schedule's normalized active statuses. Reproduce with a Scheduled or In Progress job/trip in the current week. Pre-existing; Chris explicitly agreed to track separately from the bounded Trips/Add-to-Schedule release. Not a blocker for that release; do not silently widen the next audit to fix it. |
| B108 | T2 | Open — separately deferred by Chris 2026-09-08 | [Sev 5 · Like 7 · Eff half-day] **Week Schedule print ignores the week being viewed.** | Independent review of 69b158b..0483c1d | `printWeekSchedule()` computes Monday from `new Date()` instead of accepting the visible week's date. Reproduce by viewing Oct 12 while today's week is Sep 7, then exporting Week Schedule. Pre-existing; Chris explicitly agreed to track separately from the bounded Trips/Add-to-Schedule release. Follow up with visible-week input and a browser test; no print implementation changes in v227. |
| B87 | T2 | In Progress (BUILT + gates clean 2026-09-05 — ready to merge; buildvsplan Daily-crew gap fixed, review Calendar start-only regression fixed, #2/#3 filed as B89/B90) | [Sev 6 · Like 8 · Eff half-day] **Schedule board can't show a job on a second date block (go-backs/return trips are invisible)** — the crew board picks jobs by the job's own start/end only (`Schedule.jsx` `jobOverlapsWeek`→`effStart/effEnd`), blind to the dated blocks on `job_mobilizations`, so a go-back added via the new "+ Job" flow saves but never appears on the week it's scheduled for | Smoke of B86 workstream A, 2026-09-05 (go-back on job 7069 for Sep 19 saved but not on board) | **Repo: sales-command (`src/schedule/`). This is the second half of B86 — the "+ Job → go-back" flow isn't usable until the board renders it.** Model (locked): a job has ≥1 **allocations** = blocks of days on the schedule; continuous=one, gap=next; the job's start/end is allocation #1; a go-back is just an allocation flagged for cost. Naming: "allocation" on the scheduler, "mobilization" stays the job-card/cost label — don't show "mobilization" in scheduler UI. Build has TWO coupled halves + a migration: (1) **per-allocation details** — `job_mobilizations` gets its own `crew_needed`/`lead`/`equipment`(+`vehicle`/`power_source`)/`sow` columns (NOT prevailing_wage — job-level, inherited), authored in command-suite-db + rehearsed; the "+ Job" panel restores those inputs (a go-back runs different crew/scope than the first run — Chris 2026-09-05); (2) **board reads allocations** — load `job_mobilizations` for the visible set, a job shows in a week if its own dates OR any allocation overlap, block's crew_needed drives its count; cover `Schedule`/`Calendar`/`Daily` + matching `exports.js`. Out of scope: billing/cost, job-card mobilization view, workstream B. Design + scope in `docs/plans/add_job_dedup_ideate_prompt.md` AMENDMENT "allocation-aware schedule board". **PICKUP STATE (2026-09-05, new terminal starts here):** migration is PRE-AUTHORED but PARKED OUT of the auto-swept migrations dir (so another terminal's `db:push` can't grab it) → `command-suite-db/docs/pending-migrations/20260905120000_job_mobilizations_allocation_details.sql.pending` — move it back into `supabase/migrations/` (drop the `.pending`) when ready to push. (6 additive nullable cols on `job_mobilizations`: crew_needed int + lead/vehicle/equipment/power_source/sow text; RLS inherited, no grants). **DB PUSH BLOCKER:** rehearse refused — baseline drifted. Two causes: (1) `migration_match_draft` (20260904120000, legit, applied) needs an EXPECT_* constants + baseline catch-up — public-schema drift is ONLY this (tables 54→55, indexes 170→172, policies 227→231, triggers 49→50, table_grants 1133→1154, column_grants 9220→9292); (2) prod also has an **out-of-band `migration_backup` schema** — 13 backup tables dated `202609041653` from the Sept-4 schedule-import ("Apply") run, NOT from any committed migration — decide whether it belongs in the baseline before `supabase db dump` bakes it in. Guardrail (B86) merged to main `fcd1037` WITHOUT this — no DB dependency. **BUILT 2026-09-05 (branch `feat/allocation-aware-board`):** migration applied to prod + merged to command-suite-db main (20260905120000); the two DB blockers are RESOLVED — `migration_match_draft` EXPECT_*/baseline catch-up done, and the `migration_backup` schema was DROPPED from prod (verified all 140 rows orphaned/test-noise) so the baseline is clean. Frontend: new `src/schedule/lib/allocations.js` (jobRanges/overlapsWeek/inRange); `queries.js` reads+writes the 6 cols (loadMobilizationsByJobId gains `liveOnly`, add/updateJobMobilization carry the fields); board reads wired allocation-aware in `Schedule.jsx`, `Calendar.jsx`, `Daily.jsx`, `exports.js`; "+ Job" panel restores crew#/lead/vehicle/equipment/power/scope inputs. Verified vs prod: job 1058 (disp 7069 Clorox) own dates Jul 2–5 + Sep 19 go-back now yields both ranges. Remaining: cold buildvsplan/code-review/security-review + in-browser smoke. |
| B86 | T2 | Closed 2026-09-05 (merge `db38565`, live prod) | [Sev 7 · Like 7 · Eff ~half-day] **Add-Job dedup — workstream A (guardrail): "+ Job" can no longer mint a phantom, Sales-unlinked job.** (Guardrail scope only — the allocation-aware board + per-trip fields are B87, still open. Interim "+ Job" is date+go-back until B87 lands.) The schedule "+ Job" button blind-INSERTed a `jobs` row with no `call_log_id` → phantom duplicates (the 10252 "Crew not assigned" mismatch). Workstream A reworks the button to search→pick an existing Sales-linked job→add a mobilization (with go-back flag), or BLOCK ("create it in Sales first"); hides existing orphans (null `call_log_id`) from every live reader incl. exports; adds an Unallocated data helper for workstream B. | `docs/plans/add_job_dedup_ideate_prompt.md` (BUILD PLAN rev2, 2-round audited); reframed from F58 2026-09-04 | **Repo: sales-command (`src/schedule/`).** **BUILT on `feat/add-job-dedup` 2026-09-05** — 3 files, no migration: `ScheduleLayout.jsx` (Add-to-Schedule modal: debounced search dropdown → mobilization panel / block), `queries.js` (`searchExistingJobs` call_log⨝jobs!inner on bare `job_number`; `loadJobs` `includeUnlinked` orphan filter; N1 parent-null guard INSIDE `addJobMobilization` so MobsModal is covered too; `getNextMobSeq`; `loadUnallocatedJobs`), `exports.js` (4 raw `jobs` reads get the orphan filter). `npm run build` clean; query logic prod-SQL-verified. **GATES:** buildvsplan ✅ (0 blockers; T2-1 paginate + T2-2 plan-reason fixed, commit `464e086`); code-review ✅ (0 ship-blockers, `/code-review-limiter`); security-review ✅ (0 exploitable-today, `/security-review-limiter` — read-narrowing + JS-side search + write-guard, all under existing authenticated RLS; no migration/grant/RLS/anon change). **All gates green.** **PENDING:** merge to main (auto-deploys via Vercel; no migration/edge-fn, so merge = make-it-live) + browser smoke. **Filed UX polish (cosmetic, non-blocking — fold into workstream B or fast-follow):** (1) search dropdown caps at 25 with no "showing 25, refine" hint; (2) `jobResults` isn't cleared on a new keystroke so the prior term's list lingers ~300ms during re-search (standard anti-flicker, not misleading). **Deferred to B (not this row):** cleanup/merge of existing 8 phantoms + 67 orphans, incl. retiring 10252 job_id 1278. **Shop Work overhead type split to `docs/plans/shop_work_overhead_type.md`.** Adjacent rows: B84 (restore-orphan guard), B85 (no realtime). |
| B84 | T4 | Open | [Sev 3 · Like 2 · Eff ~1h] **"Restore" can un-park an unallocated/orphan job back into live views** — `restoreJob` (`Jobs.jsx:387`) un-deletes any tombstoned row including a null-`call_log_id` one | Adjacent finding (J1), add-job dedup round-1 audit 2026-09-05 (feat/add-job-dedup) | **Repo: sales-command (Schedule Command).** Surfaced by the add-job dedup audit. Once workstream A hides null-`call_log_id` orphans from live views via the `loadJobs()` filter, a restore of a tombstoned orphan is re-hidden anyway — so this is mostly latent. But `restoreJob` should still respect the orphan guard (don't resurrect an unlinked row into a live status). Fix alongside or after the dedup guardrail. Low urgency (1 tenant). |
| B88 | T4 | Closed 2026-09-05 | [Sev 2 · Like 3 · Eff ~1h] **Schedule reskin — code-review nits; ALL (1)-(5) done 2026-09-05** — chunk-1 dashboard/list polish (renumbered from a B84 collision on the reskin branch, merged 2026-09-05) | /code-review 2026-09-04 (schedule reskin chunk 1, `feat/schedule-reskin`; limiter verdict = file, not fix) | **Repo: sales-command.** (1) DONE (already guarded — `Jobs.jsx:258` filters `m => m && m.is_go_back`, no change needed). (2) DONE — `Home.jsx` workload memo now tallies `scheduledNoDate` + renders "+$X scheduled, no start date". (3) DONE — `Home.jsx` now sets `activity` to `[]` on `actRes.error`. (4) DONE — Home's `job_changes` select narrowed from `*` to `id, field, new_value, job_id, created_at`. **(5) DONE 2026-09-05 — `command-suite-db` migration 20260905130000_billing_worklist_writes_admin_only:** added `is_admin()` (SECURITY DEFINER, Admin-only, mirrors `is_admin_or_manager`) and ANDed it into billing_worklist INSERT/UPDATE/DELETE policies; SELECT left open (worklist stays readable in-tenant). Admin-only (not Admin/Manager) to match Billing.jsx `canEdit === 'Admin'` + the ask. No app change needed (UI already gated + handles rejection). Rehearsed clean, no anon exposure; verified vs prod: 2 Admins admitted, 3 Sales Reps blocked; live policies confirmed carrying `is_admin()`. **B88 fully closed (1-5 all done).** |
| B89 | T4 | Closed 2026-09-05 | [Sev 2 · Like 2 · Eff ~1h] **`allocForWeek` duplicated across 3 schedule files + inconsistent field-merge** — DONE: extracted `allocForWeek(allocsForJob, ws, we)` + `pickAllocField(alloc, job, field)` into `src/schedule/lib/allocations.js`; Schedule/Daily/exports now import them; the field-merge is one consistent rule (non-null/non-undefined/non-empty → alloc, else job). Build green. — the "which allocation block overlaps the visible week" helper is copy-pasted in `Schedule.jsx`, `Daily.jsx`, `exports.js`; the field-override checks after each copy differ (`exports`: `!= null && !== ''`; `Daily`: falsy; `Schedule`: `!= null`) | /code-review 2026-09-05 (B87 allocation-aware board, `feat/allocation-aware-board`; limiter = file) | **Repo: sales-command (`src/schedule/`).** Cleanup, no user-facing bug for these text/int fields. Extract `allocForWeek(j, allocs, ws, we)` + a shared field-pick into `src/schedule/lib/allocations.js` alongside `jobRanges`/`overlapsWeek`/`inRange`, and consolidate the merge rule. Reduces future drift. Do when next touching the board. |
| B90 | T4 | Closed 2026-09-05 | [Sev 3 · Like 2 · Eff ~1h] **`loadMobilizationsByJobId` unpaginated — go-back blocks silently drop past 1000 rows** — DONE: replaced the single `.select().in('job_id',…)` with `loadAllRows('job_mobilizations', …, { orderBy: 'id', filterFn: q => q.in('job_id', jobIds) })` which pages with `.range()`. Build green. — single `.select().in('job_id', …)` with no `.range()`; PostgREST caps at 1000, so once live `job_mobilizations` exceeds that, some allocation blocks stop rendering on the board with no error | /code-review 2026-09-05 (B87 allocation-aware board) | **Repo: sales-command (`src/schedule/lib/queries.js`).** Latent + pre-existing pattern; matches the known PostgREST 1000-row rule. Paginate via the existing `fetchAll()` helper (`supabaseHelpers.js`). Not urgent — current live `job_mobilizations` count is far under 1000 — but the board's correctness now depends on it, so fix before the table grows. |
| B91 | T4 | Open (part 2 DOC done 2026-09-05; part 1 revoke deliberately deferred) | [Sev 2 · Like 1 · Eff ~half-day] **Latent unused anon DML grants suite-wide (defense-in-depth) — DOC fixed; the revoke deferred on purpose** — anon holds table-level SELECT/INSERT/UPDATE grants on `job_mobilizations`, but RLS is on with authenticated-only policies (no anon policy) so anon reads/writes **zero** rows; the grant is inert. Pre-existing, suite-wide pattern — NOT introduced by B87 | /security-review 2026-09-05 (B87, `feat/allocation-aware-board`; limiter = file, 0 exploitable) | **Repo: command-suite-db.** (2) DOC — **DONE 2026-09-05** (`3dfac8f`): corrected the migration `20260905120000` comment + the rehearse.sh EXPECT_COLUMN_GRANTS note (both said/implied "authenticated-only / anon full-read"; accurate = inert table-level anon grant, RLS blocks anon). (1) HARDENING revoke — **DEFERRED ON PURPOSE.** The security reviewer itself said "not this pass," and it's a genuinely riskier change than a knockout: "suite-wide" means auditing every table's anon grants and revoking only the truly-inert ones, because some anon grants ARE load-bearing (invoices/proposals public pages — the stripe_checkout_url incident). Zero real exposure today (RLS blocks anon everywhere; no anon page reads these tables), so the risk/reward doesn't favor doing it reactively. If done later: audit anon grants per table FIRST (which are used by a public page vs inert), then revoke only inert ones, each rehearsed. Not a quick fix. |
| B92 | T4 | Open | [Sev 3 · Like 2 · Eff ~1h] **Calendar `assignments` query is unpaginated AND unordered** — `Calendar.jsx:248–252` does `.select('job_id, crew_name, date').gte/.lte(date)` with no `.range()` and no `.order()`; safe today (~42-day window << 1000 rows), but the same PostgREST-1000-cap class as B89/B90. If any future range widening (e.g. multi-month view) crosses 1000 rows it silently drops crew rows | Adjacent finding (ADJ-1), calendar-modernization plan round-1 audit 2026-09-06 (`feat/calendar-modernization`) | **Repo: sales-command (`src/schedule/views/Calendar.jsx`).** Pre-existing, NOT caused by the calendar-modernization feature (that work keeps the window bounded). Add `.order('date')` regardless; route through `loadAllRows`/`fetchAll` with `orderBy:'date'` if the fetch window can ever exceed 1000 rows. Not urgent while the window stays ~monthly. (Was a duplicate-B92 with the Subcon Home "Jobs Assigned count" finding; that row renumbered to B98 on 2026-09-06 to clear the collision.) |
| B96 | T4 | Open | [Sev 2 · Like 3 · Eff ~1h] **Mandatory-lead invariant leaks via the per-WTC SOW date editor** — editing `field_sow` dates rolls up to `jobs.scheduled_start/end` (`queries.js:1004`) with no lead check, so a job can still become scheduled — and render a blank-lead bar on the Calendar — without a crew lead. The allocation modal (`ScheduleLayout`) and the two direct date editors (`JobDetail`/`Schedule`) are hard-gated (calendar-modernization Chunk A + T2-1); this third path isn't. | /code-review 2026-09-06 (calendar-modernization Chunk A + T2-1, `feat/calendar-modernization`; limiter = SHOULD-FIX) | **Repo: sales-command (`src/schedule/`).** Soft UI invariant only — no crash/corruption; it's the exact pre-existing state the mandatory-lead feature narrows but doesn't claim to fully seal. To close: add the same lead guard where the SOW editor commits dates, or enforce at the `queries.js:1004` rollup (toast/return). Pairs with B97 (DB backstop). |
| B99 | T4 | Open | [Sev 2 · Like 4 · Eff ~1h] **Schedule board has no "clear job focus" control** — arriving at the board via `/schedule/schedule?job=<id>` (e.g. a card's "Build Schedule →") highlights + scrolls to that job but shows the full board; there's no button to drop the focus/highlight and stay on the plain crew schedule. Only "← Back to job" (returns to the card) exists. | Chris, 2026-09-06 (JobDetail-retire smoke) | **Repo: sales-command (`src/schedule/views/Schedule.jsx`).** Small UX add: a dismiss on the focus highlight (clear the `?job=` param in place) so the board returns to the normal view without leaving. Not blocking — the board already shows all jobs, the focus is only a highlight. |
| B100 | T4 | Open | [Sev 2 · Like 3 · Eff ~1h] **Calendar panes — minor post-merge cleanups (from B/C gates)** — non-blocking review findings: (a) JobsToPrepare deep-link sets a redundant `setManualDate(true)` (dead — `dateFilter='all'` already makes auto-fit a no-op); (b) CalendarJobPane mob-hydrate effect keys on `[job]` object → a background jobs reload re-fires it (brief "…" flash) — key on `job?.job_id`; (c) `membersByYmd` dedupe uses `list.some()` ≈ O(jobs²) per day (bounded; switch to a Set); (d) day-pane row shows `lead‖status`, not the §8.1 work-type subtitle; (e) job pane shows `crew_needed` labeled "Crew Needed" vs §8.2 "Crew Size (avg)". | /code-review + /buildvsplan 2026-09-06 (calendar-panes B/C; limiter = file) | **Repo: sales-command (`src/schedule/`).** All cosmetic/perf; none block. (d)/(e) firm up when the pane's fuller data lands. |
| B101 | T4 | Open | [Sev 1 · Like 3 · Eff ~30m] **Job pane "Open Job" + "Edit Schedule" are the same action** — both deep-link to `/schedule/jobs?job=<id>` (the card). Two buttons, one behavior. Decide: collapse to one, or differentiate (e.g. "Edit Schedule" → the crew board `/schedule/schedule?job=`). | /code-review + /buildvsplan 2026-09-06 (calendar job pane) | **Repo: sales-command (`src/schedule/components/CalendarJobPane.jsx`).** Product/UX decision, not a defect. |
| B102 | T4 | Closed 2026-09-06 (won't-fix — confirmed unused: Chris reads per-job billing only in Sales Command invoices / Jobs, never the retired Schedule `billing_log` tab; no card equivalent needed) | [Sev 2 · Like 1 · Eff confirm] **JobDetail retirement dropped the per-job Billing tab view** — the retired JobDetail "Billing" tab showed `billing_log` + a "% billed" bar; the inline card's BudgetPanel shows bid/cost/margin, not `billing_log`. `billing_log` is retired YESv2-import placeholder data (`printBillingReport` was removed for the same reason, `exports.js:110`); live billing = `/schedule/billing?tab=worklist` + forecast. | /buildvsplan 2026-09-06 (JobDetail retirement, Tier-2) | **Repo: sales-command.** Almost certainly immaterial — confirm HDSP doesn't read per-job `billing_log` history anywhere; if not, close won't-fix. If they do, add a small billing view to the card. |
| B103 | T1 | Closed 2026-09-08 (merge `548b7cc`, live prod; Chris confirmed board looks good) | [Sev 6 · Like 8 · Eff ~half-day] **Hidden orphan jobs drop real crews from the schedule** — schedule readers hid every job with a null `call_log_id`; B86 widened that hide on the premise "null link = phantom" — WRONG for jobs that have real crew. Symptom: Weekly Crew Capacity counts crew the board/calendar/exports don't show, tooltips show raw IDs ("Job 1054"), Daily Crew Status prints "—" for crew who ARE assigned. Data intact — display filter, not data loss. | Filed 2026-09-07 (T1); part of B86/B87 workstream | **Repo: sales-command (`src/schedule/`).** Rule (LOCKED): hide a null-link job only if it ALSO has no crew (an `assignments` row). "Has crew" is not a job column — it's the existence of an assignment for the `job_id`. **MODEL (Chris 2026-09-08, supersedes hide-unless-crewed): show EVERY active job, flag the broken ones — never hide.** The app must not decide what he doesn't see; real crew is never on something he shouldn't look at, and hidden bugs are the dangerous ones. **BUILT on `fix/b103-surface-crewed-orphans`:** `loadJobs()` now returns all active jobs (linked or not) — the `.not('call_log_id')` hide is gone everywhere (one choke point covers Schedule/Calendar/Daily/Home/Billing/Materials); `exports.js` routes through `loadJobs` so prints match. Unlinked jobs are FLAGGED in `normalizeJob`: `⚠` prefix on the number + `"Needs fixing — not linked to Sales"` when there's no name — one place, every surface. The earlier crewed-set filter + `loadCrewedJobIds` were removed; `includeUnlinked` param dropped. `npm run build` clean. **Prod context 2026-09-08:** ~60 unlinked-but-crewed jobs (incl. the 3 current: Dave Lee 1278/Bash Dave, K7 1054/Ary+Little, Kalb 990 + siblings) and 7 empty phantoms — all now visible; phantoms show flagged with no crew. Plan + amendment: `docs/plans/b103_surface_crewed_orphans.md`. **PENDING:** in-browser smoke + merge to main (no migration/edge-fn → merge = live). Part 3 (historical orphan cleanup, ~50 past-dated) deferred, no urgency. |
| B97 | T4 | Open | [Sev 2 · Like 2 · Eff ~half-day] **"Scheduled job has a lead" invariant is UI-only — no DB constraint** — the mandatory-lead rule (calendar-modernization Chunk A + T2-1) is enforced only in the app; a direct write or a legacy row can still be scheduled with a blank lead (the Lead `<select>` preserves `value=""` for existing blanks). | /code-review 2026-09-06 (calendar-modernization Chunk A + T2-1; limiter = HARDENING) | **Repo: command-suite-db (constraint/trigger) + sales-command (backfill).** Defense-in-depth, only needed if the invariant must be airtight. Would require a CHECK/trigger (lead NOT NULL when a scheduled date is set) plus a one-time backfill/cleanup of existing scheduled-but-lead-blank rows (rehearse first). Out of the Chunk-A diff's scope. Pairs with B96. |
| B85 | T4 | Open | [Sev 2 · Like 3 · Eff ~half-day] **Schedule surfaces don't live-update on job state change** — there's no real-time channel on `jobs`; orphan/overhead/status changes only appear on next load (pre-existing) | Adjacent finding (J2), add-job dedup round-1 audit 2026-09-05 (feat/add-job-dedup) | **Repo: sales-command (Schedule Command).** Pre-existing, not caused by the dedup work. The crew grid and dashboards refetch on mount/refresh, not via a realtime subscription, so a job flipped to unallocated (or, later, an overhead record) won't reflect until a reload. Not blocking — schedulers already use the Refresh action. File-and-forget unless staleness is reported. |
| B82 | T4 | Open | [Sev 2 · Like 4 · Eff ~2h] **Leftover HDSP crew-days sit on wrong dates** — some old imported crew assignments are dated outside their job's own schedule range (e.g. job 10053 runs Aug 24–Sep 4 but has Jul 27 crew rows) | Double-booking diagnosis 2026-09-04 (handoff v208) | **Repo: sales-command (data) / command-suite-db (if done via migration).** These came in through the one-time HDSP import, not from normal app use — the current assign screen greys out days outside a job's range, so no NEW ones can be created here. The double-booking false-positive that surfaced these is ALREADY FIXED (detection now scoped to on-board jobs), so this leftover data no longer causes a visible bug. Cleanup only: find assignments whose `date` falls outside their job's `scheduled_start`/`scheduled_end` (or `start_date`/`end_date`) and delete or re-date them. Low urgency — cosmetic once the display fix is live. |
| B83 | T4 | Open | [Sev 3 · Like 3 · Eff ~half-day] **Tenant import can bring in crew-days dated outside a job's range** — the schedule import path (`src/schedule/lib/importData.js`) inserts assignment rows without checking each date against the job's own schedule window, so a 2nd tenant onboarding could reintroduce the same stray data B82 describes | Double-booking diagnosis 2026-09-04 (handoff v208) | **Repo: sales-command.** This is the "stop it happening again" guard. Normal in-app use already can't create out-of-range assignments (assign modal disables out-of-range days), so the ONLY remaining entry point is import. Fix: in the importer, drop or clamp any crew-day whose date is outside the matched job's range, and surface a count of what was dropped. Do before the next tenant import, not urgent while HDSP is the only tenant. Pairs with B82 (clean the existing) — B83 prevents new. |
| B77 | T2 | In Progress | **RESET half SHIPPED + LIVE + VERIFIED in prod 2026-09-03; INVITE half still open.** [Sev 6 · Like 6 · Eff half-day] **Password-reset (and invite) email links intermittently fail — email security pre-consumes the one-time link.** Recovery links are one-time-use; Microsoft/Outlook ATP "SafeLinks" pre-fetches the link before the user clicks (auth logs show GET `/verify` from `104.47.x`/`209.51.8.x`), sometimes burning the token first → the user's real click gets `403 One-time token not found / link expired`. Because the frontend ignores the resulting `#error=...` hash, the user just lands on the plain sign-in screen — silent, confusing. | Subcon go-live smoke 2026-09-03 (auth_logs, real reset attempts for chris@hdspnv.com) | **Repo: sales-command (+ command-suite-db if OTP template needed). Robust fix = switch reset/invite off magic *links* to a typed 6-digit OTP code** (`supabase.auth.verifyOtp({type:'recovery',email,token})`), which scanners cannot "click"/consume. Touches `reset-password`/`invite-user` edge fns (send code, not link) + the Login reset screen (add code field) + invite/WelcomeScreen. Interim/also-worth-doing: when the landing hash carries `error=`/`otp_expired`, show a clear "This link expired — request a new one" message instead of the bare sign-in form. NOTE the frontend reset-form-detection bug found alongside this is ALREADY FIXED + live (index.html captures `type=recovery` before the client clears it; `App.jsx` `isRecovery` renders the reset form — commits 2463370/93497a6). This row is only the scanner/one-time-token reliability half. Affects invites too (same one-time-link mechanism). **PROGRESS 2026-09-03: RESET converted to 6-digit OTP — `reset-password` fn now emails `email_otp` (no link); `Login.jsx` forgot→code screen calls `verifyOtp({type:'recovery'})` then `updateUser`. Build green. NOT deployed yet — needs `/security-review` gate + `supabase functions deploy reset-password --no-verify-jwt`. INVITE half STILL OPEN: `invite-user` uses `type:'magiclink'` whose link establishes the session that routes to WelcomeScreen; converting it to OTP needs a new "enter your code" screen wired into `App.jsx` onboarding routing (bigger surface — fast-follow). Legacy in-flight magic links degrade gracefully to "Request a new code".** **SHIPPED + VERIFIED 2026-09-03: `reset-password` fn deployed (emails `email_otp`, no link); merged to main — commits `b3cf44c` (OTP), `101b823` (accept up to 8-digit input), `b0f2463` (persist code screen across leave/return via `sc_reset_pending`+`sc_reset_email`, + "Back to sign in"), `c58d6e0` (confirm-password field checked before code is consumed; `signOut()` after reset so the user logs in fresh, email prefilled + one-time notice). Full flow smoke-verified live end-to-end by Chris. STILL OPEN on this row: (a) INVITE half (magic-link → WelcomeScreen, bigger surface — fast-follow); (b) OPTIONAL hardening for review finding #1 — bump Supabase Auth Email-OTP length to 8 (frontend already accepts it), review cleared 6-digit as ship-safe so this is optional; (c) code-email DELIVERY SPEED is a separate item, see B81.** |
| B81 | T4 | Open | [Sev 3 · Like 6 · Eff ~half-day] **Reset code email is slow to arrive (~1–2 min)** — delivery latency, not app latency; the receiving mail provider (Microsoft/Outlook ATP for `@hdspnv.com`) defers/scans the message before it lands | B77 reset-OTP smoke 2026-09-03 (Chris observed 1–2 min inbox delay) | **Repo: sales-command (sender config) — NOT a code bug.** Our path (generateLink → Resend) is sub-second; the delay is inbound scanning/greylisting on the recipient side, which we can't speed up directly. **Diagnostic:** send a reset to a Gmail vs the Outlook address — if Gmail is seconds and Outlook is minutes, it's 100% the recipient provider. **The one real lever:** improve sender reputation so Microsoft stops deferring — verify a `scmybiz.com` sender domain in Resend with SPF/DKIM/DMARC and move FROM off `noreply@salescommand.app` (this is the deferred §5c-i sender-domain cleanup; do it there). **Nuclear option if email speed keeps biting:** add SMS OTP (needs phone numbers on file + Twilio-class provider + per-text cost) — separate, bigger build. Deferred by Chris 2026-09-03 ("deal with it later"). |
| B78 | T4 | Open | [Sev 2 · Like 3 · Eff 1h] **`reset-password` email-enumeration timing oracle** — the fn returns immediately for an unknown email but does `generateLink` + Resend for a known one, a measurable response-time delta that leaks whether an address has an account | /security-review 2026-09-03 (B77 reset-OTP, finding #2 — HARDENING) | **Repo: sales-command. Pre-existing — the old link code short-circuited identically; NOT introduced by the OTP change, and the UI now always advances to the code screen so it reveals nothing.** Low value: `verifyOtp` already returns the same "invalid or expired" for unknown-email vs wrong-code, so there's no oracle on the verify side. **Fix (opportunistic):** normalize the response time for the unknown-email branch (e.g. do the same work / add a small constant delay) in `supabase/functions/reset-password/index.ts`. Fold into the next reset-password touch. |
| B79 | T4 | Open | [Sev 2 · Like 2 · Eff 1h] **`reset-password` echoes the raw Resend error body to the client on 500** (`supabase/functions/reset-password/index.ts` — `Failed to send reset email: ${emailResBody}`) — minor infra info-disclosure | /security-review 2026-09-03 (B77 reset-OTP, finding #3 — HARDENING) | **Repo: sales-command. Pre-existing line, unchanged by the OTP diff.** No OTP/PII in the body, but it exposes email-provider internals to the client. **Fix:** log the Resend body server-side (`console.error`) and return a generic "Failed to send reset email — try again" to the client. Also present in `invite-user` (same pattern) — fix both in one pass. |
| B80 | T4 | Open | [Sev 2 · Like 3 · Eff ~2h] **Reset/invite send rate-limit is per-instance, not durable** — the in-memory `3 / 15min` map in `reset-password/index.ts` resets per edge-fn instance and isn't shared across instances, so it only softly caps reset-email sends | /security-review 2026-09-03 (B77 reset-OTP, finding #4 — HARDENING) | **Repo: sales-command. Pre-existing.** Enables mild email-bombing of a victim's inbox (bounded by Resend caps); does NOT help code-guessing since re-requesting voids the old code. **Fix:** move the send-limit to a durable store (a `rate_limits` table or Supabase KV) keyed by email, shared across instances. Not worth a standalone pass — fold into a broader auth-hardening loop with B78/B79. |
| B73 | T4 | Open | [Sev 2 · Like 3 · Eff 1h] AR Command **print/export output stays green** after the on-screen green→teal token swap — `src/ar/lib/exportUtils.js:19,21,114` hardcode `#5BBD3F` in the generated Accountant-Review / Print HTML | Subcon merge Phase 4 build 2026-09-02 (Round-1 audit "Adjacent" finding) | **Repo: sales-command.** Cosmetic screen/paper parity gap. Phase 4 recolored AR's accent to teal in `src/ar/lib/tokens.js` (`pop`/`popDim`/`popDark`/`popDeep` → teal), but the print/export helper builds its own standalone HTML string with three literal `#5BBD3F` greens (header `h1`, header `.total`, and the Total-Open scorecard value), so on-screen goes teal while Accountant-Review/Print output stays green. **Pre-existing pattern (self-contained print CSS), not caused by the CSS-drop** — flagged in §4b Round-1 audit amendment. **Fix:** swap the 3 literals to `#30cfac` (or import `C.pop` if the string builder can reach the token). Low priority — only matters when someone prints/exports and notices the mismatch. |
| B74 | T4 | Open | [Sev 2 · Like 2 · Eff 1h] AR mount — two cosmetic hardening nits from the Phase 4 /code-review (T5): (1) blank inner area on a **manually-typed junk tab URL**, (2) dead `GLOBAL_CSS` export left in `src/ar/lib/tokens.js` | Subcon merge Phase 4 /code-review 2026-09-02 (T5) | **Repo: sales-command. Both HARDENING, neither blocks ship.** (1) `ARLayout.jsx`'s `<Route path=":tab">` matches ANY single segment, so `/ar/<junk>` renders `Dashboard` with `activeTab="<junk>"` → all tab conditionals false → Topbar shows but the content area under it is blank. Only reachable by hand-typed/stale-bookmark URLs; **every in-app path is guarded** (sidebar `GROUPS[ar].items`, `goTab`'s `TAB_IDS` map, `index`/`*`→triage). Optional fix: validate `tab` against `TAB_IDS` in `Dashboard` (or the route) and `<Navigate to="triage">` on miss, matching the splat backstop. (2) After AR's `App.jsx`/`main.jsx` were dropped, `GLOBAL_CSS` in `src/ar/lib/tokens.js` has **0 consumers** (ARLayout injects only the scoped `.ar-root::before` crosshatch instead) — dead export; the `green:"#43a047"` token is likewise 0-consumer (already noted in the build commit). Delete on next AR touch. |
| B75 | T4 | Open | [Sev 3 · Like 4 · Eff 1h] **Stripe billing-portal return params are ignored** — `create-billing-session/index.ts:119-152` returns the customer to `${SITE_URL}?billing=success` / `?billing=canceled` / `?page=settings`, but **no `src/` code reads those params**, so a completed billing action lands on SubconHome with no confirmation toast or return to Settings | Subcon merge Phase 5 /runaudit 2026-09-03 (ADJ-1, adjacent) | **Repo: sales-command. Pre-existing — surfaced by, not caused by, Phase 5.** Cosmetic/UX only (billing still succeeds via Stripe + webhook). **Fix:** read `?billing=success`/`canceled` on load, show a toast, and route `?page=settings` back to the Settings billing tab. |
| B76 | T4 | Open | [Sev 2 · Like 3 · Eff 1h] **§5f domain-forward checklist disagrees with the code for `sccmybiz.com`** — the plan says "`sccmybiz.com` → forward to `scmybiz.com`," but `App.jsx:104-116` **hard-routes** that host in code to render `SubConCommandPage` (the `/suite` marketing page), so it is served in-app, not DNS-forwarded | Subcon merge Phase 5 /runaudit 2026-09-03 (ADJ-2, adjacent) | **Repo: sales-command.** Doc/code mismatch, no user impact today. **Fix:** reconcile plan §5f — the *marketing* hosts (`sccmybiz.com`) are served in-app via `App.jsx` `SCC_HOST`; only the *old legacy* domains (`schmybiz.com`, `schedulecommand.com`, `salescommand.app`) actually forward. Decide whether `subconcommand.com` should also be hard-routed in-app or DNS-forwarded, and make §5f match. |
| B72 | T4 | Open | [Sev 2 · Like 2 · Eff 1h] Onboarding route branch omits `/qb/callback` + `/suite` — a not-yet-onboarded member who lands on either (mid-QB-OAuth, or a marketing link) sees WelcomeScreen instead of the real page | Phase 1 Shell /security-review 2026-09-01 (#5) | **Repo: sales-command. Pre-existing — NOT caused by the Phase 1 merge shell** — the `teamMember.onboarded === false` branch in `src/App.jsx` was preserved verbatim; it only routes `/sign/:token`, `/invoice-paid`, `/invoice/:token`, and `*`→WelcomeScreen. No data exposure: `/suite` is public marketing anyway and the QB callback just needs to complete once. Cosmetic UX quirk, bounded to the brief pre-onboarding window (and only if a brand-new member is mid-QB-connect). **Fix:** add `/qb/callback` + `/suite` (arguably the full public/token set) to the onboarding branch's `<Routes>` so they resolve for a not-yet-onboarded user. |
| B71 | T2 | **Closed 2026-08-31** (merged to main) | [Sev 6 · Like 8 · Eff 1h] **Billing retention silently strands the release invoice in "New" — it never reaches QuickBooks** — "Bill Retention" created the release invoice and navigated to its **detail view**, not the Send/Approve screen, so unless the user noticed and clicked `Send / Resend` → `Approve → QB`, the release sat unposted and QB never showed the released retention | Chris, 2026-08-31 (invoice 10165 → release 10181 created in SC, absent from QB) | **Repo: sales-command.** `handleBillRetention` (`src/pages/Invoices.jsx`) does the write path correctly — conditional flip of `retention_released` → INSERT release invoice (`status:"New"`) → `onNavigateInvoice(nextId)` — but the nav landed on `InvoiceDetail` (the plain detail view), and the actual Send/Approve UI lives in `InvoicePDFModal` (opened by the `Send / Resend` button → `setShowPDF(true)`; **Approve → QB** at :1103 and **Send Invoice** at :1104, both gated on `status==="New"`). So the required posting step was invisible: 10181 stayed "New", `qb_invoice_id` null, never synced. **Fix (4 edits, matches Chris's intent — route to the choice screen, never auto-push):** Bill Retention now carries a one-shot flag across the key-based `InvoiceDetail` remount via **router state** (mirrors the existing `newInvoiceProposalId` pattern at :3268): (1) `onNavigateInvoice(nextId, { openSend: true })`; (2) page nav handler threads `state:{ openSendForInvoiceId:id }`; (3) `InvoiceDetail` gets `autoOpenSend`/`onSendOpened` props; (4) a mount effect gated on `!loading` (so the modal opens with the same loaded content the manual button would) calls `setShowPDF(true)` once, then consumes the flag so refresh/back won't reopen. Builds clean. **Immediate ops note for the ALREADY-stranded 10181:** this fix does not retro-push it — open 10181 in SC → `Send / Resend` → `Approve → QB` to post it now. **Merged to main 2026-08-31 without browser smoke (Chris's call — small, contained fix).** The already-stranded 10181 was posted to QB **by hand** (`Send / Resend` → `Approve → QB`) before merge, so no data gap remains. |
| PB-1 | T2 | **Closed** (merge `2275723`, 2026-08-27) | [Sev 8 · Like 5 · Eff half-day] Pull-back → re-send silently produces an **empty-SOW job** — Schedule's soft-delete leaves `job_wtcs` holding the globally-unique `proposal_wtc_id`, so the re-send upsert (`ignoreDuplicates`) skips the held slot and the new job gets zero WTCs; the old rollback caught only DB errors, not silent zero-row skips → Field reads nothing | Cross-repo; canonical row **PB-1 in sch-command `docs/BACKLOG.md`**; plan `~/sch-command/docs/plans/pullback_resend_teardown.md` | **CLOSED — merged to main `2275723` (fix commit `9266ae6`), deployed to prod 2026-08-27.** Pointer row — canonical home is sch-command (plan + PB-1); the CODE lives here (`src/components/ProposalDetail.jsx` send commit). §4.1 free-the-slot delete + RLS-no-op self-check; §4.2 `.select()` count guard. No migration. **§8 browser smoke (preview, 2026-08-27):** steps 2/3a/3b/6/7 watched live & passing (re-send yields full job; short-write guard fires with exact "Another live job already holds these work types" alert + clean rollback; delete→restore keeps SOW). Steps 4 (sibling-untouched) & 5 (RLS-no-op self-check) rest on banked proofs — 4 can't be shown without a 2nd proposal, 5 can't fire without disabling an RLS policy on prod. **§5 data cleanup (6 empty jobs 86–90·98) STILL DEFERRED — separate gated step; jobs 88/89 need Chris's hand-verify first (plan §9 Q1).** Smoke-test artifacts on prod (test proposal `2726f89e` / call_log 3861 / jobs 99·100·101·105) also pending cleanup. |
| B70 | T2   | Open   | [Sev 7 · Like 8 · Eff half-day] **Archive imports mis-date sold metrics app-wide** — pulling an old job live counts it as a NEW sale | Chris, 2026-08-20 (Home redesign smoke, John's account) | **Repo: sales-command (+ maybe command-suite-db for the real fix).** Making an archived History-Locker job live creates a **Sold proposal stamped with today's `created_at`**, so any metric that keys "sold this month/period" on `proposals.created_at` counts a years-old sale as brand new. Found on John: **$618,077 "sold this month" was really $124,815** — $493,262 was archive-lineage (one $469K job actually sold Feb-2025); company thermometer was 141%-of-goal vs a true ~30%. The **real** sold date lives in `archive.legacy_records.raw_data->>'job/soldDate'` (all 61 archive jobs have it; **mixed formats** — ISO 8601 + US-locale string, must parse both). **Fixed for Home only** (`src/lib/followUp.js` `homeEngagement`/`soldMonthOf` credits archive-lineage Sold to the real month via `archiveSoldDateById`). **STILL WRONG everywhere else that counts sold-by-`created_at`:** Sales Dash (`src/pages/SalesDash.jsx` monthly/forecast), any reports, conversion metrics. **Proper root fix (own loop):** stamp the true sold date onto the proposal at import time (or a shared `soldMonthOf()` helper all surfaces use), so no screen re-derives it. Verify at plan whether import writes a column or all readers switch to `job/soldDate`. **Plan-mode item — /decide → ideate → plan.** **PROGRESS 2026-08-22 (`feat/screen-refresh-dash-retire`):** the shared-helper half is now in place — `followUp.js` exports `creditedSoldMonth()` (the extracted Home `soldMonthOf`), and the new Call Log "Your Pipeline" **Sold $ this month** stat consumes it, so that surface is archive-aware from day one. Sales Dash (the other `created_at`-based sold surface) is DELETED in the same branch (WS2). Remaining for the root loop: the import-time stamp (or migrate all readers) so nothing re-derives; conversion/reporting surfaces still to audit. |
| B69 | T3   | Open   | [Sev 3 · Like 7 · Eff ~2h] Archive proposals print as **$0 with no scope** — the proposal PDF only knows how to price work types | Chris, 2026-08-10 (spotted on job 6928 Lepori, unrelated to that session's T&M work) | **Repo: sales-command.** An archive proposal renders in Proposal Preview as `0 Work Types · $0`, PROPOSAL TOTAL `$0`, and Scope of Work reading *"No scope of work written yet. Add it in the WTC tab."* **The data is intact** — verified on proposal `c00939fe-90e0-4f4f-9671-1b04be47a2d1` (job 6928): `status Sold`, `total 2415.79`, `is_archive_proposal true`, `intro` present, **`wtc_count = 0`**. The archive wizard carried everything over correctly. **Cause:** `ProposalPDFModal.jsx:189` computes `proposalPrice` by summing `calcWtcPrice` over `wtcs`, and an archive proposal has NO `proposal_wtc` rows by design — so the sum is $0. Scope of Work reads from `wtc.sales_sow` for the same reason. **NOT a regression and NOT from the T&M build** — verified: today's merge (`aecd83e..5b72722`) does not touch that file, and `git show e24173f` (2026-07-28, the PDF/invoice total fix) shows BOTH the before and after summing work types; only the per-WTC pricing method changed. `grep -n 'is_archive_proposal\|archive' src/components/ProposalPDFModal.jsx` returns **zero hits** — the component was built for WTC-based proposals and was never taught the other kind. So this has been true since archive proposals existed. **Fix:** branch on `is_archive_proposal` — read the total from `proposals.total` (the archive wizard's stored figure, which is authoritative for this kind and has no WTCs to recompute from) and source the scope from `proposals.intro` / the job's `job_work_types`, mirroring how `Invoices.jsx` already special-cases archive lines (`:288-296`, and `archiveWorkTypes` on `PublicInvoicePage.jsx`). **ANSWERED 2026-08-10 (Chris): archive proposal PDFs are NEVER sent to customers** — archive proposals are purely an internal record of already-won work. So nothing has gone out to a customer reading $0, and severity drops from 6 to 3: T2 → T3. What remains is an internal false alarm — opening the preview on a real $2,415.79 proposal shows $0, which reads as breakage and cost a diagnosis on 2026-08-10. Worth fixing so it stops raising the question, not because money is at risk. |
| B70 | T4   | Open   | [Sev 4 · Like 5 · Eff 1h] `jobs.amount` (Schedule Command) diverges from `billing_schedule.contract_sum` when a proposal's pricing changes but the SOV hasn't been reconciled — re-locking a WTC syncs `proposals.total` → `trg_sync_job_amount` → `jobs.amount`, while `contract_sum` stays stale until manually updated. Routine, not new: filed from lock-at-sold round-1 audit (ADJ-2, 2026-08-18). Real fix rides with the deferred atomic SOV reconcile RPC (`docs/plans/lock_at_sold_enforcement.md` §6), which closes both gaps in one transaction; until then the §4.2 unlock warn is the only pointer. |
| B68 | T3 | Open | [Sev 4 · Like 3 · Eff half-day] Nothing durably records **when** an invoice was sent — `invoices.sent_at` is a `date`, so send time is unrecoverable after the fact | Found 2026-08-07 (the 5pm-Pacific date bug, handoff v178) | **The forward bug is already fixed** (`c648dc3` — `sent_at` now gets the local wall-clock date); this row is the remaining **data-model** gap, not a live defect. `invoices.sent_at` stores a date and no column stores the instant, so once a row is written there is no way to reconstruct what time it was sent. That is what made the historical sweep in v178 only partly possible: `created_at` (timestamptz) served as a witness for the 112-of-127 invoices created and sent in the same sitting, but **15 invoices created one day and sent another remain permanently unresolvable** — if any were sent after 5pm Pacific they are still a day early in both SC and QuickBooks, and nothing can tell us which. Beyond this one sweep it's a general audit-trail hole: "when did we actually send that invoice?" is an ordinary question during a payment dispute or a QB reconcile, and today it cannot be answered. **Two options, pick during ideate:** (a) backfill the existing `invoice_recipients.sent_at` — already a timestamptz, already the right shape, but populated on only **4 of 127** invoices, so the send paths would need to write it consistently (`send-invoice`, `send-pay-app`, and the Mark-as-Sent handler at `Invoices.jsx:~1617`, which has no recipient row at all); or (b) add `invoices.sent_instant timestamptz` alongside the date, which covers the Mark-as-Sent path that (a) does not. (b) is likely simpler and complete; (a) reuses a column that already exists. **Migration lives in `command-suite-db`, not here** — so this is migration + client wiring, i.e. the /decide → plan → build path, not a /fix. Do NOT change the type of `sent_at` itself: it is a date on purpose, it is what the date filter and QB `TxnDate` (`qb-sync-invoice:365`) read, and widening it would touch every consumer. Add the instant beside it. Full context: `docs/handoffs/SC_Handoff_v178.txt` ADDENDUM A1 + POINTERS #5. |
| B61 | T3 | **Retired 2026-07-28** (not fixed — the desired behavior inverted). This row existed only because round-UP was considered wrong for a revived pre-cutoff proposal. As of the exact-penny window closing (`EXACT_PRICING_END` = midnight Central 2026-07-29, `src/lib/calc.js`), round-UP is correct for that proposal and for all new work, so the manual `pricing_anchor_at` re-anchor is no longer needed. Original text kept below for history. |  [Sev 4 · Like 3 · Eff half-day] Re-approving a pulled-back pre-2026-06-26 proposal doesn't re-anchor its pricing era → still bills legacy round-UP (`Math.ceil`) instead of exact penny; needs a manual `pricing_anchor_at` UPDATE | Found 2026-07-13 (Loop — job 7442 P1, WTC exact $37,154.09 invoiced as $37,155.00; handoff v165) | `usesExactPricing()` (`src/lib/calc.js:23`) pins a proposal's pricing era to `pricing_anchor_at ?? created_at` vs `EXACT_PRICING_CUTOFF` (noon Central 2026-06-26). A proposal created **before** the cutoff rounds UP via `roundPrice`→`Math.ceil` everywhere (total + invoice lines). **Pull-back + re-approve does NOT change the era** — `created_at` stays original and `pricing_anchor_at` stays null, so a proposal revived/re-priced today still bills legacy round-up. Job 7442 P1 (`9058ada8`, created 2026-04-29) invoiced **$37,155.00** for a **$37,154.09** WTC; resolved this session by a scoped one-row UPDATE (`pricing_anchor_at = now()`, `total = 37154.09`) via the `command-suite-db` linked CLI (MCP DB is read-only). The invoice modal's "Exact" toggle is **display-only** (`roundInvoice`/`show_cents`) — it does NOT change the amount. **Fix candidate:** on re-approve of a pulled-back proposal (`handleInternalApprove` in `ProposalDetail.jsx` + the `mark_proposal_signed` RPC customer-sign path), if `created_at < cutoff` set `pricing_anchor_at = now()` and refresh `proposals.total` to the exact figure. **Design tension:** must NOT break the clone-inheritance rule — a multi-GC clone intentionally inherits its source's era via `pricing_anchor_at`, so don't blanket-overwrite; only re-anchor a genuine pull-back-then-re-approve of the SAME proposal. Shrinking population as pre-cutoff proposals age out. Workaround meanwhile: manual one-row `pricing_anchor_at` UPDATE per revived proposal. |
| B62 | T1 | Closed 2026-08-27 | [Sev 8 · Like 9 · Eff multi-day] QuickBooks → Sales Command payment sync **does not exist** — a payment entered in QB never flips the SC invoice off "Sent"; ~$128k+ of collected money reads as outstanding. Needs ideate→plan→build; **first fix attempt was wrong at the premise and is a documented spike** | Found 2026-07-14 (Chris: "SC is not getting anything back from QuickBooks") | **CLOSED 2026-08-27 — SHIPPED via the proper plan `docs/plans/qb_payment_status_sync.md` (2-round audited) + full rollout, merge `4d148bd` on main. All four review gates passed (build→buildvsplan→code-review→security-review, 0 blockers).** What shipped: (1) shared reflect core `_shared/reflectPayments.ts` — flips SC→Paid ONLY when QB `Balance==0` AND a real linked QB `Payment` exists (the A1 guard — a zero balance from a write-off/credit-memo/void is NEVER flipped, killing the spike's fatal premise); `paid_at`=MAX(linked Payment TxnDate) as local noon; never-un-pay + voided/deleted/tenant guards on resolve AND write. (2) Instant webhook `qb-webhook` (Intuit Event Notifications, HMAC/base64/fail-closed) — real-time primary. (3) 15-min cron sweep `qb-reflect-payments` (command-suite-db migration `20260827160000`) — self-healing backup. (4) `invoices.qb_reflected_at` column (`20260827120000`) + "QB" badge distinguishing QB-reflected vs manual Paid + delay notice. (5) Mark-as-Paid now LOCAL-ONLY and hidden on QB-owned invoices; the two SC→QB pushes removed; `qb-record-payment` hard-locked to Stripe (service-role) only. **Backfill (one sweep run, QB-verified):** 111 candidates → 53 flipped to Paid (all Balance 0 + real Payment, incl. retention invoices — the spike's defect 2), 58 left unpaid (all QB Balance > 0). The ~$128k backlog is corrected. **ADJ-1 relationship:** the two UI callers of `qb-record-payment` are gone + S8 dedupes the Stripe webhook, so the reflect direction reads uncorrupted QB state; ADJ-1 (service-role idempotency) stays its own row but no longer blocks this. **SPIKE SUPERSEDED:** branch `fix/qb-payment-sync-back` (commits 78f784f/e08f637, "premise wrong, do not deploy") is dead — safe to delete. **Original diagnosis retained below for history.** ~~**Blocked by: ADJ-1** (prerequisite, not adjacent — see Stripe finding below).~~ **DIAGNOSIS (holds).** Only the SC→QB direction exists: `qb-record-payment` pushes a payment INTO QB when SC marks an invoice Paid (stripe-webhook, or the manual Mark-as-Paid menu at `Invoices.jsx:1610`). Nothing reads payment state back OUT — every QB `/query` in the repo targets Customer/Item/Department; there is no `SELECT … FROM Invoice`, no QB webhook handler, no poller. The `Invoices.jsx:1450` "auto-refresh: poll for payment status" useEffect polls **SC's own `invoices` table** every 5s, so it only redraws a status something else already wrote — it is NOT a QB read. That is why this looked coded but never worked. **Blast radius (QB queried directly 2026-07-14, per [[qb-reconcile-query-qb-directly]]):** of 15 sampled SC-"Sent" invoices with a `qb_invoice_id`, **11 are Balance 0 in QB** — #10078 $50,339.70 · #10069 $30,851.95 · #10083 $22,432 · #10079 $5,884 · #10081 $5,401 · #10072 $4,723 · #10056 $3,536 · #10075 $2,658 · #10074 $1,668 · #10061 $812 — plus #10071 partially paid ($794.05 of $15,881 open). ~$128k across a **15-of-40 sample**; full population unmeasured. Same class as the B60 manual reconcile (#10028/#10004/#10011). **SPIKE — DO NOT DEPLOY (`fix/qb-payment-sync-back`, commit `78f784f`).** Added edge fn `qb-sync-payments` marking Paid where `Balance == 0 AND TotalAmt > 0`, triggered on Invoices page load. **Premise is wrong:** a QB balance reaches zero for ≥4 distinct reasons — customer paid; credit-memo/write-off zeroed it with no money collected; invoice voided; or the net cleared while retention is still owed. The fn conflates all four into "Paid". Code review 2026-07-14 (30 candidates → 28 verified → **10 distinct defects**, 2 refuted). **8 of 10 detailed here; 2 remain in the review terminal output and are NOT captured — pull them before planning.** (1) **Credit-memo write-offs book as collected revenue** — unrecoverable by patching. (2) **Retention invoices flip to Paid when only the net clears** — QB `TotalAmt` is already net of the retainage line, so every retention-bearing invoice whose net cleared drops out of `retentionInvoices` (`Invoices.jsx:2831`) and the worklist **in one batch on first run**, retention still outstanding (#10078 $2,649.46 · #10069 $1,623.79 · #10077 $62.15 + unsampled). (3) **Stripe link never deactivated** — writing `status='Paid'` direct to the DB bypasses `updateStatus`, where B33 wired link deactivation; link stays live, a second customer payment hits `stripe-webhook` which updates unconditionally and calls the non-idempotent `qb-record-payment` (**ADJ-1**) → duplicate QB payment against a settled invoice → **corrupts the ground truth this sync reads from**. Candidate select doesn't even fetch `stripe_payment_link_id`. (4) **`paidDateSource` is mute** — produced once, read nowhere; server log emits counts only; caller swallows everything in a bare `catch {}`. If the `LinkedTxn` assumption is wrong, all ~11 heals silently stamp today's date with zero signal — i.e. silently delivers the "today's date" option that was explicitly rejected. (5) **Void guard unproven** — `total <= 0` is the sole defense, never run against QB; `PrivateNote` is already in hand from `SELECT *` and never inspected. (6) **Update has no `.select()` / rows-affected check** — a zero-row write reports as a successful heal (the `.neq("status","Paid")` guard itself is fine). (7) **Page-load trigger races OAuth refresh-token rotation** across concurrent users; a lost race kills the QB connection until someone re-runs the connect flow. (8) **SalesDash misdating** — buckets on `sent_at \|\| paid_at`, so any sync-time fallback lands revenue in the wrong period (the `T12:00:00Z` anchoring itself is fine). **Refuted:** QB query escaping (unreachable over DB-sourced ids; style note only); tenant scoping (clean). **DESIGN QUESTIONS FOR IDEATE (need business judgment, not code reading):** (a) what constitutes "paid" — honest signal is summed linked **Payment** transactions vs. what SC expects to collect, not `Balance`; (b) does a retention-bearing invoice need a state SC lacks ("net collected, retention outstanding")? ; (c) is a credit memo / write-off a payment event or its own business event? ; (d) trigger mechanism given the OAuth rotation race rules out page-load (webhook needs Intuit dashboard config + verifier token; cron needs a migration in `command-suite-db`); (e) ADJ-1 sequences **first**. **Interim option (not built):** strip to a read-only reconcile *report* — no writes, no auto-trigger, prints the SC-vs-QB diff for a human to eyeball; surfaces credit-memo/retention cases as rows to review rather than silently miscategorizing them. That is what B60 did by hand. **Scope note:** spike is sales-command only, no migration — deliberately clear of the `command-suite-db` DMS-1 build in flight. |
| B64 | T1 | **Closed 2026-07-28** (`0dc6f00`, merged @ `8393606`, `send-proposal` v45 live) | [Sev 7 · Like 9 · Eff 1h] Multi-GC sister proposal shows (and would send to) the **parent job's customer**, not the GC it was cloned to — every recipient surface resolves the customer from `call_log.customer_id` and ignores `proposals.customer_id` | Found 2026-07-28 (Chris — job 10220 Boys & Girls Club, P1 KalB Construction / P2 sister to Peerless Construction: "Calb is still hardwired into the recipients of the Peerless proposal") | **Data was correct; every read was wrong.** The clone RPC stamps the sister's own `proposals.customer_id` (= Peerless) and `proposals.customer` (= "Peerless Construction, Ltd"), and `clone_proposal_to_gcs` had already inserted the right `proposal_recipients` row (Vince Mack, vince@peerlessconst.com, signer). But **`MultiGCWizard.jsx:126` was the only consumer in the codebase reading `proposals.customer_id`** — every other surface resolved the customer as `call_log.customer_id`, which is permanently the parent GC. Four sites fixed to `proposals.customer_id ?? call_log.customer_id` (the COALESCE shape B19 already assumed): (1) **`ProposalDetail.jsx`** — Recipients card's "Primary" row rendered the parent's name + email, "Add from customer contacts" listed the parent's contacts, and `savePrimaryEmail` would have **written the sister's email onto the parent customer's record**; new `gcCustomer` state loads the sister's GC when `customer_id` differs. (2) **`ProposalPDFModal.jsx`** — send screen's contact list + **default signer** came from the parent, so the pre-filled To was the wrong GC. (3) **`send-proposal/index.ts:90`** — the server-side allowlist validated the recipient against the *parent's* `customer_contacts`, so sending to any genuine Peerless address would have **400'd "Recipient email not associated with this customer"**; every sister send was blocked, not just mis-defaulted. (4) same PDF header's "Prepared For" address block printed the parent's business address under the (correct) GC name. Client build green. **Shipped + deployed same session** — `send-proposal` redeployed `--no-verify-jwt` to v45 (source read back from the live project and confirmed to contain the fix); client merged to main @ `8393606` and Vercel auto-deployed. No migration. **Display half smoke-verified on prod by Chris** (correct GC addresses populated through the whole send flow); **the Send button itself was never pressed**, so the server-side allowlist remains the one unexercised path — failure mode is a loud, non-destructive 400. Branch `fix/multi-gc-sister-recipients` (merged, deleted). _(Status line corrected 2026-07-28 per v169 pointer 2 — the row said "gates pending" after the work had already gone live.)_ |
| B65 | T1 | **Closed 2026-07-28** (`4b5024d`, merged to main @ `02c3a08`) | [Sev 7 · Like 8 · Eff 1h] Editing a **retention-release** invoice zeroes its amount — the billed retention silently disappears from A/R | Found 2026-07-28 (Chris — job 10104 Mountain River Construction: "Bill Retention went to an invoice screen that showed the correct information, and then it just zeroed out… it doesn't show an invoice anywhere with an amount") | **Third instance of the CLAUDE.md #6 trap** (archive `14000c5` 2026-04-20, pay-app `33c385e` 2026-06-04, now retention-release). A release invoice is minted by `handleBillRetention` (`Invoices.jsx:1912`) carrying its dollars on the **header only** — deliberately **no `invoice_lines`**, because `qb-sync-invoice`'s release branch (`index.ts:208`) reads `invoice.amount` and emits its own single retention-item line. `handleSaveEdit` derived `newAmount` by reducing over `lines`, which for a release invoice is `[]` → `0`, and `isArchiveInvoice` (`:1734`) requires `lines.length > 0` so the archive preserve-branch never caught it. Any in-place edit therefore wrote `amount = 0`. **Fix:** `newAmount` falls back to the stored `inv.amount` when there are no lines (`Invoices.jsx:1801`) — nothing to recompute from means preserve, never zero. **Blast radius (prod swept 2026-07-28):** 5 invoices have zero `invoice_lines`; only **#10137** was damaged (release of #10104, $87.50 → $0). **Data repaired** — #10137 set back to $87.50 (still New / unsent / not QB-synced, so no downstream correction needed). #10064/#10066 (voided/deleted), #10101, #10116 all carry correct amounts. Client build green. No migration, no edge fn change. **Smoke-verified on the branch preview 2026-07-28 (Chris): Edit → Save on #10137 held at $87.50.** Branch `fix/retention-release-zeroed-on-edit`, merged to main @ `02c3a08`. |
| B63 | T3 | Open | [Sev 3 · Like 4 · Eff 1h once unblocked] Retention StatCard shows a useless "Avg per Invoice" dollar figure; should show **average aging** — how long we have been holding each retention. **Blocked by B62** (see below) | Found 2026-07-15 (Chris, reviewing the `fix/retention-list-filter` preview: "average per invoice is a useless piece of data, but how old these things are is relevant") | **Label swap is one line (`Invoices.jsx:2935`); the number behind it does not exist yet — do NOT build it as a swap.** **THE RULE (Chris, 2026-07-15, ratified):** *"Aging for retention would start when the full invoice has been paid, not until then."* Retention on an unpaid invoice is not aging — the clock has not started. **This INVERTS the existing helper:** `aging()` (`Invoices.jsx:2838`) returns `null` when `status === "Paid"` and requires `due_date`; Chris's rule makes Paid the ONLY state that ages, and `due_date` irrelevant (retention was never billed, so it was never due). Do not reuse `aging()` — it answers a different question (days-past-due on a live invoice). Per [[extend-canonical-not-twin]], decide deliberately whether to extend it or add a distinct `retentionAging()`. **DATA CHECK (prod, 2026-07-15, read-only):** the clock field is `invoices.paid_at`, and it IS populated for both currently-aging rows — #10004 (paid_at 2026-07-01, 14 days) and #10104 (paid_at 2026-07-02, 13 days). The other 8 of the 10 retention rows are unpaid → clock not started → correctly excluded from the average. So today the card would read "~14 days" over n=2. **WHY IT IS BLOCKED BY B62 (the load-bearing reason):** the rule depends on knowing *when the full invoice was paid* — which is precisely the fact B62 exists to establish, and which SC does not reliably have. (1) **`paid_at` is a click timestamp, not a cash date.** #10004's `paid_at` is 2026-07-01, but the actual QB payment (ref 3108) landed **2026-06-02** — the clock is **29 days short** because it starts when someone remembered to press Mark-as-Paid. (2) **Mark-as-Paid is PAUSED** (block 1 of B62's 7-block sequence, done 2026-07-14) and every SC "Paid" is manual (B62 finding iv — only 7 QB payment pushes EVER, of 91 invoices). **So no new invoice can start its retention clock until B62's sync lands.** Built today, this card freezes at n=2 and slowly lies. (3) B62 finding (ii): QB is only truth once payments are APPLIED — an unapplied payment makes a paid invoice read unpaid, so a naive B62 sync would start the clock on the wrong day anyway. **SEQUENCING:** ships after B62 block 7 (the sync) gives a trustworthy paid-date. If it is wanted sooner, the honest interim is to source the clock from the **QB applied-payment date**, not `paid_at` — which is B62 block 3's read-only SC-vs-QB report, not this card. **Cosmetic sibling (not blocked, ~5 min):** the middle StatCard still reads "Open Pay Apps with Retention" but now counts Paid-but-unbilled rows too (a consequence of the 2026-07-15 roll-up fix); wording left alone per [[stay-scoped]] — Chris did not flag it. |
| B58 | T0 | Closed 2026-06-29 | (was B52) [Sev 8 · Like 9 · Eff 1h] Public invoice page `salescommand.app/invoice/:token` shows "Oops — Invoice not found" for every invoice (customer-facing) | Found 2026-06-29 (HDSP customers — invoices #10093 Contract Flooring + #10091 Mason Swaiger, first incident 2026-06-26) | **Closed same session (migration `20260629104507_anon_invoices_grant_token_expiry.sql`, applied to prod via `db query -f` + `repair --status applied`).** Regression from the 06-25 column-security work. `20260625140000_anon_invoices_column_grant.sql` replaced anon's table-level SELECT on `invoices` with a 15-column grant (to hide `stripe_*`/`qb_*` per #SEC1). But the public page's anon RLS policies on the RELATED tables (`proposals_public_view_token`, `invoice_lines_public_read_token`, `proposal_wtc_public_view_token`, `call_log_public_view_token`, `customers_public_read_token` — all added by **B13** on 2026-05-20) each run `EXISTS (SELECT 1 FROM invoices i WHERE i.<fk>=… AND i.viewing_token_expires_at > now())`. Those subqueries run as anon and read `invoices.viewing_token_expires_at` + `invoices.call_log_id` — **two columns the 140000 grant omitted** → `42501 permission denied for table invoices` on every embed/related read → page can't assemble the invoice. **Fix:** `GRANT SELECT (viewing_token_expires_at, call_log_id) ON public.invoices TO anon` — the two policy-referenced columns. No code change (the React query was correct). `stripe_*`/`qb_*` stay ungranted — #SEC1 boundary preserved (verified: anon read of `stripe_checkout_url`/`qb_invoice_id` still 401s). Smoke: full page query for #10093 → HTTP 200 with customer/amount/lines; security cols → 401. **Lesson:** when REVOKE-all + re-GRANT(cols) on a table, the grant list must include every column referenced by anon RLS policies on OTHER tables, not just the columns the page itself selects. |
| B59 | T2 | Closed 2026-06-30 | (was B54) [Sev 5 · Like 7 · Eff 1h] Multi-GC wizard's "add GC" customer list truncates (stops well short of the 758 Commercial customers) | Found 2026-06-30 (user: list stops at ~50) | **Closed same session (branch `fix/multi-gc-customer-pagination`).** `MultiGCWizard.jsx` screen-1 GC picker fetched `customers` via a plain unpaginated `.select("id, name, customer_type, billing_terms").order("name")` (line ~106) — PostgREST returns only the first page, so the client-side `filteredCustomers` Commercial filter ran against a truncated set, cutting the GC list short. DB has 2077 customers / 758 Commercial. **Fix:** swap the raw select for `fetchAll("customers", …, { order })` from `supabaseHelpers` (the same paginated helper the Customers page uses). No schema/migration. Build green. Smoke: open Multi-GC wizard on any proposal → screen 1 GC list now shows all Commercial customers (search "z…" surfaces late-alphabet names that previously vanished). Same fetchAll-not-raw-select class as the documented 1000-row-limit pattern. |
| B51 | T4 | Open (blocked) | [Sev 2 · Like 2 · Eff 1h] Drop the retired deposit columns (`proposals.deposit_*`, `call_log.deposit_required` / `deposit_amount` / `deposit_invoice_id`) | deposit-tag v2 cleanup (2026-06-22); scope corrected 2026-07-31 (T4) | Dead job-level deposit columns from two generations. (a) `20260620120000` added `proposals.deposit_*`, unused since deposit-tag v2. (b) `20260731120000` moved the flag onto the invoice (`invoices.is_deposit`), retiring the `call_log` trio. **BLOCKED — and by more than the migration note says.** Both *deployed mains* still SELECT the trio (sch-command `queries.js:108-110`; sales-command `ProposalDetail.jsx:233,622,877,887` + `PayAppDetailModal`), so the drop waits until BOTH apps' new code is live in prod — not merely branched. **`invoices.type` is EXCLUDED from this row** (see B67). **Fix:** one cleanup migration in `command-suite-db` after both apps ship, then refresh the baseline snapshot. Re-grep all four app repos first. |
| B66 | T4 | Open | [Sev 3 · Like 2 · Eff half-day] No automated control proves the anon-gate's added-column branch keeps its teeth under `--pre` | multi-deposit T4 gate review (2026-07-31) | `assert-invoice-lock.sh` case 1 (the vacuity control) strips the invoices REVOKE, which exercises the UN-HARDENED branch — a path `--pre` never touches. The staging mechanism added 2026-07-31 (`anon_hardened_pending.txt`) is therefore covered only by hand-probing: on 2026-07-31 a stale pending line for `public.invoices.status` was confirmed to fail the pre-state check, and an unlisted `GRANT SELECT (void_reason)` to fail the post-state check. Neither is codified, so a future edit could quietly neuter the added-column branch and every push would still read green. **Fix:** add a control container that grants a NOT-allowlisted column and asserts `check_anon_exposure --pre` fails. Weigh the added push runtime. |
| B67 | T4 | Open | [Sev 4 · Like 2 · Eff 1h] `invoices.type` cannot be dropped as "zero readers" — it has four live WRITERS | multi-deposit T4 gate review (2026-07-31) | Carved out of B51, which called it unused. Zero *readers* is true; `Invoices.jsx` still WRITES it on every invoice-create path (~`:271`, `:1931`, `:2114` replacement insert, + the pay-app mint), and the `invoices_type_check` CHECK constraint still permits `'regular'/'deposit'/'pay-app'`. Dropping the column breaks every create path. Prod carries **0 rows** with `type='deposit'` (probed 2026-07-31), so no deposit history hides there and `20260731120000`'s pointer-only backfill loses nothing. **Fix:** remove the writes first, then drop column + constraint in a later migration. |
| B48 | T4 | Closed 2026-06-22 (obsolete) | Invoices with `call_log_id IS NULL` bypass single-select deposit enforcement | deposit-tag T5 review (2026-06-21) | **CLOSED — obsolete after deposit-tag v2.** v2 removed `invoices.is_deposit` and the partial unique index; single-select is now inherent (one `call_log.deposit_invoice_id` per job), so the null-`call_log_id` bypass it described can no longer exist. |
| B49 | T4 | Closed 2026-07-31 (obsolete) | Job-level deposit field is last-write-wins across sibling proposals (no conflict detection) | deposit-tag T5 review (2026-06-21) | **CLOSED — obsolete after the multi-deposit fix (`20260731120000`).** The job-level deposit controls (`call_log.deposit_required` / `deposit_amount`) and both `saveDeposit` handlers (ProposalDetail + CallLogDetail) were REMOVED — a job's deposits are now told entirely by its invoices (`invoices.is_deposit`), which are per-invoice and cannot be overwritten by a sibling proposal. There is no shared job-level value left to race on. |
| B50 | T4 | Closed 2026-07-31 | Tagging a retention-bearing regular invoice as a deposit doesn't zero retention until the next edit-form save | deposit-tag T5 review (2026-06-21) | **FIXED** in `handleToggleDeposit` (`Invoices.jsx`) as part of the multi-deposit fix: marking a deposit now writes `retention_pct`/`retention_amount` = 0 in the SAME update, and syncs the edit-form input. Pay-app and QB-synced invoices are excluded — that money is owned by the pay-app flow / already in QuickBooks, so the toggle must not move it. |
| B47 | T4   | Open   | [Sev 4 · Like 2 · Eff 1h] `invoice_lines.billing_schedule_line_id` is `ON DELETE SET NULL` → deleting a billing_schedule_line orphans a pay-app line to null/null | §1a deposit round-2 audit (2026-06-20, non-blocking observation) | **Latent SOV data-model wart, NOT a migration defect.** `invoice_lines.billing_schedule_line_id ON DELETE SET NULL` (`20260416175646:146`). If a `billing_schedule_line` is ever deleted, an affected pay-app invoice's line orphans to null/null — and **both** the `invoices.type` migration backfill **and** the app's live classifier (`isArchiveInvoice`, `Invoices.jsx:1293`) would then degrade it to `regular`/archive **identically**. So it's not a migration-vs-app divergence — it's a pre-existing wart in the SOV layer. **Fix:** reconsider the FK action (RESTRICT, or re-derive type) so a SOV line deletion can't silently relabel a pay-app invoice. Low priority — pay-app invoices' SOV lines aren't deleted in normal flow. |
| B39 | T2   | Open   | [Sev 5 · Like 5 · Eff 1h] "Resume QB Sync" green toast reads as "job is QB-connected" — but only un-skips; job still has no `qb_customer_id`, so invoice Sync-to-QB button stays hidden | Found 2026-06-03 (Loop #31 — invoice #10068, job 7406 IVGID Chateau Deck Coating; Chris hit Resume QB Sync, saw green banner, assumed connected, then couldn't find Sync to QuickBooks on the invoice and cycled pull-back/re-status several times) | **Workflow trap, not a code bug — the invoice button gate behaves correctly.** Invoice *Sync to QuickBooks* button (`Invoices.jsx:1760-1767`) requires `call_log.qb_customer_id != null` (gate 3). For a job that was "skip QB sync", **"Resume QB Sync"** (`CallLogDetail.jsx:583-596`) only sets `qb_skip_sync=false` — it does NOT create the QB job, so `qb_customer_id` stays null and the invoice button never appears. The resume path uses `setQbToast({ kind: "linked", ... })` which renders **green/success** (`CallLogDetail.jsx:539` — green when kind==="linked"), even though its own text says "Connect this job to a QuickBooks customer to enable syncing." After resuming, the job correctly falls into the `Connect to QuickBooks` state (`CallLogDetail.jsx:598-608`) → QBActionModal → `qb-create-job` writes `qb_customer_id`. **Fix candidates:** (a) give the resume toast a neutral/amber kind (not `linked`) so it doesn't read as done; (b) reword to an explicit next-step CTA ("Sync resumed — now click **Connect to QuickBooks** to create the job in QB"); (c) optionally auto-open the Connect flow right after resume. (a)+(b) preferred, ~1h. Same UX-clarity class as B35 (Sync-and-Send label). |
| B60 | T2   | Closed 2026-07-02 | (was B55) [Sev 7 · Like 3 · Eff half-day] QB duplicate-docnum data heal — reconcile invoices wedged with `qb_invoice_id IS NULL` while QB holds/burned their DocNumber | Found 2026-07-01 (Loop — invoice #10051 `qb_duplicate_docnum`) | **Step (4) of `docs/plans/qb_sync_duplicate_docnum_fix.md`, split-deferred by round-2 audit.** Prevention (steps 1-3 + client/server persist-recovery) shipped on `fix/qb-sync-duplicate-docnum` (92d3583, awaiting gate chain). **Heal rule:** enumerate `qb_invoice_id IS NULL AND deleted_at IS NULL AND voided_at IS NULL AND status<>'New'` on QB-linked, non-test, non-skip jobs; per row query QB by DocNumber, **refuse on 0 or >1 matches** and on voided records (TotalAmt 0 / all-lines 0 / PrivateNote "Voided"); set `qb_invoice_id` only if QB `CustomerRef` == the job's QB sub-customer (`call_log.qb_customer_id`) AND QB `TotalAmt` == SC net (`amount − discount − retention_amount`) ±$0.01. SC-side write only. **2026-07-01 live reconciliation (QB re-authed):** blast radius = **9 wedged rows** (8 real + 1 test #09122), all Apr–May, zero since June. **#10051 RESOLVED** — customer holds #10051 (send verified), so instead of a new SC id we booked QB under a **suffixed DocNumber `10051R` (QB id 147840, sub 11761, $5,754.16)** and set SC `qb_invoice_id=147840`. Two **refinements** (see plan §4/§7): (a) a "0 QB matches" invoice may still be **burned by a hard-deleted QB record** (invisible to API, still blocks re-create — #10051's case) → heal must not assume clean re-sync; (b) **never re-issue a *Sent* invoice under a new SC id** (customer holds that number) — keep the customer-facing number, suffix the QB DocNumber (the 10051R pattern). **#10038 + #90360 ADOPTED 2026-07-01** (SC qb_invoice_id set to 147286 / 146641). **CLOSED 2026-07-02 — full reconciliation complete.** All wedged + adjacent invoices squared SC↔QB: **booked fresh + linked** #10051 (10051R/147840) · #10038 (147286) · #90360 (146641) · #10044 (147853) · #10052 (147854) · #10022 (147856) · #10014 (147857); **already Paid in QB, now linked** #10028 (147170) · #10004 (146928) · #10011 (145481); #09122 = test. Two extras beyond the original 9: **#10022** (CO job 7423 CO1) had a stale `qb_skip_sync=true` masking it → cleared flag + booked; **#10014** (Tolles) job had a QB sub-customer (11671) SC never linked → linked job + cleared skip + booked. **Lesson banked** ([[qb-reconcile-query-qb-directly]]): query QB by DocNumber for ground truth — two false "unbooked revenue" alarms (#10028, #10011) came from inferring QB state from SC fields. **Handoff (not engineering):** #10021 (voided in QB — Chris pulling back + resending, fixing QB manually); #10004 retention $1,561.65 (bookkeeper records the received check). ✅ **Id-collision resolved 2026-07-02:** B52/B54/B55 were each duplicated (main-backlog vs the exact-penny-pricing cluster). Kept the referenced, still-Open exact-penny items at B52/B54/B55; renumbered the three Closed main-backlog collisions → **public-invoice B52→B58, multi-GC B54→B59, this QB item B55→B60**. Git history + `docs/plans/qb_sync_duplicate_docnum_fix.md` still reference this work as "B55". |
| B38 | T2   | Closed 2026-05-22 | Stripe pay-link integrity surface — kill Refresh Stripe Link button, replace with edit-staleness + past-due triggers | Found 2026-05-22 (B34 smoke — hidden Refresh button is dead weight; users won't remember to click it; need automated triggers/pulses) | **Closed same session (branch `fix/b38-stripe-link-staleness` based on B36 branch).** Shipped: (a) Refresh Stripe Link button removed from action bar (`Invoices.jsx:1615-1619`). (b) Renamed `Preview / Send` → `Send / Resend` (pay-app variant stays `Preview` since pay-app modal is preview-only). **All three triggers and the `invoices.link_stale` column dropped from scope** — after tracing the code, the original B38 premise was wrong: `send-invoice/index.ts:125-165` already deactivates the prior payment link and mints a fresh one on every send, and `handlePullBack` nulls `stripe_payment_link_id` + calls `deactivate-payment-link`. Edit Invoice is gated on `status === "New"` (Invoices.jsx:1603), so the only way to edit a Sent invoice is Pull Back → Edit → Send, which guarantees a fresh link every time. There's no scenario in current code where a customer holds a stale-amount link, so a pulse/webhook/staleness column adds no value. If in-place editing of Sent invoices is added later, revisit. |
| B37 | T2   | Closed 2026-05-22 | False-sister detection blocks sequential proposals from going Sold | Found 2026-05-22 (B34 smoke side-find — job 10116 P2 stuck at Signed after internal-approve because P1 was Sold under same call_log) | **Closed same session (Loop #25, branch `feat/b37-false-sister-fix`).** Replaced broad `call_log_id` sibling scan with `cloned_from_proposal_id` lineage check in both `handleInternalApprove` (`ProposalDetail.jsx:627-636`) and `mark_proposal_signed` RPC (migration `20260522160000_b37_mark_signed_lineage.sql`, `CREATE OR REPLACE` — no DROP, 1-arg wrapper unaffected). Backfilled 1 row: job 10116 P2 (`aca7f47d`) `Signed` → `Sold`; QB job already existed from P1. Smoke 5/5: (1) sequential P2 → Sold, (2) solo P1 → Sold, (3) sister internal-approve → Signed, (4) backfilled P2 invoice path works, (5) sister customer-sign via PublicSigningPage → Signed. `award_proposal` RPC has same hazard but no UI wires it — deferred as B37-followup. |
| B36 | T1   | Closed 2026-05-22 | send-invoice ignores discount + retention — customer-facing amount + Stripe pay link both use gross | Found 2026-05-22 (Danny Peltier #10028 — Chris applied $825 discount in SC, customer received email + Stripe link calling for full $1,651) | **Closed same session (branch `fix/b36-send-invoice-net-amount` based on B34 branch).** `supabase/functions/send-invoice/index.ts` now SELECTs `discount, retention_amount, retention_pct` (line 47), computes `netAmount = amount - discount - retention_amount`, and emits net to Stripe `unit_amount` (line 174) + email body headline (~line 230) + sender notification (~line 294). Email shows breakdown (Gross / Discount / Retainage withheld / Payment due) only when discount or retention > 0 — clean single-line headline when neither applies. Net ≤ 0 returns 400 (defense vs. an over-applied discount silently zeroing out the link). Canonical formula matches `Invoices.jsx:1443` "Payment Due" StatCard. **send-pay-app swept and found clean** — it attaches client-rendered PDFs only; no Stripe link and no inline amount in the body, so the gross/net bug doesn't apply. qb-sync-invoice unchanged (QB intentionally receives gross + discount/retention as separate line items per `project_qb_retention_strategy` memory). Workaround used today on Danny's #10028: edited invoice to bake discount into `amount` directly with `discount=0` — no longer needed post-fix. |
| B35 | T3   | Closed 2026-05-29 | "Sync and Send" button label reads as "send to customer too" — actually only syncs to QB | Found 2026-05-22 (user expected the button to be QB-only; got confused when sender confirmation email arrived during Sync and Send 500 attempts) | **Closed — fix candidate (a).** Renamed non-pay-app variant "Sync and Send" → "Sync to QuickBooks" at `Invoices.jsx:1612` (pay-app variant stays "Sync to QB"). Help-doc label + steps in `TableOfContents.jsx:227` updated to match. Handler `handleQBSync` unchanged — always QB-only (`qb-sync-invoice` + `qb-record-payment` if Paid; never `send-invoice`). Label now reads unambiguously as a QB push, not a customer email. |
| B34 | T2   | Closed 2026-05-22 | Invoice re-send after QB void fails with opaque 500 — duplicate DocNumber blocks re-sync; no audit record of voided invoice | Found 2026-05-22 (user pull-back of #10028 to re-attach Stripe Payment Link voided in QB + nulled `qb_invoice_id`, then Sync and Send 500'd because QB rejects duplicate DocNumber) | **Closed same session (ERD Loop #24, branch `feat/b34-invoice-void-record`, primary commit `04da90f` + follow-ups `c9ee8af` / `912a4a0` / `de8b835`).** Migration `20260522130000_invoices_voided_at.sql` applied to prod (`voided_at timestamptz` + `void_reason text`, nullable). Edge fn `qb-sync-invoice` redeployed with voided early-return (after tenant gate) + Duplicate DocNum error catch + expanded `qbApi` throw to include `Fault.Error[0].Message`. **Two-row pull-back design:** original row marked voided (keeps `qb_invoice_id` for audit linkage), new invoice INSERTed at next-free-ID with copied fields (tenant_id, job_id, job_name, call_log_id, proposal_id, amount, discount, retention_pct/amount, due_date, description, intro, show_cents). Pay-app branch: original voided + pay app to draft + `billing_schedule_pay_apps.invoice_id` cleared (new invoice spawns on re-lock). Voided rows surface on Invoices list (status → red VOIDED pill, totals exclude), Job Detail (linked invoices with strikethrough + VOIDED treatment, Billed sum excludes), Detail page (VOIDED pill + red reason banner + only "Hide from Lists" button), PublicInvoicePage ("no longer active" notice). URL-direct fallback fetches voided rows that list filter excludes. `key={sel.id}` on InvoiceDetail forces remount on id change to prevent stale React state across two-row navigation. Smoke verified end-to-end on Vercel preview: real-job send → QB sync → pull-back-with-reason → new invoice spawned → aggregators correct → voided UI on all surfaces → re-sync new invoice to QB (no DocNum conflict, fresh ID). Followups: **B38** (kill Refresh Stripe Link button, add staleness triggers), **B36** (send-invoice math), **B37** (false-sister blocks sequential proposals). Branch awaiting merge to main. |
| B33 | T1   | Closed 2026-05-21 | Invoice Stripe pay links die 24h after send — Checkout Sessions hard-capped; customers re-clicking older emails get "You're all done here" dead-end | Found 2026-05-21 (customer hit pay link for invoice #10028 ~24h+ after send → Stripe "checkout session has timed out" page) | **Closed same session (Loop #23, PR #26, merge SHA `896b09b`).** Swapped `POST /v1/checkout/sessions` → `POST /v1/payment_links` (link lives until `active=false`). New column `invoices.stripe_payment_link_id` (migration `20260521130000`). Three deactivation triggers: webhook on `checkout.session.completed` (paid), `handlePullBack` (pulled back), `handleVoidConfirm` (voided/deleted). New edge fn `deactivate-payment-link` for void/pullback (takes `{invoiceId}` only, server reads link ID after tenant binding). Audit round-2 M3 fix in commit `00c2eac`: deactivate prior Payment Link on resend (caught a leaked-link case where a customer holding an older email could pay the prior amount). `inactive_message` policy: paid → "This invoice has been paid. Thank you!"; void/pullback → "This invoice is no longer active." Smokes verified: paid (Stripe Dashboard shows `Active: false`), resend (both old + new plinks correctly handled), pull-back (link deactivated). Plan: `docs/plans/invoice_payment_links.md`. **Side find during smoke (root cause distinct from B33):** HDSP's `STRIPE_WEBHOOK_SECRET` in Supabase had been wrong since 2026-04-06 (overwritten with SCC's secret during v55 SCC billing setup) → every customer Stripe payment since silently failed signature check. Fixed via `supabase secrets set STRIPE_WEBHOOK_SECRET=<correct HDSP whsec>`. Audit pulled CSV from Stripe, identified 2 historical silent-paid invoices: #10011 Tim Hohl $2,092.19 + #10023 Randy & Heather Dixon $3,887. Backfilled in same session: DB UPDATE (status='Paid', paid_at, stripe_payment_id), QB reconciled manually by Chris. |
| B32 | T2   | Open   | Draft pay app shows stale `scheduled_value_snapshot` after SOV edit + missing rows for new SOV lines | Found 2026-05-20 (Plenium 6532 Reed HS — after editing SOV from WTC-derived to GC-supplied 7-line breakdown, Pay App #1 modal still rendered old snapshot $ values and was missing rows for the 5 newly-added SOV lines) | `billing_schedule_pay_app_lines.scheduled_value_snapshot` captures the SOV line's `scheduled_value` at pay-app-creation time (`NewPayAppModal.jsx:158`). When the SOV is later edited (rename / re-price / add lines), the snapshot doesn't refresh and new lines have no corresponding `pay_app_lines` row, so the PayAppDetailModal renders a confusing partial view. Current workaround (today's Plenium session): Delete the draft pay app via `PayAppDetailModal.jsx:266-304` then create a fresh one — works cleanly but loses any per-line billing % the user had already entered. **Fix candidates:** (a) when opening a draft pay app, detect SOV drift (any `billing_schedule_lines` row missing from `pay_app_lines`, or any `pay_app_lines.scheduled_value_snapshot` ≠ current `billing_schedule_lines.scheduled_value`) → show a banner with "Sync to current SOV" action that upserts missing lines + refreshes snapshots, preserving billed % where the line still exists; (b) auto-sync on draft modal open (less intentional, may overwrite). (a) preferred. Snapshot semantic for non-draft (submitted/paid) pay apps must stay frozen — only drafts sync. ~half day. **2026-08-18 (lock-at-sold round-1 ADJ-1):** when the deferred SOV auto-reconcile is built (`docs/plans/lock_at_sold_enforcement.md` §6), gate it on submitted/paid pay apps, not raw pay-app count — a never-finalized draft must not permanently block reconcile. |
| B31 | T2   | Closed 2026-05-20 | Billing Schedule auto-lock blocks SOV edits after Pull Back — any non-deleted invoice (incl. status=`New`) keeps schedule locked | Found 2026-05-20 (Plenium 6532 Reed HS rejected Pay App #1 — required GC-format SOV breakdown, schedule couldn't be edited even after pulling back) | **Closed same session (Loop #22).** `BillingScheduleSection.jsx:58-65` counts ALL non-deleted invoices for the proposal to set `locked`. Pull Back (`Invoices.jsx:1250`) resets invoice → `status="New"` + pay app → `status="draft"` but does NOT soft-delete the invoice. Result: pulled-back pay apps still trigger the auto-lock, so user can't revise the SOV (e.g. switch to GC-supplied line item breakdown) without manual DB intervention. **Fix shipped:** (1) added `.neq("status", "New")` to the count query so only committed invoices (Sent / Waiting for Payment / Past Due / Paid) hold the schedule locked — manual `schedule.status="locked"` flag still wins (commit `4531c1f`); (2) ungated the "Add SOV to Pay App" button from the previous `locked &&` precondition so it shows whenever a pay app exists, since the auto-lock no longer implies "pay app exists" (commit `224ac11`). Verified live on preview deploy — Plenium Pay App #1 regenerated with GC's 7-line format and shipped. ON DELETE CASCADE on `billing_schedule_pay_app_lines.billing_schedule_line_id` worked as expected when SOV lines were swapped. Follow-up gap surfaced: stale `scheduled_value_snapshot` after SOV edit on draft pay apps — filed as B32. |
| B30 | T1   | Closed 2026-05-20 | `send-pay-app` sender confirmation From=`noreply@salescommand.app` lands in Outlook Junk for every pay-app send | Found 2026-05-20 (user repro: Plenium job 6532 Reed High School Polish, Pay App #1, invoice 10045) | **Closed same session — 15 min fix.** Logs confirmed every leg of the function succeeded (`Pay-app email response: 200`, `Sender confirmation email: 200`, `qb-sync-invoice response: 200`), but Outlook silently junk-foldered Chris's confirmation because the From was a third-party noreply address claiming to deliver to chris@hdspnv.com. 5 invoice confirmations earlier same day landed in Inbox because `send-invoice` (line 216-225) sends confirmation `from: fromAddress` (sender's verified address) and gates on `VERIFIED_DOMAINS.includes(senderDomain)`. `send-pay-app` (line 401-443) had drifted to `from: "noreply@salescommand.app"` hardcoded + `if (senderEmail)` only. **Fix:** mirrored `send-invoice`'s pattern — guard `if (senderEmail && VERIFIED_DOMAINS.includes(senderDomain))`, `from: fromAddress`. `senderDomain` + `fromAddress` already computed at lines 324-327 for the customer email path so no new variables. Edge fn redeployed to prod with `--no-verify-jwt`. **No backfill needed** — previous confirmations existed and could be retrieved from Outlook Junk. Same conceptual class as F28 (silent confirmation failure) but a different code path; F28's status-flip-server-side scope still stands separately. |
| B29 | T0   | Closed 2026-05-20 | `send-proposal` 500s on every send since H11 deploy — `escapeHtml(proposal.proposal_number)` throws TypeError on INT input | Found 2026-05-20 (user repro: job 10109 Crossbow Daycare) | **Closed same session — silent customer-facing failure.** Regression from H11 commit `be65547` (2026-05-20 09:46 PDT). New `escapeHtml(input: string)` helper at `supabase/functions/send-proposal/index.ts:11` calls `input.replace(...)`. Call site at line 132: `escapeHtml(proposal.proposal_number || proposal.id)`. `proposals.proposal_number` is INT — `(1).replace(...)` throws `TypeError: input.replace is not a function`. Outer try/catch returns 500. Client `handleSend` in `ProposalPDFModal.jsx` throws at line 100, skipping `proposal_recipients` insert AND `call_log.stage='Has Bid'` update. **Critical impact:** the throw at line 132 happens BEFORE the Resend POST at line 157, so the customer ALSO doesn't receive the proposal — but `proposals.status='Sent'` was already flipped client-side at line 73-83 (pre-H5 ordering, intentional). The "Sent" pill + activity timeline are lies. Edge fn logs show 4 invocations today all errored identically. **Fix:** widen `escapeHtml` signature to `input: unknown` + `String(input ?? "")` coerce. Defensive at the helper, covers every call site. **Backfill:** every proposal sent 2026-05-20 09:46 PDT → fix-deploy needs manual re-send from UI. Identify via `select id, proposal_number, sent_at from proposals where sent_at >= '2026-05-20 16:46:00+00' order by sent_at;` **Verified:** pull-back + re-send of 10109 (Crossbow Daycare) confirmed live — customer email, rep email, call_log.stage flip all landed. Commit `90f394f`. Post-mortem → F28 (server-side status flip) + F29 (post-deploy smoke ritual), both T2. Global memory rule saved: `feedback_edge_fn_post_deploy_smoke.md`. |
| B11 | T2   | Closed 2026-05-20 | Sold proposals with `requires_pay_app=true` fall back to regular invoice flow when `billing_schedule` is missing | Found 2026-05-08 (B8 smoke) | Routing in `NewInvoiceModal.selectProposal` (`Invoices.jsx:89`) gates on existence of `billing_schedule`, not on `customers.requires_pay_app`. Schedule is auto-created only when all WTCs lock (`ProposalDetail.jsx:253`); archive-imported / approved-without-locking proposals reach Sold without one. Result: Create Invoice routes to the regular invoice modal even though the customer is flagged for pay apps. **Repro**: KalB Legislative — Weather Proofing P1 (KalB Industries, `requires_pay_app=true`, archive proposal, no `billing_schedule`) → regular invoice modal opens instead of NewPayAppModal. **Fix candidates**: (a) auto-create `billing_schedule` on Sold-transition for flagged customers; (b) surface a "Generate Pay App Schedule" button on Sold proposals where `requires_pay_app=true` and no schedule exists. ~half-day either way. |
| B28 | T1   | Closed 2026-05-19 | `send-pay-app` returns 500 on every successful send — `authHeader is not defined` ReferenceError after C9 audit dropped the variable declaration | Found 2026-05-19 (user repro: AGRU 230 Lyon Pay App #1) | **Closed same session.** Commit `37bafc7` (PR #25, merged to main `9896cc5`), edge fn `send-pay-app` redeployed to prod. Cherry-picked onto `feat/multi-gc-allocation` as `f97b726`. **Backfill:** one un-synced invoice in the 2026-05-09 → 2026-05-19 window — DA Builders Muscle Motors Pay App #2, invoice `10031` ($31,232.80), synced via Invoices page "Sync to QB" button + verified in QB Online. **Regression from C9 commit `e662d24` (2026-05-09).** That commit replaced the manual JWT extraction with `authenticateCaller()` and removed `const authHeader = req.headers.get("authorization")` at the top of `serve()`. The fire-and-forget `qb-sync-invoice` invoke at `supabase/functions/send-pay-app/index.ts:466` still referenced that deleted variable in the `Authorization` header. JS evaluates the options object synchronously; `ReferenceError` caught by outer try/catch → returns 500 to UI ("Edge Function returned a non-2xx status code"). **Side effects observed:** email DID send via Resend, `billing_schedule_pay_apps.status='submitted'` and `invoices.status='Sent'` DID get written — but `qb-sync-invoice` never fired. **Fix:** restored `const authHeader = req.headers.get("Authorization") || "";` at top of handler (pre-C9 behavior). Audit confirmed only `send-pay-app` was affected — every other edge function that uses `authHeader` declares it locally. **Lesson for future fn→fn auth refactors:** grep for ALL references to the variable being removed, not just the declaration site. |
| B14 | T3   | Open   | Proposal Summary panel does not reflect `call_log.show_cents` toggle in same view session | Found 2026-05-11 | Repro: open proposal (rounded display) → click `JOB →` → flip "Show cents on proposals & invoices (legacy jobs)" → Save → return to proposal. WTC rows in the body update to show cents, but the right-rail Summary panel (PRICE/COST/MARGIN/PROFIT, `ProposalDetail.jsx:~1088-1140` — line refs updated 2026-06-10 audit, was :1021-1081) still shows rounded values. Both surfaces share the `money` formatter declared at `ProposalDetail.jsx:18` (was :16) (`p.call_log?.show_cents ? fmt$c : fmt$`), so the discrepancy points to a stale `p` (call_log relation) at this entry path — body re-renders from a fresh `wtcs` fetch but `p.call_log` was loaded before the toggle. Likely fix: re-fetch `proposals` with the `call_log(show_cents,...)` join when the proposal view re-mounts/regains focus (mirror the existing refreshes at `:481/:616/:622`). Cosmetic — no money math affected. ~1h. |
| B12 | T3   | Open   | QBActionModal "Skip QB Sync" — visual weight of Skip icon (#1 remaining)      | Found 2026-05-10 (3a smoke) | **#2 shipped 2026-05-11** (Touches B12) — persistent "QB SYNC SKIPPED · Resume" pill now renders on CallLogDetail in place of the green Connect CTA when `qb_skip_sync=true && !qb_customer_id`. Resume clears the flag in one click, mirrors the existing LINKED/Unlink pattern (`CallLogDetail.jsx`). **#1 still open**: Skip button's icon background `rgba(28,24,20,0.08)` (faint gray) reads as disabled vs. the bold green / dark-with-teal Create/Link icons in `src/components/QBActionModal.jsx`. Fix candidate: raise Skip icon contrast to match Create/Link visual weight. ~15 min. |
| B1  | T3   | Closed 2026-06-10 | WTC step tabs clip at narrow widths, no nav to later steps                    | Found 2026-04-18 | **Closed 2026-06-10 (audit — already shipped).** Verified fixed in current code: tab bar has `overflowX:"auto"` + per-tab `whiteSpace:"nowrap"` (`WTCCalculator.jsx:2122`), every step is a directly-clickable `setTab(t.key)` button (`:2126`), and Back/Next exist (`:2073/:2079`). Original ask (horizontal scroll or wrap) is live. |
| B2  | T3   | Open   | `send-invoice` error surfacing — apply `fnErr.context.json()` pattern         | v90 carryforward | Pattern lives in QBLinkModal. UX polish, not a bug per se. |
| B3  | T3   | Open   | Page remount on list ↔ detail transitions                                     | v90 carryforward | Performance polish; minor flicker. |
| B4  | T3   | Open   | History Locker pagination — `DataTable` sorts only the visible page           | v90 carryforward | Sort-across-pages is the real ask. |
| B5  | T3   | Open   | `importApi.js` bulk CSV does NOT honor virtual `qb_skip_sync`                 | v90 carryforward | Bulk-CSV path skips the per-row flag set by the regular flow. |
| B46 | T3   | Open   | `award_proposal` RPC has same false-sister predicate as B37 — uses `call_log_id` scan instead of lineage | B37 followup 2026-05-22 (re-IDed from duplicate B39 on 2026-06-10) | `supabase/migrations/20260518180000_multi_gc_rpcs.sql:548-551`. No UI wires `award_proposal` yet (`grep award_proposal src/` = 0 hits). Fix when Mark Awarded UI lands (§10 step 9). Same lineage predicate shape as B37 fix. ~15 min. |
| B7  | T3   | Open   | Archive imports were landing `archived=true` (root cause unknown)              | Found 2026-05-06 | Defensive fix shipped (explicit `archived: false` in ImportToLiveWizard call_log insert, commit `eb0b94f`). DB column default is `false`, no INSERT triggers, auto-archive doesn't match Sold + fresh rows. Suspect: accidental "Move to Old Jobs" click on CallLogDetail (`CallLogDetail.jsx:414`) during testing, OR an unfound code path. Reproducible test on prod will tell us if defensive fix alone is enough. |
| B20 | T3   | Open   | 36 historical invoices have `job_id` storing full descriptive strings instead of plain `display_job_number` | Found 2026-05-14 (Loop #5 root-cause investigation) | Surfaced while diagnosing B21 (public invoice 400). All 36 live invoices have `job_id` like `"7448 - Tim Milton Construction LLC - 2900 Clear Acre Ln - Demo,..."` instead of just `"7448"`. Current live writers (`Invoices.jsx:221`, `NewPayAppModal.jsx:176`) correctly use `display_job_number || job_name`, so new invoices written via the UI today look fine; the 36 came from a removed code path or an import script. **Downstream impact:** QB sync edge functions (`qb-record-payment`, `qb-void-invoice`, `qb-sync-invoice`) look up call_log via `display_job_number = invoice.job_id` — they silently no-op for these 36 if triggered. Customer-facing impact is already closed (B21 makes RLS go through `call_log_id` instead). Cleanup path: `UPDATE invoices SET job_id = split_part(job_id, ' - ', 1) WHERE job_id ~ ' - ';` then sanity-join back to `call_log.display_job_number` for the 36. ~30 min. |
| B15 | T3   | Open   | Discount sanity — WTC price can go negative with steep discount on low-markup WTC | Found 2026-05-11 (Multi-GC §3 Round 4) | Pre-existing pathology surfaced (NOT introduced) by Multi-GC `markup_override_pct` analysis. `calc.js:73` does `Math.ceil(labor.total + mats + trav - (wtc.discount || 0))` with no floor at zero. With `wtc.markup_pct=10, discount=200` on a low-labor WTC, the result can land below zero; `Math.ceil(-50) === -50` doesn't help. Worsens slightly under Multi-GC: an additive `markup_override_pct=-15` on the same WTC clamps effective markup to 0 (per §3 Markup Arithmetic resolution), making the negative-total floor easier to hit. Fix candidates: (a) clamp `calcWtcPrice` return at `Math.max(0, ...)`; (b) UX warning at `WTCCalculator.jsx:1786-1790` when `discount` would push total negative (requires recompute on discount change). ~1h either way. Cosmetic-financially, but proposal totals reading "-$50" on the right rail is wrong. |
| B16 | T2   | Closed 2026-05-20 | QB doubled DisplayName regression — PR #22 `dedupRepeated()` did not catch the real-world pattern | Found 2026-05-11 (post-PR #22 verification in prod) | Shipped 2026-05-11: PR #22 (commit `7cd5633`) added `dedupRepeated()` at top of `supabase/functions/qb-create-job/index.ts` to strip exact `"X - X"` doubles before push to QB `DisplayName`. Chris re-verified in prod after deploy: doubled DisplayName **still landing**. Implication: actual failure mode doesn't match PR #22's assumption (split on `" - "`, even segment count, case-insensitive first-half === second-half). Possible variants: odd segment count, different separator/whitespace, partial-overlap repeats, or the doubling happens *upstream* of `qb-create-job` (e.g., in the `call_log.job_name` write path during archive import or NewInquiryWizard) rather than at QB push time. **Deferred — not chasing now.** Next step when picked up: capture the literal `call_log.job_name` value pre-push side-by-side with the resulting QB DisplayName for a known-bad case, then redesign dedupe (or fix the upstream writer). Persists until manually cleaned up in QB. ~1h once a real example is in hand. |
| B19 | T2   | Closed 2026-05-20 | `merge_customers` RPC drops sister proposal lineage — `proposals.customer_id` silently nulled on merge | Found 2026-05-14 (§10 step 4 audit Pass 3) | Migration `20260430120000_customer_delete_merge.sql:192-330` was authored before Migration 1a (2026-05-13) added `proposals.customer_id`. The merge body re-points `call_log.customer_id`, `customer_contacts.customer_id`, `customer_pay_app_templates.customer_id` from duplicate → survivor, then `DELETE FROM customers WHERE id = p_dup_id`. The DELETE fires `ON DELETE SET NULL` on the new FK `proposals_customer_id_fkey`, silently nulling any sister proposal that pointed at the duplicate. **Functionally OK via COALESCE fallback** (sister resolves to survivor via `call_log.customer_id` because step 1 of the merge just repointed it), but explicit lineage is lost — audit/history forgets the proposal was ever a sister, and any future re-point of `call_log.customer_id` drifts the sister away from the survivor. **Fix:** add `UPDATE public.proposals SET customer_id = p_survivor_id WHERE customer_id = p_dup_id;` immediately before the `DELETE FROM customers` in step 6. One line, same shape as step 1's `UPDATE public.call_log SET customer_id = p_survivor_id WHERE customer_id = p_dup_id`. Returns `int` for `v_proposals_moved` to mirror existing diagnostics pattern at lines 264/269/289. ~30 min. **Filed as separate migration from §10 step 4 RPCs** per Step 4 Ratifications row #6 (write-side gap vs read-side fix; cleaner rollback granularity). Also auto-heals `CustomerMergeModal.jsx:61` count display (transitive — the count is fetched live from `call_log` so the next merge run gets correct numbers once sisters get repointed pre-DELETE). |
| B22 | T2   | Closed 2026-05-14 | C1 fix sister-aware `mark_proposal_signed` + `'Signed'` status — shipped + verified in prod | Loop #6 (2026-05-14) | **Closed.** Merged commit `1738370`, migration `20260514000000` applied to prod, edge fn `proposal-signed` redeployed, Vercel main built. Smoke complete on TEST customers: (a) Multi-GC PASSED — signed sister wrote `status='Signed'`, `call_log.stage` unchanged, no new QB sub-customer; UI pill + filter tab live. (b) Single-GC PASSED on fresh TEST proposal — `status='Sold'` + `stage='Sold'` byte-for-byte unchanged. Two side findings filed (NOT caused by C1): B23 (Pull Back doesn't clear `signing_token_consumed_at`), B24 (`PublicSigningPage` silent-success UI). Plan ref: `docs/plans/multi_gc_allocation.md:2516`. Loop #6 amended success → 10/10 at 15:24 (see `~/erd-loop/LOG.md`). Replace with Multi-GC umbrella row when §10 step 11 ships. |
| B23 | T2   | Closed 2026-05-20 | Pull Back does not clear `signing_token_consumed_at` — proposal can never be re-signed after pullback | Found 2026-05-14 (Loop #6 single-GC smoke) | **Pre-existing since H5 (2026-05-11), not caused by C1.** When a customer signs a proposal, `signing_token_consumed_at` is set (H5 single-use guard). Pull Back (`ProposalDetail.jsx` `handlePullBack`) currently clears `status` → `Draft`, clears `approved_at`, deletes `proposal_signatures` rows — but **does NOT clear `signing_token_consumed_at`**. Result: after pull-back + re-send + customer attempts to sign, the RPC's `WHERE signing_token_consumed_at IS NULL` matches 0 rows and raises `ALREADY_SIGNED`. Customer sees success in UI (B24), but DB never updates. Smoking-gun row: proposal `6e6b120b-e960-4791-b87e-2e4f3a7a8349` (call_log 3573, displayed as "10085 - TEST-P1") — status=`Sent`, approved_at=`null`, signing_token_consumed_at=`2026-05-11 02:13:02`, 0 signature rows. **Fix:** `handlePullBack` UPDATE must also set `signing_token_consumed_at = null` (and refresh `signing_token` to a fresh uuid to invalidate the old emailed link). Companion: send-proposal already mints fresh token per H5 `handleSend` refresh, so a single `signing_token_consumed_at = null` set in pullback may be sufficient — verify by reading current `handlePullBack` body. ~30 min including verify + smoke. **DB cleanup for already-broken rows:** `UPDATE proposals SET signing_token_consumed_at = NULL WHERE status IN ('Draft','Sent') AND signing_token_consumed_at IS NOT NULL AND approved_at IS NULL;` — one-shot ad-hoc query, not a migration. |
| B24 | T2   | Closed 2026-05-20 | `PublicSigningPage` shows success to customer even when DB rejects the sign (e.g. `ALREADY_SIGNED` 409) | Found 2026-05-14 (Loop #6 single-GC smoke) | **Pre-existing, not caused by C1.** When `proposal-signed` edge fn returns non-2xx (409 `ALREADY_SIGNED`, 400 `INVALID_TOKEN`, 400 `INVALID_PDF_URL`, etc.), `PublicSigningPage.jsx` appears to render the success state regardless. Customer types name → clicks Sign → sees "Thank you, your signature has been recorded" → walks away thinking it's done. DB never wrote. Witnessed today: proposal `6e6b120b-...` accepted two sign attempts in the UI; zero signature rows in DB; `signing_token_consumed_at` still pointing at original 2026-05-11 timestamp. **Fix:** surface the edge fn error to the customer-facing UI — at minimum show "This proposal has already been signed" for 409, "This signing link is no longer valid" for 400 INVALID_TOKEN. Pattern lives in `QBLinkModal` (`fnErr.context.json()`) per existing B2. ~1h. **Worse-than-bug class:** customer-facing silent failure on a money-bearing surface; partially masked by B23 today but will resurface on any sign failure (expired token, network race, etc.). |
| B25 | T2   | Closed 2026-05-15 | CallLogDetail edit Save Changes — duplicate `job_number` (23505) silently dropped, no popup, edit mode exits, refresh shows old value | Found 2026-05-15 (user repro on job 10103) | **Closed same session.** Commit `9787a29` shipped + verified live on prod by Chris. **Symptom (searchable):** silent save fail / no error popup / edit mode dropped on save failure / changes don't persist after Save Changes / duplicate Job Number not blocked. **Repro:** CallLogDetail → Edit → change Job Number to a value already in use on another active job → Save Changes → no error visible, edit mode exits, browser refresh shows old value. **Root cause (two-part):** (1) `idx_call_log_unique_job_number` partial unique index (B10) raised 23505 and `handleSave` correctly set `setError(err.message)`; (2) the Save button onClick was `await handleSave(); setEditing(false)` — `setEditing(false)` ran unconditionally on both success AND failure, and the error `<div>` lives inside `{editing && (...)}`, so it unmounted the same tick it was set. **Fix:** `handleSave` now returns `true`/`false`. Both Save buttons (the top-bar pill at `CallLogDetail.jsx:428` and the bottom save block at `:883`) only call `setEditing(false)` when save returned `true`. 23505 / duplicate-key err mapped to a plain-English message ("Job Number X is already in use by another active job. Pick a different number or merge the duplicate."); `alert()` popup mirrors the existing `alert()` pattern used for delete failures at `:161`. **File:** `src/components/CallLogDetail.jsx` only. **Tests it should have caught earlier:** any "save returns error, UI must show it" check on edit-style modals. Similar shape may exist elsewhere — audit candidates: anywhere the pattern `await handleX(); setEditing(false)` (or `setOpen(false)`) appears around an async save. |
| B27 | T2   | Closed 2026-05-15 | WTC MaterialPicker — three save-side bugs + missing edit affordance | Found 2026-05-15 (Loop #8 user repro on Resuflor 3746 Tint) | **Closed same session.** **Symptoms:** (1) custom row's `SAVE`-to-catalog button disappears with zero visible feedback; user can't tell if save worked. (2) Saved-to-catalog material doesn't appear in MaterialPicker dropdown until full page refresh — `MaterialPicker.catalog` was loaded once on mount and never refetched. (3) Search filter only matched `name + kit_size` — searching by supplier ("Sherwin") didn't find materials whose supplier was Sherwin Williams. (4) Side find: `saveCustomToCatalog` had a stale-closure on `items`; any field edits made during the supabase round-trip were reverted when `from_catalog: true` got stamped. **Fixes (all in `src/pages/WTCCalculator.jsx`):** (a) `MaterialsTab` now owns `catalog` state + `loadCatalog`; `MaterialPicker` accepts catalog as prop. (b) After `saveCustomToCatalog` insert success, `loadCatalog()` runs so dropdown shows the new row immediately. (c) Picker filter haystack now `name + kit_size + supplier`. (d) `onChange(curr => curr.map(...))` functional setter replaces the closured `items.map(...)`. (e) `justSavedId` state renders `✓ Saved` in the row for 1.5s before `from_catalog` flips and the button hides. **Edit affordance added (same scope):** pencil ✎ button on each tenant-owned (`tenant_id != null`) row in the picker dropdown opens a centered modal (`CatalogEditModal`) with name/kit_size/price/coverage/supplier; saves via `UPDATE materials_catalog`, refetches, closes. Built-in system rows stay read-only. Mirrors `MaterialsCatalogSection` pattern in `Settings.jsx:229` but inline to the WTC flow so price changes don't require a context switch. **Verified by Chris:** all 7 smoke tests pass (save feedback, picker refresh, supplier search, stale-closure preserved Qty edit, edit modal opens/saves/refreshes, system rows have no pencil, cancel discards). |
| B40 | T2   | Closed 2026-06-10 | NewInquiryWizard `customers` UPDATE silently swallows errors; wizard proceeds to call_log INSERT on half-applied state | Loop #28 audit 2026-05-28 | **Closed 2026-06-10** (`fix/b40-wizard-update-error-guard`) — `NewInquiryWizard.jsx:324` now destructures `{ error: cuErr }` and halts with `setError` + `setSaving(false)` + early return on failure, mirroring the `custErr` guard at `:352`. ORIGINAL: `NewInquiryWizard.jsx:324` ignores the UPDATE's return value and proceeds to `call_log.insert` at `:383` regardless. On RLS rejection or network blip, the wizard ships a `call_log` row for a customer whose contact/billing fields didn't update. Same shape as B25/B28/B29 silent-failure class. **Fix:** destructure `{ error: cuErr }` from the UPDATE, halt on error with `setError` + `setSaving(false)` + early return. ~10 min. **F28 (server-side status flip + UI banner) is the architectural generalization** that would close this and the entire silent-failure class across all send-* paths; B40 is the local fix until F28 ships. |
| B41 | T3   | Open   | QB sync/void "test"-name asymmetry — manual Sync pushes test invoices to QB, but Void/Pull Back skips QB → orphaned live QB records | Found 2026-06-01 (Loop #30 retention-release smoke cleanup) | **Asymmetry:** the manual "Sync to QuickBooks" button (`handleQBSync`) and `qb-sync-invoice` itself have NO `"test"`-name guard, so a job whose `job_name` contains "test" still pushes invoices to QB. But `handleVoidConfirm` (`Invoices.jsx:1398-1400`), `updateStatus`→Paid (`:1123`), and `handleSaveEdit` (`:1280`) all gate QB calls on `!job_name.includes("test")`. Net: test invoices can get INTO QB but the app refuses to take them OUT — Void/Pull Back marks the row voided in the SC DB while leaving the QB invoice live + Open. **Witnessed:** Loop #30 smoke on job #10021 ("TEST Sold Job to Schedule Command connection") — invoices #10063 (source) + #10064 (release) synced to QB, then Pull Back left both live/Open in QB; Chris had to void them manually in QBO. **Fix candidates:** (a) add the same `isTest` skip to `handleQBSync`/`qb-sync-invoice` so test jobs never reach QB in the first place (symmetric, prevents the orphan); (b) drop the `isTest` guard from the void/paid/edit paths so the app always mirrors QB regardless of name (symmetric the other way — but then test jobs DO live in QB). (a) is the cleaner default — test jobs shouldn't touch real QB books at all. Until fixed, QB cleanup after any test smoke that synced must be done by hand in QBO. ~30 min. |
| B42 | T2   | Open   | [Sev 6 · Like 4 · Eff half-day] `PayAppDetailModal.handleSaveLines` updates the invoice header but NOT `invoice_lines` → stale per-line amounts on QB re-sync | Found 2026-06-04 (Loop #32 — pay-app-invoice-edit-zeroing plan §7) | **Non-zeroing UI→QB drift, distinct from the §3 edit-zeroing bug fixed this loop.** `PayAppDetailModal.handleSaveLines` (`src/components/PayAppDetailModal.jsx`, ~`:232-261`/`:249-252`) writes the recomputed total to `invoices.amount` + `billing_schedule_pay_apps.this_app_amount` but does **not** update the corresponding `invoice_lines.amount` / `billing_pct` rows. So after a user re-edits pay-app billing % via the modal, the invoice header is correct but the per-line `invoice_lines` rows hold the prior amounts. `qb-sync-invoice` builds QB lines from `invoice_lines.amount` (`supabase/functions/qb-sync-invoice/index.ts:226-238` — ref updated 2026-06-10 audit, was :232-244), so a later "Sync to QuickBooks" re-sync would push the **stale** per-line numbers (header total and QB line sum diverge). Not a zeroing bug (lines keep real values, just outdated) and not triggered by the Edit form, so out of scope for the §3 fix. **Fix:** in `handleSaveLines`, also UPDATE each `invoice_lines` row (amount + billing_pct) from the recomputed pay-app lines, in the same write that updates the header. ~half-day incl. smoke. Independently confirmed by the planning agent (plan §8). |
| B43 | T3   | Open   | [Lev 6 · Eff half-day] `handleSaveEdit` does ad-hoc per-line-type dispatch with a **silent-zero default** — invert so unknown line shapes preserve, not zero | Found 2026-06-04 (Loop #32 — pay-app-invoice-edit-zeroing post-mortem) | **Hardening against the recurrence of a money-zeroing bug class, not an active bug** (both known line types — archive + pay-app — are now guarded). `handleSaveEdit` (`src/pages/Invoices.jsx:1250-1266`) recomputes every line's amount on save via a chain of `if`s: `if (isArchiveInvoice)` → `if (l.billing_schedule_line_id)` → else `wtcTotal × pct`. The **else** branch is the trap: `calcWtcPrice(null) → 0`, so any line type whose recompute source is absent lands in the default and saves `$0` silently. This exact mechanism zeroed archive invoices (fixed 2026-04-20 `14000c5`) and then pay-app invoices (fixed 2026-06-04 `33c385e`) — two instances of one root pattern. A 4th invoice/line type added later repeats it unless someone remembers to add a guard. **Fix:** invert the default — a line with no real recompute source (no `proposal_wtc` AND no directly-entered amount) must **preserve** `parseFloat(l.amount)`, never compute `0`. Equivalently: only recompute when you positively have a source; otherwise carry the stored value. Then the next new line type fails safe. Pairs with new CLAUDE.md Data Integrity Rules #6/#7. ~half-day incl. smoke across all three current types (archive / pay-app / proposal-WTC). |
| B44 | T2   | Open   | [Sev 6 · Like 4 · Eff half-day] No trustworthy QB **re-sync** path for an already-synced invoice — `handleQBSync` button is gated `!inv.qb_invoice_id`, so it only shows BEFORE first sync | Found 2026-06-04 (Loop #32 — #10069 manual remediation) | The "Sync to QuickBooks" button (`src/pages/Invoices.jsx:1760`) renders only when `!inv.qb_invoice_id` (plus qb_customer_id + not-skip + status/payapp) — a *first-time* sync affordance that disappears once `qb_invoice_id` is set. So for an invoice already in QB whose data later changes (e.g. the #10069 repair), there is **no button that calls the error-surfacing `handleQBSync`**. The only UI path that re-invokes `qb-sync-invoice` is Edit→Save (`:1300-1302`), which is fire-and-forget (`.catch(()=>{})`) and swallows QB errors — the exact path plan §5 said NOT to use for re-sync. Net: after correcting an already-synced invoice you cannot cleanly re-push to QB from the app; on #10069 the QB invoice was fixed by hand in QBO instead. **Fix candidates:** (a) also render a "Re-sync to QuickBooks" button (calling `handleQBSync`) when `inv.qb_invoice_id` IS set, so the trustworthy path is always reachable; (b) make the Edit→Save QB call await + surface errors. (a) is cleaner. ~half-day incl. smoke. **UPDATE 2026-07-13 (v164): candidate (b) is DONE** — the Edit→Save resync now awaits + reads `fnErr.context.json()` + surfaces the real QB fault, non-destructive on failure (shipped with the edit-sent-invoice feature). **B44 stays OPEN for candidate (a):** a standalone "Re-sync to QuickBooks" button for an already-synced invoice whose data changed OUTSIDE the edit form. |
| B45 | T2   | Open   | [Sev 6 · Like 4 · Eff half-day] Invoice PDF copy in the pay-app package is **cached on `invoices.pdf_url` and never invalidated** when the invoice changes → a stale/zeroed PDF persists in the package | Found 2026-06-04 (Loop #32 — #10069 package invoice copy showed $0 after data repair) | `PayAppDetailModal.handleSend` (`src/components/PayAppDetailModal.jsx:330-345`) does `invoicePdfUrl = invoice.pdf_url \|\| null; if (!invoicePdfUrl) { generate + store }` — it only regenerates when the cached URL is empty. So a PDF generated while the invoice was wrong (e.g. #10069's $0 copy) stays referenced forever; the data repair fixes the row but not the stored file, and the package keeps attaching the stale copy. Same cache-not-invalidated family as B43. **Witnessed:** after #10069 was repaired in DB + QB, the package's invoice copy still showed $0; cleared manually with `update invoices set pdf_url = null where id='10069'` so it regenerates correct on next send. **Fix:** clear/invalidate `invoices.pdf_url` whenever invoice money fields change (handleSaveEdit, handleSaveLines, the repair path), OR always regenerate on send instead of caching. ~half-day. Pairs with B44 — both are stale-artifact-after-correction bugs surfaced by the #10069 remediation. |
| B26 | T2   | Closed 2026-05-15 | CallLogDetail sticky-tag bug — `handleSave` regexed `CO\d+` out of old `display_job_number` and pasted it back, cementing phantom CO tags into rows that never were COs | Found 2026-05-15 (Loop #7 root cause for job 6618) | **Closed same session.** **Symptom (searchable):** phantom CO / CO1 stuck in display_job_number / display says CO1 but is_change_order=false / co_number=null but display has CO / sticky CO tag / display string disagrees with relational columns / "CO" leaked from Project Name. **Repro:** New Inquiry wizard → Standard Job (or Manager Override) → Project Name = "CO1 - Lakes Crossing - Underlayment" → save. Wizard's display construction at `NewInquiryWizard.jsx:358-360` builds `${jobNum} - ${projectName}` = `"6618 - CO1 - Lakes Crossing - Underlayment"`. Later open CallLogDetail → Edit → clean Project Name to `"Lakes Crossing - Underlayment"` → Save Changes. Old `handleSave` regex `oldDisplay.match(/ (CO\d+)/)` extracted `" CO1"` and pasted into newDisplay → final `"6618 CO1 - Lakes Crossing - Underlayment"`. Display now permanently looks like a CO, but `is_change_order=false, co_number=null, parent_job_id=null` because none of the wizard's CO-mode write paths ran. Every subsequent edit preserves the stale CO tag via the same regex. **Real-world casualty:** job 6618 (call_log id 3512) — customer signed a CO1 proposal for $13,170 Underlayment but the row had no parent and no CO semantics in the DB. Loop #7 rebuilt the data: parent 6618 inserted as id 3601, 3512 properly linked as CO (`co_number=1, is_change_order=true, parent_job_id=3601`). **Code fix:** rebuild `display_job_number` from canonical columns — `parseInt(form.job_number || job.job_number)` + (` CO${job.co_number}` if non-null) + `job_name`. Never regex the old display. `parseInt` also defangs the inverse leak (free-text "6618 CO1" in the Job Number field, which is `type="text"` at `:478`). **Side effect (desired):** future edits on phantom-CO rows self-heal — saving any field strips the stray "CO1" because `job.co_number` is the truth. **File:** `src/components/CallLogDetail.jsx` only. **Companion product question (open, not blocking):** Joe's mental model — naming a fresh job "CO1" because it's a CO of work done outside the system — is real and recurring. UX paths to consider: (a) tighten Project Name validation to reject leading "CO\d+", (b) surface a "Is this a CO of work that lives outside this system?" branch in the wizard, (c) remove Manager Override entirely if edit functions suffice (Chris's instinct — separate decision). |
| B52 | T3   | Open   | [Sev 3 · Like 3 · Eff half-day] Multi-invoice cumulative 1¢ drift — sum of independently cent-rounded partial invoices can differ from `proposals.total` | exact-penny-pricing R3 audit (ADJ-D3, 2026-06-26) | Pre-existing under ceil; exact-penny pricing makes it marginally more frequent. Each partial/progress invoice cent-rounds its own lines, so across a draw schedule the summed billed total can land 1¢ off `proposals.total` (e.g. $1234.57 split as `round(617.285)×2 = 1234.58`). The §3.2.1 D-sum reconciliation only reconciles **within one invoice**, not across the full schedule. **Fix:** track cumulative billed and true-up the final invoice to `proposals.total` (carry the rounding remainder forward). Cosmetic 1¢; low at 1 tenant. |
| B53 | T2   | Open   | [Sev 6 · Like 3 · Eff half-day] Override-sister mispricing — multi-GC clone copies `v_source.total` ignoring per-GC `markup_override_pct`; invoice sites call `calcWtcPrice(wtc)` with no override → an override sister bills at the source's markup | exact-penny-pricing R3 audit (ADJ-D4, 2026-06-26) | Pre-existing; exact-penny pricing widens the visible gap. `clone_proposal_to_gcs` stamps the sister's `total = v_source.total` regardless of the sister's `markup_override_pct`, and invoice recompute (`Invoices.jsx` `calcWtcPrice(wtc)`) passes no override → a sister with a different GC markup bills at the **source** markup, not its own. **Fix:** thread `markup_override_pct` into the clone total + every invoice-side `calcWtcPrice` for sisters. Affects override sisters only (rare today). |
| B57 | T3   | Open   | [Sev 6 · Like 3 · Eff 1h] QB "Create in QuickBooks" / "Link to Existing" fails for any job or customer whose name contains a colon (`:`) — opaque "non-2xx" error | Found 2026-06-29 (QB connect on job 10118 "3:35 bug hunt work", customer Chris Berger) | QuickBooks forbids `:` in a customer/job `DisplayName` (`:` is QB's internal parent:sub-customer separator). `qb-create-job` (`supabase/functions/qb-create-job/index.ts:246-248`) builds the sub-customer DisplayName as `"<jobNum> - <jobName>"`; a job name like "3:35 bug hunt work" → DisplayName contains `:` → QB API rejects (400) → `qbApi` throws (`:72-74`) → function returns 500 with the real message in the body. But `QBActionModal.handleCreate` (`src/components/QBActionModal.jsx:21-24`) throws `fnErr.message` — the generic "Edge Function returned a non-2xx status code" — and never reads `fnErr.context`, so the user can't see the cause. "Link to Existing" then also fails to find the job, because create never succeeded → nothing exists in QB to match (not a "customer can't have multiple jobs" issue). Witnessed on job 10118; **workaround = rename the job/customer without a colon (confirmed working same session).** **Fix (two parts):** (1) sanitize the names used for QB DisplayName in `qb-create-job` — strip/replace `:` (and trim) before the QB POST, for BOTH `parentName` and `subName`; (2) surface the real error in `QBActionModal.handleCreate` via `fnErr.context.json()` (the pattern `handleQBSync` already uses in `Invoices.jsx` ~`:1446`) so future QB failures show the actual reason instead of "non-2xx". Also check whether `QBLinkModal` search needs the same sanitize. ~1h incl. smoke. Pairs with the [Invoice Send Error Surfacing] error-surfacing class. |
| B55 | T1   | Open   | [Sev 8 · Like 4 · Eff half-day] **Anon invoice-viewer can read `proposals.signing_token` (+ pricing internals) — column-grant gap** | exact-penny-pricing /security-review 2026-06-29 (pre-existing, surfaced not caused) | `public.proposals` has **no column-level REVOKE** for anon (contrast `invoices` got `REVOKE SELECT(stripe_*)` in 20260625130000). The `proposals_public_view_token` RLS policy (20260502130000) grants anon row-level SELECT on any proposal reachable from an invoice whose `viewing_token` the caller holds. With no column REVOKE, an anon holding a valid invoice viewing-token can hand-craft `GET /rest/v1/proposals?select=signing_token&id=eq.<pid>` with header `x-viewing-token: <token>` and read that proposal's **`signing_token`** — the live secret gating the anon proposal-sign/update path (`proposals_public_sign_token`) — escalating invoice-view → proposal-sign capability, plus exposing `markup_override_pct, total, billing_terms_override, intro`. Bounded (HIGH not CRITICAL) because signing tokens expire/consume on signature (20260510120000), so the window is proposals that are both invoiceable AND have a live signing token. **Fix:** mirror the invoices hardening — `REVOKE SELECT (signing_token, markup_override_pct, billing_terms_override, …) ON public.proposals FROM anon;` (**FROM anon only, never public** — GOTCHA 1 of 20260625130000), then re-trace the two anon proposal readers (PublicInvoicePage, PublicSigningPage) to confirm neither needs the revoked columns. **Separate shared-prod security migration** — needs its own anon-surface trace + smoke; not bundled into feat/exact-penny-pricing. |
| B56 | T4   | Open   | [Sev 3 · Like 1 · Eff 1h] Strip unused anon table-level write grants on `proposals` (defense-in-depth) | exact-penny-pricing /security-review 2026-06-29 (pre-existing) | Probe confirmed anon holds Supabase's default `GRANT ALL` on `public.proposals` (INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER). Not exploitable today: INSERT/UPDATE/DELETE are default-deny under RLS (all anon UPDATE policies dropped in 20260502130000 STEP 3; no anon INSERT/DELETE policy exists), and TRUNCATE — though not governed by RLS — is unreachable via PostgREST HTTP (the only anon surface). Latent unnecessary grants relying entirely on RLS as the gate. **Fix:** `REVOKE INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER ON public.proposals FROM anon;`. Defense-in-depth, not a blocker. Pair with B55 in the same anon-hardening migration. |
| B54 | T3   | Open   | [Sev 3 · Like 2 · Eff half-day] Exact-penny code-review cleanups (deferred, non-blocking) | exact-penny-pricing /code-review 2026-06-29 | Five cleanups deferred from the build's code review (fixes #1/#7/#8 already shipped in `c807a81`). **#4 (low/med):** `PublicInvoicePage.jsx:214` recomputes the Amount column via `usesExactPricing(invoice.proposals)` — if the proposal embed is ever null it safe-defaults to ceil while the stored Line Total was billed exact → display de-reconciles. Edge (embed normally present); proper fix is to store an era flag on the invoice/line instead of recomputing. **#5 (reuse):** the 5 inline `roundPrice` copies in `WTCCalculator.jsx` (1126/1180/1376/1562/2090) duplicate `calc.js` math (which says "do NOT duplicate"); a future formula change diverges on-screen/print/signing from the invoice total. Route through `calcWtcPrice`/`calcWtcBreakdown`. **#6 (reuse):** §3.2.1 line-sum reconciliation is implemented twice (`Invoices.jsx` `getLineAmount` create-path vs `handleSaveEdit` edit-path) — a later B52 fix to one misses the other; extract a shared helper. **#9/#10 (perf/altitude):** `usesExactPricing` recomputed per-row in `Invoices.jsx` `getLineAmount` / `PublicInvoicePage:214` / `ProposalDetail:830` — hoist once per render. All cleanup-only; no current incorrect output. |
| MF1 | T4   | Open   | [Sev 4 · Like 2 · Eff sprint] No transaction across Send-to-Schedule writes — a crash after the `jobs` insert strands an un-rollback-able job | material_flow Screen 1 round-2 audit 2026-07-09 (adjacent N4, pre-existing) | `handleSendToSchedule` (`ProposalDetail.jsx:557`) is N separate PostgREST calls (`jobs` insert → `job_wtcs` upsert → `materials` → `call_log` Parked). The existing code rolls back the `jobs` row only on a `job_wtcs` *error*; a process crash / network drop *between* calls leaves a partial job with no rollback, and re-send is blocked once a `jobs` row exists (`:561`). **Pre-existing, not caused by material-flow** (the plan adds no new write under D4=b). Unfixable without wrapping the send in a single RPC / edge-fn transaction. Low priority at 1 tenant; flagged so it isn't mistaken for a material-flow regression. |
| MF2 | T4   | Closed | [Sev 2 · Like 3 · Eff 1h] `[K1]` abort message shows `'undefined'` for a day missing `day_label` | material_flow Screen 1 round-2 audit 2026-07-09 (adjacent N5) | **Closed in the Screen-1 build (`buildMobValidation`, ProposalDetail.jsx).** The [K1] failure builder now names the day `d.day_label || \`Day ${di + 1}\``, so a day with no label renders "WTC 2 'Day 3'" not "'undefined'". Shipped on `feat/material-flow`. |
| MF3 | T4   | Open   | [Sev 2 · Like 3 · Eff 1h] `parseFloat("12abc") → 12` silently truncates pasted junk in `sq_ft`/`linear_ft` | material_flow Screen 1 round-2 audit 2026-07-09 (adjacent N6) | The planned `DAY_COERCE` map (`§3.1`) coerces `sq_ft`/`linear_ft` via `parseFloat`, which truncates a pasted `"12abc"` to `12` with no signal. Display-only, low harm (metrics feed Field production tracking, not money). **Fix when built (optional):** reject/flag non-numeric input on paste, or show a validation hint. Applies only once Screen 1 ships. |
| MF4 | T4   | Open   | [Sev 3 · Like 4 · Eff half-day] A proposal sent to Schedule *before* mobilizations were authored has no path to carry them to its existing job | material_flow Screen 1 live smoke 2026-07-12 | Send-to-Schedule is send-once: once a `jobs` row exists, re-send is blocked (`ProposalDetail.jsx` already-sent guard), so a proposal sent before its days were mobilization-tagged can never push the tags to its existing `job_wtcs.field_sow` — the job keeps the old untagged snapshot. Surfaced during smoke (job 93 was sent 7/6 pre-feature; re-send was a no-op). Same root as the documented pull-back staleness (plan §2A D2 / Risk #5). Not a regression — it's the existing send-once model meeting a new field. **Fix path:** a "re-sync to Schedule" action that updates the existing `job_wtcs.field_sow` in place (or a guarded re-send). Matters most for in-flight jobs at the 1 live tenant. |
| MF5 | T4   | Open   | [Sev 1 · Like 5 · Eff 1h] Mobilization dropdown label reads redundantly (e.g. "Mob 1 — Mob 1") when the user names a mob "Mob N" | material_flow Screen 1 live smoke 2026-07-12 | The WTC day mob selector renders `Mob {seq} — {label}` (`WTCCalculator.jsx` SowTab). When the user's typed label is itself "Mob 1"/"Mob 2" (the natural default), it shows "Mob 1 — Mob 1", which reads like a duplicate and confused the user during smoke. Cosmetic only. **Fix:** drop the `Mob {seq}` prefix when the label already starts with "Mob", or show just the label and rely on the row's own seq column; consider a smarter default label than "Mob N". |
| B98 | T4   | Open   | [Sev 4 · Like 5 · Eff half-day] **Schedule "Jobs Assigned" count isn't deduped by `call_log_id`** — `computeHomeDashboard` counts `jobs`-table rows (`queries.js:1694,1742`), but one `call_log` can carry multiple `jobs` rows, so a job with several allocations can over-count vs the MOBILIZATION≠ALLOCATION rule | Adjacent finding (ADJ-1), Subcon Home round-1 audit 2026-09-06 | **Repo: sales-command (`src/schedule/`).** Pre-existing in Schedule, surfaced (not caused) by the Subcon Home build, which reuses this number. Not urgent because Subcon Home shows the SAME number Schedule Home already shows, so they're at least consistent. Fix at the source (`computeHomeDashboard`) so both surfaces correct together. |
| B93 | T4   | Open   | [Sev 3 · Like 4 · Eff day] **No partial-payment column on `invoices`** — `paid_at` is binary, so a partially-paid invoice reads as full-outstanding everywhere (AR aging, Subcon Home Outstanding AR, etc.) | Adjacent finding (ADJ-2), Subcon Home round-1 audit 2026-09-06 | **Repo: sales-command + command-suite-db (schema).** Product-level gap, not specific to Home. Would need a partial-payment amount column + every outstanding-balance calc to net it out. Low likelihood at 1 tenant with mostly full-pay invoices; file for when partial payments become common. |
| B95 | T3   | Open   | [Sev 4 · Like 7 · Eff 1h] **Schedule Command "Recent Activity" panel is empty in prod (wrong DB column)** — `src/schedule/views/Home.jsx:59` selects/orders `job_changes` by `created_at`, which doesn't exist (real column is `changed_at`); the query errors and the panel silently shows nothing | buildvsplan of Subcon Home, 2026-09-06 (live-DB probe) | **Repo: sales-command (`src/schedule/`).** Confirmed against the live DB (Postgres 42703) and `JobDetail.jsx:81`, which correctly uses `changed_at`. One-word fix (`created_at` → `changed_at`, both the `.select` and `.order`). Subcon Home had the same copied bug — fixed on `feat/subcon-command-home`; this is the Schedule-Home twin, left untouched per gate scope. Same fix applies. |
| B94 | T4   | Open   | [Sev 3 · Like 3 · Eff 1h] **Two different "YTD" definitions in the app** — Managers.jsx "YTD Actual" buckets accepted-proposal dollars by `created_at` (`Managers.jsx:23,75`); Subcon Home "Sold YTD" buckets Sold-proposal `total` by `approved_at`. The two YTD numbers won't match | Adjacent finding (ADJ-3), Subcon Home round-1 audit 2026-09-06 | **Repo: sales-command.** Reconcile on ONE canonical YTD definition (population + date field) and point both screens at it, so management doesn't see two "this year" numbers that disagree. Decide which bucketing is correct (approved_at vs created_at) before unifying. |

### Features

| ID  | Tier | Status | Item                                                                          | Source           | Notes                                                                                                                                                                                                                  |
|-----|------|--------|-------------------------------------------------------------------------------|------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| F63 | T2 | In Progress — Crew Schedule and Calendar accepted / LOCKED | **Launch UI: accepted Crew Schedule and Calendar baselines; other propagation and follow-ups remain** | Chris, 2026-09-17 | **Crew Schedule visual acceptance complete:** `87e99ad49d55b987f87becd401c1dc0b5793ef93`, [exact accepted preview](https://sales-command-j5d0vc9s5-chris7berger-droids-projects.vercel.app/schedule/schedule), [handoff v287](handoffs/SC_Handoff_v287.txt). No further Crew Schedule polish without explicit authorization. Use its REAL implemented material/surface system for future controlled propagation while preserving each destination screen's existing functionality; do not recreate AI mockups. **Calendar visual acceptance complete — 2026-09-17:** Chris reviewed and accepted the [real Vercel Calendar preview](https://sales-command-3hil1ghj7-chris7berger-droids-projects.vercel.app/schedule/calendar), implementation `69ab28cf69b0631369461013b0b95c8ef7559588`; see [handoff v288](handoffs/SC_Handoff_v288.txt). Retained accepted preview branch: `ui/calendar-canonical-reskin`, originally based on `6483984`. Calendar launch-UI visual baseline is LOCKED; no additional UI changes or polish unless explicitly reopened. Existing Calendar structure, functionality, calculations, controls, scrolling and semantic colors were preserved. Build, source-boundary/lint comparison, 26 local browser state comparisons and deployed preview checks passed with intercepted fixtures and zero app data writes; Chris's subsequent real-preview visual acceptance is complete. Crew Schedule remains LOCKED and untouched; shared tokens/styles remain unchanged. Calendar has not been merged to main or released to production. Main remains `b32b30ad9a01c17a0ff17fccae7960cfc6d41f1c`. **Remaining, separately scoped/authorized:** final reference preservation, dimensional cyan-and-black Geometry A production identity assets (not implemented in sidebar; hard-hat photograph is not a reusable identity asset), propagation, and launch UI standard/DOCX/brand PDF alignment. **DESKTOP SHELL FEATURES — FUNCTIONAL FOLLOW-UP:** global search, notifications, user/business account card, additional functional top-shell controls remain deferred and were not implemented; no placeholders. Geometry A production identity work also remains deferred and was not implemented. v284 retains locked brand principles; v285/v286 record implementation. No automatic continuation, merge or production release authorization. |
| F62 | T2   | In Progress | [Lev 7 · Eff half-day] **WTC trip Job Site Contact + Access** — capture on Step 1 · Trips, not per day | Chris, 2026-09-13 (WTC; Field v17) | **WTC capture shipped 2026-09-13** (PR52 merge `698ad68`; Chris accepted preview). Sales writes people to `customer_contacts` (typed First/Last/Phone, role Job Site Contact) or references `team_members` (Sales person). Trip jsonb holds ids + snapshot + per-contact note + `access_note`. Days stay tasks/materials only. **Still open:** `job_mobilizations` columns + copy-at-Send (do not add unknown columns before the migration — seed would fail closed as a warning and drop the whole trip list). Field Customer/Job Site pipe waits on that copy. Access is one note box (Field also asked for lock/security split — not built). |
| F60 | T3   | Open   | [Lev 4 · Eff multi-day] **Ability to schedule Sunday work** — the Mon–Sat crew grids omit Sunday entirely | Split from calendar-modernization, 2026-09-06 (feat/calendar-modernization) | **Repo: sales-command (`src/schedule/`).** The business runs a 6-day week, so every crew/week surface is hardcoded Mon–Sat (`Daily.jsx:11`, `Schedule.jsx:9–10`, `StatsBar.jsx:4`, `exports.js:60,135`, `wkDates()` returns 6 days) — Sunday is structurally absent. A Sunday assignment is *counted* by the weekend-exception (`DaysModal.jsx:48–50`) and *shows* on the month calendar, but there is **no UI path to create/manage it**. Give schedulers a real way to schedule Sunday work as the exception (default day off). Cross-cutting change to the Mon→Sat surfaces + `wkDates`/`getMonday` bucketing. **Own /decide → plan when picked up.** Plan stub: `docs/plans/sunday-scheduling.md`. The calendar-modernization build makes bars weekend-aware (shows an assigned weekend day) but adds no create path — that lives here. |
| F59 | T4   | Open   | [Lev 3 · Eff half-day] Overhead / shop-work cost report — "cost of overhead to the business" | Add-job dedup ideate, 2026-09-05 (feat/add-job-dedup) | **Repo: sales-command (Schedule Command, `src/schedule/`). Follow-on from the add-job dedup ideate — deliberately kept OUT of that fix to avoid scope creep (`docs/plans/add_job_dedup_ideate_prompt.md`, Shop work section).** The ideate makes shop/training/no-customer work a first-class **overhead record type** (no customer, hidden ID, born in Sales, openable from schedule's "+ Job"). Because those records carry crew like any job, the **labor is already captured** — no new tracking needed. The gap this item fills is the **rollup view**: sum crew hours × labor rate across all overhead records into a single "cost of overhead/shop to the business" number, so Chris can see what non-billable time is costing. Not urgent; no data risk; build after the dedup fix ships. |
| F57 | T4   | Open   | [Lev 1 · Eff ~15min] AR print/export output stays green after the teal recolor | Round-1 audit of Phase 4 (AR moves in), 2026-09-02 (feat/schedule-merge-plan) | **Repo: sales-command (post-Phase-4 mount, `src/ar/`). Adjacent finding — pre-existing, NOT caused by the Phase-4 CSS drop.** `ar/lib/exportUtils.js:19, 21, 114` hardcode the old Command-Green `#5BBD3F` directly in the generated print/export HTML (header `h1`, header `.total`, and the Total-Open scorecard value). Phase 4 recolors the on-screen accent to teal `#30cfac` via `ar/lib/tokens.js`, but these 3 literals live in a self-contained print stylesheet string and are untouched → **on-screen goes teal while the Accountant-Review / Print output stays green** (screen/paper color mismatch). Fix = swap the 3 `#5BBD3F` literals to `#30cfac` if screen/paper parity matters; otherwise document print-stays-green as intentional. Trivial, cosmetic, no data. |
| F56 | T3   | In Progress (PR60 merged `3bc81ac` 2026-09-15; #2 #3 #4 remain) | [Lev 2 · Eff ~2h] Field Command web — read-layer alignment + minor cleanup (T5 review residue, 0 ship-blockers) | T5 /code-review of feat/schedule-merge-plan, 2026-09-02 (Phase 3 Field web) | **Repo: sales-command. Scoped to `src/field/`. The Phase 3 Field web build reviewed clean — every premise validated against LIVE prod (columns/types, stage filter = phone exact match, enum values SOD/EOD/submitted/approved/clock_in/clock_out all match, `jobs.lead` exists). 0 BLOCKS-SHIP. These are all LATENT / polish, filed not fixed per the limiter.** **(#1, the one with correctness teeth)** `fetchActiveFieldJobs` (`src/field/lib/queries.js`) filters `scheduled_start IS NOT NULL` server-side and windows on `scheduled_start` ONLY — it does NOT use Schedule's canonical `effectiveStart = scheduled_start || start_date` ([[feedback_extend_canonical_not_twin]]). The phone's job list filters by **stage only** (no date filter), so an active-stage job with no dates / start_date-only would show on the phone + crew but be silently dropped from every Field web screen — incl. Jobs ("Every active field job") and Crews, which don't even need date windowing yet inherit the filter. **LATENT:** prod today has 0 active jobs that are startdate-only or date-less (all 6 active-stage jobs have `scheduled_start`), so it isn't biting the current deployment. **Update 2026-09-15 (PR60 `3bbdfe2`):** desktop Field command-board implementation from PR58 was preserved/integrated, active-job filtering no longer requires `scheduled_start`, and Field date windows now read live `job_mobilizations` ranges first with effective-date fallback. Build/lint passed and Vercel preview is Ready; authenticated board-by-board data walk remains pending Chris review on preview. **Update 2026-09-15 (PR60 `a159cf7`):** Crews correction pass — JOB # is display number only; Unassigned is job-first; Crews Off → Exceptions from `crew_status` (Called Out / No Show only); Single Day + Date Range. Waiting on Chris preview accept; do not merge. **Update 2026-09-15 (PR60 `9bf11a8`+):** Field Command → Crews no longer reads `job_crew`. The office command view is a date-scoped read of Crew Scheduler `assignments` + live `job_mobilizations` + `crew` / `crew_status` via `crewWeekRows`. No Schedule writer change, no `job_crew` bridge. Waiting on Chris preview accept; do not merge. **(#2)** "today"/"last 7d" log windows compare `daily_log_entries.created_at` (timestamptz) to a wall-clock date string → day-boundary imprecision near midnight / non-UTC. No date column exists on that table (only `created_at`), so created_at is the only handle; leave unless it proves noisy. **(#3)** `Today.jsx` reimplements loading/error state instead of the shared `useAsync` (it needs `today` alongside `rows`; `useAsync` could return `{rows,today}`) — minor dedup. **(#4)** `PlainTable` falls back to array-index React keys on Crews/TimeClock/DailyLogs (no `keyField`) — harmless for static view-only lists, pass a stable key. See handoff v198. **Update 2026-09-15 (PR60, Scheduled Off date-range):** Crew Scheduler **Off** opens a FROM/TO modal independent of the visible week. Writes `scheduled-off` one `crew_status` row per inclusive day. Does not overwrite Sick / Call In / No Show. Existing `assignments` in the range warn and remain unchanged. Waiting on Chris preview accept; do not merge. **Update 2026-09-15 (PR60, Scheduled Off lifecycle):** Compact M–S dots show legend-gray `scheduled-off` (not legacy `off`). Crew detail groups contiguous ranges with Edit Dates / Remove Scheduled Off. Edit updates only that range’s `scheduled-off` rows. Remove confirms, then deletes only those rows. **PR60 merged `3bc81ac` after Chris accepted the Ready preview. Handoff v277.** Remaining original F56 polish: #2 log date-window, #3 Today `useAsync`, #4 PlainTable keys. |
| F55 | T3   | Open   | [Lev 2 · Eff ~1h] QB→SC reflect core hardening (T5/T6 non-blocking findings) | T5 code-review + T6 security-review 2026-08-27 (feat/qb-payment-status-sync) | **Repo: sales-command. All non-blocking — the reflect feature shipped clean (0 ship-blockers); these are polish.** Three items from the review: **(1)** `qb-webhook` `work[]` isn't deduped — a large signed Intuit batch fans out to many serial `reflectInvoicesFromQB` calls inside `EdgeRuntime.waitUntil`; at scale that could exceed background wall-clock. The 15-min sweep backstops any drop, so low. Fix: de-dup work items + optionally coalesce by tenant. **(2)** `reflectPayments.ts` QB `IN`-list escaping strips `'` only, not general — safe today because ids are DB/QB-sourced (`qb_invoice_id`, QB Payment `TxnId`), never attacker-reachable; harden to a strict numeric/id validator if that ever changes. **(3)** `paid_at = ${date}T12:00:00` is noon-**UTC**, not literally local noon — it still lands the same Pacific calendar day, so the audit-C2 anti-drift goal holds; tighten to a real local-noon construction only if a non-Pacific tenant onboards. Finding #4-in-review's sibling (the gate comment overclaim) was fixed in-flight, not filed. |
| F54 | T3   | Open   | [Lev 4 · Eff ~half-day] **Opened / not-opened indicator on Sent proposals AND invoices** | Chris, 2026-08-22 (screen refresh, Proposals scoreboard) | **Repo: sales-command. Covers BOTH proposals and invoices — same feature, do them together.** The new Proposals "Sent – not opened" / "Opened – no response" scoreboard keys on `proposal_recipients.viewed_at`, and there's now a per-recipient "Mark received" action (stamps `viewed_at`). Chris wants that opened/not-opened state **shown as an indicator on each proposal's Sent status going forward** — e.g., on the Proposals list Status cell (a green "Opened" vs amber "Not opened" badge next to the Sent pill) and/or the job/proposal surfaces — so a rep can see at a glance which sent proposals have been seen without opening each one. Data already exists (`proposal_recipients.viewed_at`, loaded in the list select). Just presentation: derive `opened(p)` per row and render a badge; reuse the `opened` helper. No DB change. **Note:** `viewed_at` now means "seen via link OR manually confirmed received" — the badge should read as "opened/received," not strictly "opened the link." **INVOICES (added 2026-08-22, Chris — tie to this row):** apply the identical opened/unopened tag + a per-recipient "Mark received" action to **sent invoices**. `invoice_recipients.viewed_at` already exists but is **"parity only, NOT wired"** (see CLAUDE.md `invoice_recipients`) — so this needs the public invoice page to stamp `viewed_at` on view (like the proposal signing page's `mark_recipient_viewed`) **plus** the manual "Mark received" fallback on `InvoiceDetail` for out-of-band delivery, then the badge on the Invoices list Status cell. Slightly more than proposals (the invoice view-tracking isn't wired yet); still no schema change (`viewed_at` column exists). |
| F53 | T4   | Open   | [Lev 3 · Eff ~day] **Rebuild Cash Flow Forecast + Analytics in AR Command** — deleted from Sales Command with the Sales Dash retirement | Chris, 2026-08-22 (Sales Dash retirement, `feat/screen-refresh-dash-retire`) | **Rebuild target: AR Command (NOT sales-command).** The 12-month invoice/payment forecast + proposals-by-work-type analytics were deleted with `SalesDash.jsx` (plan `docs/plans/screen_refresh_dash_retire.md`). Chris rarely used them; deferred rather than reparented into Home. **Original source to port from:** SalesDash `CashFlowModal` (forecast keyed `proposal_wtc.end_date` → invoice month → payment month via `customers.billing_terms`) + `AnalyticsModal` (`work_types` × `proposal_wtc`/`invoices`). **HARD REQ on rebuild — gate server-side.** RLS on `proposals`/`proposal_wtc`/`invoices`/`customers` is `tenant_id`-scoped only (no role predicate), so a client route guard does NOT keep company financials from a Sales Rep. Compute behind an edge fn that checks role and returns only numbers (never raw rows), or add role-aware RLS. **T4** — needs AR Command to exist as a build target first. |
| F52 | T3   | Open   | [Lev 3 · Eff ~half-day] **Home money-metric polish (T5 review residue)** — secondary pipeline-$ figures slightly overstate | T5 /code-review of feat/home-follow-up-screen, 2026-08-22 | **Repo: sales-command.** Non-blocking findings from the F47 engagement-redesign code review (0 ship-blockers; #1 re-engaged-$ fixed on-branch). Remaining, all **display-only** (never stored/transacted money): **(#2) multiple-proposals-per-job double-count** — scoreboard Wants/Has-Bid `amount` (`followUp.js` ~:412), `goneQuietBids.value` (~:289), `huntResults.jobBidValue` (~:483) all SUM every non-deleted proposal per `call_log`, so a job carrying archive+live or revised sisters shows 2× its bid $ (see [[project_mixed_proposals_per_job]]). Needs a **rule**, not a patch: multi-GC sisters are legitimately separate bids (sum), but versions/revisions of one bid are not (pick newest/live). **(#3) suppression edge** — `suppressedCustomerIds` keys only on `customer_id`, so a gone-quiet bid with null `customer_id` never suppresses off the hunt list after being worked (rare; jobs almost always have a customer). **(#5) cleanup** — extract one `todayLocal()` (America/Los_Angeles) into edge-fn `_shared`; duplicated in `follow-up-reminders` + `send-pay-app`. Core numbers (sold-this-month, target, thermometer, Sold tile) verified correct. Pick up after F47 merges. **#2 CLOSED 2026-08-22 (`feat/screen-refresh-dash-retire`):** the double-count is fixed at the source — a new shared `pipelineStats` selector (Home "Your Book" + Call Log pipeline row read it) plus `goneQuietBids`/`huntResults` now de-dup to one bid per (call_log_id, customer_id) — newest revision wins, multi-GC sisters stay separate — and value via `calcProposalTotal`/`bidValue`, never `proposals.total`. **#3 (null-customer_id suppression) and #5 (todayLocal extraction) remain open.** |
| F51 | T2   | Open   | [Lev 7 · Eff ~half-day+] **Work-a-follow-up + notes thread** — a real way to action follow-ups, not just accumulate dates | Chris, 2026-08-20 (Home redesign smoke, John's account) | **Repo: sales-command.** Surfaced by the engagement redesign's "What You Owe" box (F47): John had **89 overdue follow-ups**. Chris's diagnosis — reps set `call_log.follow_up` dates **at customer/job creation**, and until this box there was **no surface** bringing them back up, so they were written into a black hole and never worked. Today the ONLY way to "work" a follow-up is to hand-edit the date inside the job-detail screen, and there is **no visible thread of notes** on the customer/job. **The feature:** (1) let a rep **action a follow-up from where it surfaces** ("What You Owe") — mark done / reschedule / log-a-call — instead of editing a date; (2) a **notes thread** per customer/job so the rep has context. **Spine already exists:** the Hunt-list "**Log**" button writes `outreach_log` (outcome + note, tied to customer/job) and drives suppression — but it is **NOT wired to follow-ups** (logging a call does not clear a follow-up off the owe list) and the `outreach_log` notes are **never displayed back**. Wiring Log→follow-up + surfacing the notes thread is the heart of this. **Interim shipped (F47):** (a) owe list capped (8 + "show more"); (b) confirmation toast on log so a snoozed job doesn't feel vanished; (c) **Box 6 results panel v1** — Activity ("N calls logged this week") + Impact ("$X back in motion" = value of stalled bids re-engaged this week, via `huntResults()`). **NOT yet built (the real depth of F51):** true "revived/won" impact (needs outcome-over-time tracking, not the re-engaged proxy), Log→follow-up wiring (logging still doesn't clear a follow-up), and the notes thread. **Plan-mode item — /decide → ideate → plan** (touches interaction + possibly outreach_log reads; no known migration, verify at plan). |
| F50 | T4   | Open   | [Lev 2 · Eff ~2h] **Home hero image: weekly rotation + curated set** — cycle a set of sunrise/landscape photos by week-of-year | Chris, 2026-08-17 (Home redesign, "one for now") | **Repo: sales-command.** The Home redesign ships v1 with a **single static hero** (`src/assets/hero/hero-01.jpg`, the mockup's sunrise-mountains shot). This is the deferred upgrade: source + license ~20 sunrise/landscape images (Unsplash/Pexels, free-commercial, **download + bundle, never hotlink**) named `hero-01…hero-NN.jpg`, then wire the deterministic weekly helper `images[weekOfYear % images.length]` in `Home.jsx`. Full spec: `src/assets/hero/README.md`. Optional spice (also deferred): daily variant, time-of-day tie-in (sunrise in the AM). Pure polish — no urgency; a single hero looks fine. |
| F49 | T3   | Open   | [Lev 4 · Eff ~half-day] **Crew runway: "% of crew booked" upgrade** — replace the weeks number with a % gauge on a 5-band color scale | Chris, 2026-08-15 (Home redesign round-2 audit, Option A) | **Repo: sales-command.** Deferred out of the Home redesign (`feat/home-follow-up-screen`) per Chris's Option A; this is the follow-up that builds it. **UPDATE 2026-08-22:** Chris rejected shipping the weeks bar even as-is ("don't want it until it works the way I want") — Home now shows a **"Crew Runway · Coming soon"** placeholder in Box 3 until this lands. The weeks `RunwayBar.jsx` is no longer rendered on Home (still exists in the tree). When F49 ships, replace the placeholder with the real % gauge. **Full parked spec already written** — `docs/plans/home-engagement-redesign.md` amendment parts 1, 2, and 4-§A (all banner-marked DEFERRED). Scope: new `tenant_config.schedule_crew_booked_pct` (int 0–100) column + migration; `bandOf(pct)` fixed 5-band scale (<30 crit / 30–50 thin / 50–70 filling / 70–90 good / 90+ ideal); 2 new tokens `C.critical` #c0392b + `C.orange` #e67e22; deep→bright teal top bands (brightest = peak, matching app's performance-highlight teal); slide-up-when-critical + hysteresis (lift ordering to `Home.jsx`, not the child); one % Settings input (Admin/Manager). Rewrites `src/components/followup/RunwayBar.jsx` (currently weeks-based). **Why deferred:** net-new mechanism that generated 10 of 19 round-2 findings — scope-cut off the reskin, gets its own plan + audit. **Plan-mode item — /decide → ideate → plan** when picked up. |
| F47 | T2   | Closed 2026-08-22 | [Lev 7 · Eff ~half-day] **Home → Follow-Up screen: visual redesign** — the engine is built, the LOOK is not | Chris, 2026-08-11 (build session) | **CLOSED — merged to main `4bb33a4` (PR #35) + LIVE in prod 2026-08-22; all three cold gates green (T4 /buildvsplan, T5 /code-review, T6 /security-review). Smoke on John Paul's live data caught + fixed 3 accuracy bugs (archive sold-date → real `job/soldDate` month, goal divisor → active Sales Reps, donut >100%). Follow-ups: F49 (% runway), F51 (work-a-follow-up), F52 (metric polish), B70 (archive dating app-wide).** BUILD LANDED 2026-08-17 on `feat/home-follow-up-screen` (engagement redesign per `docs/plans/home-engagement-redesign.md`): 6-box reskin of `Home.jsx` matching `docs/home-engagement-mockup-v1.png` — YOU hero (results/effort/fresh states, static `hero-01.jpg`), personal money bar + 2-view SVG donut, shared runway (weeks `RunwayBar` reused as-is) + goal thermometer, Your Book scoreboard (tap-to-drill), What You Owe (bids+follow-ups), Where To Hunt finder + $-tagged lists. New selectors in `followUp.js` (homeEngagement/owedItems/activeRepCount + rep-scoped variants); token scales in `tokens.js`; `repName` hoisted in `App.jsx`; CallLog nav-seed wired. Zero migrations. Build + /buildvsplan clean (3 reviewers; 2 fixes applied — rep-scoped jobCount + teal-bg on-dark CTA). **Awaiting Chris smoke on preview + merge → then Close.** Do NOT rebuild the logic — it's done + /buildvsplan-passed. The data/state engine (`src/lib/followUp.js` shared snapshot + selectors, `src/lib/alerts.jsx` provider, suppression/supersede, refresh wiring, both migrations LIVE in prod) all works and is verified. What failed is the presentation: Chris — "looks like a boring list… needs a pretty major redesign." Diagnosis: the ideate (plan `docs/plans/home-follow-up-screen.md` "Ideation decisions" + "Layout / UI") specified the WHAT (problem, data, actions, zone order) but never designed the LOOK — the Layout section is a bare ASCII wireframe + style tokens, so a faithful build = a faithful functional list. Also a tension in the ideate: it says "outbound is the point" but locked "Zone 1 Alerts on top"; tonight shipped an "action-first" redesign (runway + who-to-call hero, bids demoted) that deviates from the [LOCKED] order and still reads too list-like. **Next = a design/polish session of its own** (reference screenshots, real visual hierarchy/weight/feel). Redesign target is presentation only: `src/pages/Home.jsx` + `src/components/followup/*`; the `useAlerts()` data contract stays. See handoff v183. |
| F48 | T3   | Open   | [Lev 3 · Eff ~2h] **Dormant last-touch fallback from archive raw_data date** | Chris/build, 2026-08-11 | **Repo: sales-command.** Deferred per Chris (ship-as-is). Dormant "last touch" reads `archive.legacy_records.record_date`, but only **3 of 58** imported jobs have it populated — the other 55 show "last touch never." Fails safe (they still surface as dormant), but the plan's P4 raw_data-date fallback (`raw_data` carries `job/soldDate` / `job/calledDate` / `job/timestamp`, messy/inconsistent) was NOT built. Add the fallback parse only if the "never" rows prove noisy in real use. `followUp.js buildTouchMap()`. |
| F46 | T3   | **Closed 2026-08-10** | [Lev 4 · Eff ~2h] T&M residuals — 1 fixed, 1 verified absent, 1 folded into F44 | /code-review-limiter, 2026-08-10 (T&M billing build) | **Repo: sales-command. Closed same day — filed and then actioned rather than left to age.** **(1) FIXED** — `invoicePdf.js` mishandled a rate-card line (printed `calcWtcPrice` = $105 as the value of a $4,460 line, "0%" where hours belong, and the work-type name "T&M" on every row). Unreached today (only caller is `PayAppDetailModal`, SOV lines), but the branch went in NOW rather than as a warning for later: wiring the future regular-invoice caller (§3.6) would otherwise inherit a broken renderer. Copy of the shape already live in `Invoices.jsx` and `PublicInvoicePage.jsx`. **(2) VERIFIED ABSENT — the finding's premise did not hold.** It claimed `ProposalDetail` ArchiveProposalPanel's `billedSC` sums job-wide invoices, folding T&M into an archive card's Remaining. `linkedInvoices` is loaded at `ProposalDetail.jsx:109` with `.eq("proposal_id", p.id)` — scoped to that proposal, not the job — so it can only contain its own invoices, and T&M lines belong to a different proposal. A "fix" was written and reverted: it would have been a silent no-op, since that select does not fetch `invoice_lines` at all. Comment left at the site so the same finding is not re-raised. **(3) FOLDED INTO F44** — no server-side check that `invoice_lines.amount == reg/ot/dt hours × rate`. Real but needs a migration and a design call (cent-rounding, deliberate rate edits, and it must not fire on non-T&M lines where hours are null). F44 is already opening a migration; doing it there avoids a second one for a constraint. |
| F44 | **T1** | **Closed 2026-08-10 (merge `dc4f0a3`)** | [Lev 6 · Eff ~2h → actual ~half-day] T&M rate cards should not count as contract dollars (split out of the T&M billing loop). **DONE:** `calcProposalTotal` excludes `is_rate_card` and is the sole contract-value primitive (6 sum-sites routed through it); rate cards render as an hourly rate on proposal detail + PDF + signing page; SOV seeding skips them; `get_public_proposal_view` carries `is_rate_card`/`rate_amount`/`rate_class` (migration `20260810170000`, rehearsed green + pushed live); P7's three legacy unflagged rate lines flagged in prod ($28,379.64 → $27,999.64). The billing primitive `calcWtcPrice` was deliberately NOT touched (Data Rule #6 zeroing trap). PDF render verified by Chris. **Follow-ups (own rows / notes):** F46 (server-side T&M amount check) stays deferred; hide the now-vestigial burden-rate field on rate cards in WTCCalculator (minor). Original detail below. | Split 2026-08-07 from `docs/plans/tm_billing.md` §13 (round-2 audit) | **Repo: sales-command.** P7 on job 7215 reads **$28,379.64**; the real contract is **$27,999.64** of material plus three hourly rates ($105/$125/$150) that were entered as 1-hour WTCs and so count as $380 of phantom price. **Scope:** make `is_rate_card` WTCs contribute $0 to every proposal total and print as a rate instead of a line price — including on the **customer-facing proposal PDF and signing page**. **Why it was split:** in the round-2 audit this mechanism accounted for **8 of 14 findings (57%)**, including every finding touching a customer-facing surface; the billing half ships independently without it. **Known starting points (all verified 2026-08-07):** `proposals.total` has **three** writers — `ProposalDetail.jsx:341`, `WTCCalculator.jsx:2161`, `:2188` — all unfiltered reduces, and the §4.5 backfill path runs through `:2161`; `calcProposalTotal` (`calc.js:186`) has exactly **one** caller (`MultiGCWizard.jsx:221`), so seven of eight sum-sites hand-roll their own reduce; `WTCCalculator.handleSave` (`:2110-2140`) writes `regular_hours`/`markup_pct`/`burden_rate` unconditionally, so hiding the inputs does not neutralise `calcWtcPrice`; the signing page needs `get_public_proposal_view` replaced (`SECURITY DEFINER` — `CREATE OR REPLACE` resets `SECURITY DEFINER` and `SET search_path` unless restated) to carry `is_rate_card`/`rate_amount`/**`rate_class`**; and `rehearse.sh` currently prints "Safe to push" against a drifted baseline, so the harness must be refreshed `--against-prod` **before** that migration, with a behavioural key-diff since fingerprinting cannot see a function body. **PRIORITY RAISED T2 → T1, 2026-08-10.** The "not urgent, harms nothing" rationale has EXPIRED. It was true while the overstatement sat buried in a proposal total. The T&M build's new Job Totals ledger now surfaces it as an actionable figure on the job screen — observed on fixture job 999 with the material 100% billed:

    Contract          $2,720   ($2,340 material + $380 phantom rate-card hours)
    Contract billed   $2,340   (fully billed)
    Remaining on contract $380 (nothing — it IS the three rate cards)
    % Invoiced          96%    (should be 100%)

"Remaining on contract $380" invites someone to bill $380 that does not exist. That is operational, not cosmetic.

**Do NOT half-fix it on the job screen.** Subtracting rate cards from the contract figure there alone is ~5 lines, but then the job screen reads $2,340 while the proposal screen still reads $2,720 — two screens disagreeing is worse than one consistent wrong number. And the moment `proposals.total` drops to $2,340, `ProposalPDFModal.jsx:189` still computes $2,720 independently: that is the cents-short defect of June 2026 (`calc.js:12-18`), which is why the PDF and signing-page work is COUPLED to the total change and cannot follow later. **Plan-mode item — /decide → ideate → plan.** |
| F43 | T3   | Open   | [Lev 5 · Eff TBD] Mobilization flow is clunky / unnatural — UX rethink | DMS-1 Phase 2 smoke 2026-07-28 (Chris) | **Repo: sales-command.** Chris: "I don't like how the mobilization flow is. It's clunky and not natural." Observed pain (Phase 2 smoke): mobilizations are authored in one place (Mobilizations editor on the proposal) but assigned in another (per-day dropdown in the WTC → Scope of Work), and a proposal with none shows "None authored" with no inline way to add one from where you're working — so you bounce between two surfaces and must Save before Send re-validates. Needs a design pass (not a quick patch): make authoring + per-day assignment feel like one natural flow (e.g. add-mobilization inline from the day, or a guided first-mob prompt). **Plan-mode item — /decide → ideate → plan.** Pairs with F42 (clickable blockers deep-linking into this same flow). Scope/effort TBD at plan.  **B116 update 2026-09-10 (PR49, not merged):** retain the existing WTC trip authoring and per-day Field SOW assignment; rename visible Sales labels to Trips. Broader authoring redesign remains deferred. |
| F42 | T3   | Open   | [Lev 5 · Eff half-day] Make Send-to-Schedule review blockers CLICKABLE — jump straight to the day/material to fix | DMS-1 Phase 2 smoke 2026-07-28 (Chris) | **Repo: sales-command.** The "Send to Schedule — Review" modal (`ProposalDetail.jsx`, `buildMobValidation` + `buildSpecConfirmValidation` render blocks) lists each blocker as **dead text** ("Open the WTC → Scope of Work, assign a mobilization…" / "…click Confirm specs…"). Chris (smoke): "each one of these red things needs to be clickable and take me to the area where I need to do something" — a non-Chris user won't know where to go. **Scope:** make every red blocker row a button → closes the modal and opens the WTC editor (`setShowWTC` + `setActiveWtcId` + `setWtcInitialTab('sow')`) scrolled to the offending **day** (and, for spec blocks, the specific **material** highlighted / its Confirm-specs chip pulsed). Mechanics partly exist: WTCCalculator SowTab renders per-day; sch-command's `FieldSowBuilder` already has a `focusDayIndex` scroll-into-view pattern to mirror. Applies to BOTH block types (mobilization + spec-confirm). Also consider: a mobilization block when the proposal has **zero** mobilizations should deep-link to the Mobilizations editor, not the day dropdown (nothing to pick yet). ~half-day.  **B116 update 2026-09-10 (PR49, not merged):** routine review modal removed; invalid Field SOW trip selections and unconfirmed specs surface through the direct-send error path. Clickable correction links remain deferred and need re-scoping to that path. |
| F31 | T3   | Open   | [Lev 4 · Eff half-day] `customer_contacts` cross-surface symmetry sweep — set both `role` + `is_billing_contact` on every Billing Contact writer | Loop #28 audit 2026-05-28 | Today: `Customers.jsx` ContactModal (`:182-185`) sets BOTH `role='Billing Contact'` AND `is_billing_contact=true`. `NewInquiryWizard.jsx` (`:344-352` new-customer, `:325+` existing-customer after Loop #28 fix) sets `role` only. `ImportToLiveWizard.jsx:425-433` and `CallLogDetail.jsx:320-326` likely same shape (verify on touch). Read-side `ContactBillingPicker.isBilling` (`:19-20`) uses OR-check so reads are tolerant; but any purely-`is_billing_contact`-based reader misses wizard-inserted rows. Specific known site: `CallLogDetail.jsx:121` filters role-only and misses `is_billing_contact`-only rows — change to OR-pattern. Scope: (1) sweep every `customer_contacts` writer to set both columns when `role='Billing Contact'`; (2) sweep every reader to use OR-check; (3) consider one-shot backfill: `UPDATE customer_contacts SET is_billing_contact = true WHERE role = 'Billing Contact' AND is_billing_contact = false`. ~half-day. |
| F32 | T2   | Open   | [Lev 6 · Eff half-day] `customer_contacts` Billing Contact DB-level uniqueness — partial unique index | Loop #28 audit 2026-05-28 | Closes the race window where two wizard sessions can both insert a Billing Contact row for the same customer (caught in Loop #28 §5). Migration: `CREATE UNIQUE INDEX idx_customer_contacts_billing_unique ON customer_contacts (customer_id) WHERE role='Billing Contact' OR is_billing_contact=true;`. Backfill: dedupe existing duplicates (likely 0–3 rows in current prod). Lets `NewInquiryWizard.jsx` switch to optimistic `INSERT ... ON CONFLICT DO NOTHING RETURNING id` pattern in a future refactor — removes the preflight-SELECT path that Loop #28 considered. **Sequencing:** ship AFTER F31 (so the backfill can canonicalize on both-column shape). ~half-day. |
| F33 | T3   | Open   | [Lev 4 · Eff 1h] `ContactBillingPicker` unlock affordance for existing-Billing-Contact case | Loop #28 audit 2026-05-28 | Picker auto-locks (`ContactBillingPicker.jsx:85-93`) when `pickBillingContact()` returns a row and has NO UI to override. Forces users to edit the Billing Contact from the customer detail page before they can use a different one through a wizard flow. Add an unlock button on the locked-billing render at `:200-228` that flips `manualMode=true` and clears `billingSourceContactId`. With F32 in place, the wizard's existing-customer billing INSERT becomes idempotent (ON CONFLICT) so unlock is safe. **Blocked by F32** (otherwise unlock + type + save creates a duplicate). ~1h. |
| F30 | T2   | In Progress | [Lev 6 · Eff half-day] CC support on pay app + invoice send flows — **invoice half DONE, pay-app half OPEN** | Found 2026-05-20 (Plenium 6532 — user asked to CC additional viewer on Pay App #1 send) | **Invoice half shipped** on `feat/invoice-recipients` (2026-06-25, plan `docs/plans/invoice_recipients.md`): new `invoice_recipients` table (main/viewer), Recipients section in InvoiceDetail, `send-invoice` rewired to 3-branch resolution + per-recipient soft allowlist + separate viewer template (no pay link) + main-first multi-send, and the public invoice page Pay button removed (§4.5) so a shared `viewing_token` can't let a viewer pay. **Pay-app half still open:** `send-pay-app/index.ts:358` still sends to a single `to: verifiedRecipient` with no viewer support; UI in `PayAppDetailModal.jsx:641` exposes only a single recipient. Remaining scope: port the same main/viewer model (or a pay-app-appropriate equivalent) to the pay-app send flow + UI, allowlist-gated, with the sender confirmation listing all recipients. ~half day on the pay-app fn + UI. |
| F34 | T3   | Open   | [Lev 4 · Eff half-day] Full block/cascade void UX for retention release invoices | Loop #30 round-2 audit 2026-06-01 | Deferred from the per-invoice retention release loop (`docs/plans/retention_invoice_process.md`, scope-cut). When a **source** invoice with `retention_released=true` is voided/soft-deleted while its **release** invoice is still unpaid, there is no orphaned-release handling beyond the one-line `retention_released` copy onto the void-replacement insert (which only prevents button reappearance / double-bill). Full UX: detect an outstanding release on source void → either block ("release #N outstanding — void it first") or offer cascade-void of the unpaid release; if the release is already **Paid**, source void is independent (money already moved). Decide enforcement layer (UI vs edge fn vs trigger). The FK `retention_release_of ... ON DELETE RESTRICT` already blocks hard-deletes at the DB layer, but the app uses soft-delete/void, so this is a UI-path concern. ~half-day. |
| F7  | T2   | Open   | E: Multi-tenant onboarding auto-provision of QB retention item + Other Current Asset account | v93              | **Blocked by: H5, S2.** Growth unlock — turns Sales Command from "Chris's tool" into a sellable product. Without this, other subs (Essentials/Plus) won't have correct routing on first connect. Sprint-sized. **AUDIT 2026-06-10:** not greenfield — a retention-item resolver + an explicit onboarding TODO already live server-side at `qb-sync-invoice/index.ts:271,292`. Intended config field is `tenant_config.qb_retention_item_name`; that lookup (`:191-292`) is the integration point to build on, not a from-scratch implementation. |
| F5  | T2   | Open   | C2: Retention reminders + scheduled emails per invoice                        | v93              | Billable retention follow-up. Half day to a day. Needs: new table, datepicker UI, cron edge function, Resend wiring, dedupe + state-change skip logic.                                                                |
| F1  | T3   | Open   | Invoice numbering                                                             | v92/v93 carry    | **AUDIT 2026-06-10:** not from-scratch — sequential auto-numbering already works (`Invoices.jsx:233-245` mints the next ID via a median-cluster heuristic, with a manual-renumber outlier comment). F1 is therefore an **enhancement/config** layer over that baseline (e.g. configurable prefix/start, gap handling), not a new feature. Tier held at T3 (Chris, 2026-06-10). |
| F2  | T3   | Open   | Multi-invoice header link                                                     | v92/v93 carry    | Re-tier when scope is clearer.                                                                                                                                                                                          |
| F3  | T3   | Open   | Customer settings page                                                        | v92/v93 carry    | Re-tier when scope is clearer.                                                                                                                                                                                          |
| F4  | T3   | Open   | QB fan-out sweep                                                              | v92/v93 carry    | Re-tier when scope is clearer.                                                                                                                                                                                          |
| F6  | T3   | Open   | D: Retainage Release flow (cumulative-release invoice when job substantially complete) | v93              | Was Phase 5 of original SOV plan. **Touched Loop #30 (2026-06-01):** the per-INVOICE release path shipped (`docs/plans/retention_invoice_process.md` — "Bill Retention" button spawns one release invoice for a single source invoice's held amount + QB positive line, migration `20260601120000`). Still open: the per-JOB / cumulative release across multiple invoices or pay-apps. Bump to T2 when cumulative release becomes manual-cycles-burned-per-month rather than rare. |
| F8  | T3   | Open   | Go-backs flow (re-scheduling invoiced work)                                   | v93 carry        |                                                                                                                                                                                                                        |
| F9  | T3   | Open   | Work Types Phase 2 — Smart Field SOW suggestion                               | v93 carry        |                                                                                                                                                                                                                        |
| F10 | T3   | Open   | Call Log Archiver                                                             | v93 carry        |                                                                                                                                                                                                                        |
| F11 | T3   | Open   | Sales Rep Reminders                                                           | v93 carry        |                                                                                                                                                                                                                        |
| F12 | T3   | Open   | PandaDoc PDF attachment                                                       | v93 carry        |                                                                                                                                                                                                                        |
| F39 | T3   | Closed 2026-07-06 | [Lev 5 · Eff half-day] Persisted, multi-file attachments on the regular invoice email (release waiver etc.) — viewable in invoice history | 2026-07-02 (Chris — attach release waiver to invoice send) | **SHIPPED TO PROD + SMOKE-VERIFIED 2026-07-06.** Migration merged to command-suite-db `main` (`512c053`) + applied to shared DB via `npm run db:push` (safety + collision checks passed, table `invoice_attachments` live, REST 200); `send-invoice` edge fn redeployed `--no-verify-jwt` (bundled `_shared/attachments.ts`); sales-command merged to `main` (`e7a77e4`) → Vercel prod. Chris ran the end-to-end smoke on prod (attach → send → email carries attachment → history → remove) — PASSED. Label UX simplified 2026-07-06 (no chips; field pre-fills "Release Waiver", extra files untitled). Follow-ups **S10** (bucket-wide DELETE / cross-tenant storage.remove vector — now a live low-likelihood exposure) and **S11** (public-bucket doc sensitivity) remain open. Original build detail below. — New `invoice_attachments` table (command-suite-db) + upload UI in `Invoices.jsx` send view + attach in `send-invoice/index.ts` by lifting the proven `send-pay-app` attachment mechanism (base64 + Resend `attachments` + `isAllowedStorageUrl`). Attachments persisted per-invoice so history shows what went out. Plan + audit manifest: `docs/plans/invoice_email_attachments.md`. Distinct from F12 (PandaDoc). **Round-2 audit folded in 2026-07-02 — BUILD-READY.** R1: 6 caused-by (2H/4M). R2: 1 regression + 6 caused-by (0H/3M/3L), plateau; broken by Option 1 (bound at upload: max 3 × ≤10MB client-side). Adjacent storage-DELETE vector filed as S10. **BUILD (code) 2026-07-02 on `feat/invoice-email-attachments`:** migration authored in command-suite-db (`20260702120000_invoice_attachments.sql`, branch `feat/invoice-attachments`, **authored-not-pushed** — push gated + must sequence after Schedule's in-flight migration, §8); `_shared/attachments.ts` (bucket+invoice-prefix `isAllowedStorageUrl` + `arrayBufferToBase64`); `send-invoice/index.ts` (hoisted `warnings`+`attachmentsPayload`, fail-open pre-Stripe load/fetch/encode w/ size-guard-before-fetch, attach to main+viewers, list in sender notif); `Invoices.jsx` send-view Attachments panel (client bounds, random-entropy path, row-first remove) + `InvoiceDetail` load-effect + historical list + safe-href. `vite build` green; no new lint errors. `/buildvsplan` CLEAR (0 code Tier-1/2). **Review-fix pass (high-effort review, 8 findings) folded in 2026-07-02:** CAP 35→24MB (binds + base64-wire-safe, kills the orphaned-pay-link-on-oversize path); delete verifies row before storage.remove (silent-no-op guard); warning header genericized; label preserved on rejected pick; edge-fn filename de-dupe; 3 attachment queries → one `loadInvoiceAttachments` helper; modal double-query removed. **`/security-review` CLEAR 2026-07-02** — no High/Critical; all 4 angles (SSRF/tenant-isolation triple pin, pre-Stripe fail-open, injection/egress, RLS/privilege) refuted; L1 filed as **S11** (public-bucket doc sensitivity), S10 unchanged. **ALL GATES GREEN.** **Post-build design change 2026-07-06 (Chris):** attachment management (add/label/remove) moved from the send-flow modal to an **Attachments section on InvoiceDetail below Recipients** (send view is now read-only review) — mirrors Recipients; no edge-fn/migration/security change. `vite build` green, lint clean. Plan §"Post-Build Design Change". **PENDING (deploy sequence, not the build terminal):** migration push in command-suite-db (sequenced after Schedule's in-flight ledger entry, §8) → `send-invoice` deploy `--no-verify-jwt` → §8 smoke against a TEST recipient. |
| F13 | T3   | Open   | Contacts import from Glide (1,612 records)                                    | v93 carry        |                                                                                                                                                                                                                        |
| F14 | T3   | Open   | Multi-viewer recipients on sister proposals — defer from §7 Round 5            | Multi-GC §7 wizard re-spec 2026-05-11 | Deferred from Multi-GC v1. Wizard Screen 2 ships with one signer per sister; `proposal_recipients` already supports `role='viewer'` rows on single-GC proposals. v1.1 ask: extend Screen 2 to collect `viewer_contact_ids[]` per sister and have `clone_proposal_to_gcs` insert N viewer rows alongside the signer. Plan anchor: `docs/plans/multi_gc_allocation.md` "## Round 5 Ratifications" row #3. ~half-day. |
| F15 | T3   | Open   | Replace Commercial-heuristic with explicit GC flag on customers                | Multi-GC §7 wizard re-spec 2026-05-11 | Multi-GC v1 Screen 1 filters GC picker by `customer_type='Commercial'` as a proxy. Heuristic produces false positives over time (direct commercial owners that aren't GCs). Cleaner: add explicit `customers.is_gc boolean DEFAULT false` (or new `customer_type='General Contractor'` enum value if a values rationalization pass happens first) + backfill from existing Commercial rows that have a sales-rep history of multi-GC fanout + UI checkbox on Customer detail. Plan anchor: `docs/plans/multi_gc_allocation.md` "## Round 5 Ratifications" row #8. ~half-day. |
| F16 | T1   | Closed 2026-05-18 | Re-plan §5 sync identity after §5(c) reversal — clone-lineage column on proposal_wtc | Filed 2026-05-12 (§5(c) reversal) | §5 sync RPCs (`preview_sync_to_sisters`, `apply_source_edit_to_sisters`) and §4 clone RPC (`clone_proposal_to_gcs`) assumed `proposal_wtc.work_type_id` is a 1:1 join key. §5(c) reversal invalidates that. New shape needed: add `proposal_wtc.cloned_from_wtc_id uuid REFERENCES proposal_wtc(id) ON DELETE SET NULL` in a future migration, then sister WTC ↔ source WTC matches by lineage (mirror of `proposals.cloned_from_proposal_id`). Re-plan covers: (a) clone RPC body sets `cloned_from_wtc_id` per sister WTC row at clone time; (b) sync RPCs join sisters back via `cloned_from_wtc_id`, not `work_type_id`; (c) award flow — confirm no other 1:1-WTC-join assumptions; (d) `locally_edited_fields[]` semantics unchanged (still per-WTC override list). **Blocks: §10 step 6 (all sync + clone RPCs) and any downstream RPC work.** Step 8 (wizard scaffold) can start without it but RPC wiring waits. Estimated: one Plan-agent round in the existing file-as-memory pattern on `feat/multi-gc-allocation`. |
| F17 | T1   | Closed 2026-06-11 | Multi-GC wizard surface (§10 steps 8–11)                                              | §10 implementation 2026-05-18 | **CLOSED 2026-06-11 — verified in production use** (Chris confirmed): multi-GC ran end-to-end on a real job — **Invoice #10046, Plenium / Reed High School Polish**. Wizard surface + step-11 differentiators are code-complete on main and all three migrations are live (`20260513000000_multi_gc_allocation.sql`, `20260518180000_multi_gc_rpcs.sql`, `20260519200000_multi_gc_step9_wiring.sql`). Real prod use is the smoke — no separate preview-deploy smoke needed. Steps 8–11: CallLog multi-GC count chip, Proposals sister differentiator (↳ + SISTER badge), WTCCalculator→SyncConflictModal wiring. All *polish* work is carried by F20–F26. Plan doc: `docs/plans/multi_gc_allocation.md`. |
| F18 | T3   | Closed 2026-05-19 | Multi-GC Screen 2 schema adds — `proposals.rfp_number`, `proposals.bid_due_date`, `proposals.billing_terms_override` | MultiGCWizard OPEN 1-3, 2026-05-18 | Migration `20260519200000_multi_gc_step9_wiring.sql`. |
| F19 | T3   | Closed 2026-05-19 | Multi-GC clone RPC intro_override param + locally_edited_fields['intro']               | MultiGCWizard OPEN 4, 2026-05-18 | Same migration as F18. Clone RPC now reads `intro_override` from targets, writes to sister `intro` + sets `locally_edited_fields = ARRAY['intro']` when non-null. |
| F20 | T3   | Open   | Multi-GC GC picker — Commercial-only default + CO call_log gate                        | MultiGCWizard OPEN 5-6, 2026-05-18 | **AUDIT 2026-06-10 — half shipped, narrowed to CO-gate only:** the Commercial-only-default filter IS already implemented (`MultiGCWizard.jsx:129` `c.customer_type !== "Commercial"`, with Residential toggle at `:316`). **Remaining work = the CO gate only:** the "+ Send to Additional GCs" trigger (`ProposalDetail.jsx:736-741`) does NOT check `is_change_order`, so it still shows on CO rows — hide it when `call_log.is_change_order`. |
| F21 | T3   | Closed 2026-06-10 | Multi-GC Screen 1 — selected GCs disappear when search filters them out                | Step 8 preview walkthrough 2026-05-18 | **Closed 2026-06-10 (audit — no longer reproduces).** Selection is keyed by `customer_id` in `targets[]` state, decoupled from the search-filtered list (`MultiGCWizard.jsx:135` toggle, `:388-406` Screen-2 tab bar renders from `targets`), so selections survive search-narrowing. The data-loss bug is gone. NOTE: there is still no Screen-1 *chip-bar/pinned-row visual indicator* — if that specific visual affordance is wanted, re-file as a small new polish row; the disappearance bug itself is fixed. |
| F22 | T3   | Open   | Multi-GC Screen 2 — no inline "Add new primary contact" affordance                     | Step 8 preview walkthrough 2026-05-18 | When a GC customer has zero contacts, the signer dropdown is empty with no way to create one in-flow. §7 line ~1625 calls for inline contact creation. Polish for step 9. |
| F23 | T3   | Open   | Multi-GC Screen 1 — search scope too narrow                                            | Step 8 preview walkthrough 2026-05-18 | GC picker search limited beyond Commercial-only default; needs broader scope or configurable filter. Polish for step 9. |
| F24 | T3   | Open   | Multi-GC Wizard Screen 2 — required-field affordance                                   | §10 step 11 smoke 2026-05-19 | Target/contact/RFP/bid-due fields don't visually signal they're required. Users discover only when the → nav circle stays disabled. Add red asterisk + "Required" placeholder + border treatment on `primary_contact_id`, `rfp_number`, `bid_due_date` per target tab. ~1h. |
| F25 | T3   | Open   | Multi-GC Wizard Screen 3 — markup adjuster UX is confusing                              | §10 step 11 smoke 2026-05-19 | Current `markup_override_pct` reads as a single percentage delta — not clear if positive adds or deducts. Better shape: explicit Add/Deduct toggle + Percentage/Amount toggle + value input. Wraps existing additive percentage math (`calcWtcPrice` override param); amount path needs a new calc helper. ~half-day. |
| F26 | T3   | Open   | Multi-GC Wizard — no success feedback on Create + Screen 4 button label                | §10 step 11 smoke 2026-05-19 | Successful clone closes the wizard silently. User can't tell if create succeeded without checking Proposals list. Add: (a) success screen inside wizard with "View N GC copies" CTA, (b) close + toast on parent showing "Created N GC Copies", or (c) navigate parent to `/proposals` filtered to the job. Also: Screen 4 button currently reads "+ CREATE 1 SISTER PROPOSAL" — rename to "+ CREATE N GC COPY/COPIES" to match badge terminology. ~1h. |
| F27 | T2   | Open   | Customer detail page doesn't surface GC Copy jobs/proposals                             | §10 step 11 smoke 2026-05-19 | Jobs + Proposals tabs join via `call_log.customer_id` (primary GC). Sister-side customers (`proposals.customer_id` where `cloned_from_proposal_id IS NOT NULL`) are invisible. Fix: UNION the primary-customer join with a sister-side query on `proposals.customer_id`. Apply to JOBS, PROPOSALS tabs, and Customers list job-count pills. ~half-day. |
| F28 | T2   | Open   | [Lev 9 · Eff half-day] Move proposal/invoice status flip server-side + add silent-failure UI guard | B29 post-mortem 2026-05-20 | **Architectural fix for the entire silent-failure class.** Today's B29 was invisible because `ProposalPDFModal.jsx:73-83` flips `status='Sent'` + `sent_at` BEFORE invoking the `send-proposal` edge fn — when the fn 500'd, DB still showed Sent and the UI lied. **AUDIT 2026-06-10 — re-scoped:** the "same shape in `send-invoice`/`send-pay-app`" claim is **no longer true** — both invoice paths flip status in the `onSent` SUCCESS callback (post-2xx, e.g. `Invoices.jsx:1846-1847`), so they're already safe. Only the **proposal** path (`ProposalPDFModal.jsx:75-83`) still flips pre-send. Part (a) [server-side move] now targets the **proposal path ONLY**; the invoice/pay-app paths need only part (b) [defensive banner]. Fix in two parts: (a) move the proposal status flip INSIDE `send-proposal`, after Resend confirms 2xx; H5 ordering rationale ("refresh signing_token_expires_at BEFORE Resend fires so the link can't be DOA") needs to be preserved by writing token expiry separately at the head and status/sent_at at the tail. (b) Add a defensive banner on `ProposalDetail` + `InvoiceDetail`: when `status='Sent' AND NOT EXISTS (select 1 from proposal_recipients where proposal_id = p.id)` → render "Send may have failed — verify and re-send." Backstop for any future silent failure of any cause. ~Half-day for the proposal path; banner is additive. |
| F29 | T2   | Needs re-scope | [Lev 7 · Eff 1h] Post-deploy smoke ritual for money-bearing edge fns                    | B29 post-mortem 2026-05-20 | **STALE SPEC (flagged 2026-06-10):** `send-proposal` was refactored (H11 trust-boundary fix) into an **email-only** fn. Assertions (3) `proposal_recipients` insert and (4) `call_log.stage`→"Has Bid" are now **client-side** (`ProposalPDFModal.jsx:125` + `:129`), AFTER the fn returns — the fn no longer does them, so a script asserting them against the deployed fn always fails. Also: fn rejects service-role callers (403), so the script must sign in a TEST user (email+pw via anon key) for a real JWT → needs `TEST_USER_EMAIL`/`TEST_USER_PASSWORD` secrets. **Re-scope before building:** smoke = sign-in test user → invoke against real TEST proposal/recipient → assert (2) 200 + (5) clean error log only. The recipients/stage coverage belongs to a client/E2E test (file separately). ORIGINAL SPEC: **Catches deploy-time regressions before users do.** Create `scripts/smoke-edge-fn.sh send-proposal <test-proposal-id>` that (1) invokes the deployed fn against a known TEST customer, (2) asserts fn returns 200, (3) asserts `proposal_recipients` row appears within 5s, (4) asserts `call_log.stage` flips to "Has Bid", (5) checks fn error log for stack traces in the last 60s. Wire into `npm run db:push`-style wrapper or document as a step in CLAUDE.md right next to `--no-verify-jwt`. Roll out: `send-proposal` first (most-recent burn), then `send-invoice`, `send-pay-app`, `proposal-signed`, `qb-sync-invoice`, `qb-record-payment`. ~1h for the first script + send-proposal wiring; 15min per additional fn to pattern-match. Pairs with `feedback_edge_fn_post_deploy_smoke.md` global rule (saved 2026-05-20). |
| F40 | T2   | Open   | [Lev 6 · Eff half-day] Post-send lock integrity + re-send round-trip (pull-back / re-send / orphaned `job_wtcs`) | Loop #40 Budget-tab code review 2026-07-06 | **Surfaced by the Budget-tab review, NOT caused by it.** The frozen bid stamped onto `job_wtcs.bid_breakdown` at Send-to-Schedule can drift because the post-send unlock path isn't guarded end-to-end. Intended invariant (Chris): WTCs lock at approval; a sent proposal can't be pulled back or its WTCs edited until the Schedule job is deleted — exactly the invoice guard. **Three seams must move together — fix all or none (half-fixing yields a dead-ended workflow):** (1) **pull-back guard** — `ProposalDetail.handlePullBack` (`:526`) blocks on live invoices but has NO check for an existing Schedule job, so pull-back unlocks WTCs (`:538`) while a sent job's snapshot still points at them → drift. Add a job-exists guard mirroring the invoice check, filtering out soft-deleted jobs (`jobs.deleted <> 'Yes'`) so it releases once Schedule soft-deletes (sch-command `JobCardList.jsx:89`). (2) **re-send guard** — `handleSendToSchedule` (`:561`) blocks re-send on ANY `source_proposal_id` job incl. soft-deleted, so after a Schedule delete the reopened pull-back path dead-ends at "already sent"; apply the same `deleted <> 'Yes'` filter. (3) **orphaned `job_wtcs`** — a soft-deleted job leaves its `job_wtcs` children; the send upsert `onConflict: proposal_wtc_id, ignoreDuplicates` (`:657`) then SKIPS them on re-send, so the new job comes back with NO SOW and NO Budget. Re-send must clear/repoint the prior job's `job_wtcs` (or the Schedule delete must cascade them). **Low today:** test-tenant data, round-trip not in active use. Do it right as one scoped change, not a Budget-loop bolt-on. |
| F41 | T3   | Open   | [Lev 4 · Eff half-day] Per-job "Bill to the exact penny" switch (replaces the display-only `show_cents` checkbox) | Deferred 2026-07-28 during the proposal-PDF rounding fix | **Job-level only — a customer-level default was considered and rejected** (exact change is rare and not a standing trait of any GC; the trend is toward rounding). Today `call_log.show_cents` ("Show cents on proposals & invoices (legacy jobs)", `CallLogDetail.jsx:596`) reads like it controls money and does not — it is display-only, while the actual rounding is decided by the pricing-era window in `calc.js` (`usesExactPricing`). That gap is what let the proposal PDF and the invoice disagree. **When built:** (1) reuse `proposals.pricing_anchor_at` — anchoring a proposal inside the closed exact-penny window (6/26–7/29) opts it into penny pricing with NO new column and NO new SELECT-coverage sweep; (2) the mode MUST freeze at approval/signing so toggling the job checkbox can never move money on work already under contract; (3) relabel or absorb `show_cents` at the same time so one control means one thing. Not urgent — round-UP is the house default and the PDF now prints the number it bills, so the only gap is the rare exact-change customer, handled meanwhile by a one-row `pricing_anchor_at` UPDATE. See `docs/plans/exact_penny_pricing.md` "Amendment 2026-07-28 → Deferred (not built)". |

### Invoice updates

| ID  | Tier | Status | Item                                                          | Source                          | Notes                                                                                                                                                                                                                                                                                                                                                                                                                       |
|-----|------|--------|--------------------------------------------------------------|---------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| F35 | T3   | Open   | Remove the "Waiting for Payment" status/button — it's useless | Loop #37 (undo-paid) 2026-06-22 | [Lev 2 · Eff 1h] "Waiting for Payment" is functionally identical to "Sent" — verified in Loop #37 recon: both treated the same for `pending` totals, aging, and past-due, same action buttons, no email/webhook/timestamp fires. Only the label color differs and the transition is a manual no-op (`Invoices.jsx:1112-1129`; status actions `:1193-1198`). It adds a confusing zero-value click. Remove the "Mark Waiting for Payment" button so the lifecycle is New → Sent → Past Due → Paid. Check nothing filters on the literal `'Waiting for Payment'` before dropping (the `pending` filter at `:1990` includes it). |
| F36 | T3   | Open   | "Undo Paid" affordance — multi-click confirm + role-gated     | Loop #37 (undo-paid) 2026-06-22 | [Lev 6 · Eff half-day] No in-app way to reverse an accidental "Mark as Paid"; today it needs hand-written SQL (Loop #37 reverted invoice 10015 Paid→Sent + cleared `paid_at` via guarded prod UPDATE). Add a guarded action: multi-click confirm + **role-gated to Admin/Manager** (money surface — see `feedback_role_gating`). Must set `status`→prior + `paid_at`→null; if `qb_payment_id` is set, delete/void that QB payment first (no reverse of `qb-record-payment` exists yet — build it); if `stripe_payment_id` is set, **block** (real payment, undo not allowed). Mirror the QB-aware void-modal routing at `Invoices.jsx:1393`. Could bump to T2 if mis-clicks recur. |
| F37 | T3   | Open   | Stripe Payment Links are reusable by default — double-charge window relies on webhook deactivation | invoice_recipients plan T2 audit (ADJACENT) 2026-06-25 | [Sev 6 · Like 2 · Eff half-day] **Pre-existing, surfaced (not caused) by the invoice-recipients work.** `send-invoice` mints a Stripe Payment Link that stays live until explicitly deactivated; the only thing closing the double-pay window is `stripe-webhook` deactivating it on `paid` (and `deactivate-payment-link` on void/pullback). If the webhook is delayed/missed, a payer holding the link could pay twice. Relates to F30 / the invoice-recipients feature (`docs/plans/invoice_recipients.md` §6 Adjacent). Options: set a Payment Link `restrictions[completed_sessions][limit]=1` at mint, or verify webhook deactivation latency/retry. Confirm current behavior before changing. |
| F38 | T4   | Open   | Per-recipient invoice `viewed_at` tracking (needs per-recipient tokens) | invoice_recipients plan T2 audit (ADJACENT) 2026-06-25 | [Lev 2 · Eff half-day] `invoice_recipients.viewed_at` ships as a parity column but is **not wired** — the public invoice page is reached via one `viewing_token` per invoice (shared by all recipients), so a page open can't be attributed to a specific recipient. Deferred in `docs/plans/invoice_recipients.md` §6. Only becomes feasible if per-recipient viewing tokens land (the declined "Option B" from A4) — **blocked on that.** Low value; file-and-forget unless per-recipient tokens are built for another reason. |

### Refactor

| ID  | Tier | Status | Item                                                                          | Source           | Notes                                                                                                |
|-----|------|--------|-------------------------------------------------------------------------------|------------------|------------------------------------------------------------------------------------------------------|
| R1  | T3   | Open   | Extract Invoices.jsx (~2,150 lines)                                           | v93 carry        | **AUDIT 2026-06-10:** file is now **2,152 lines** (was ~1,200) — nearly doubled after the F6 retention-release feature landed inside it (`:56`, `:229-268`, `:500-509`). Hosting Retention list view + retention header cards on top of the original invoice CRUD. Extraction pressure is higher; pull forward when the next feature touch here gets painful. |
| R2  | T3   | Open   | Extract WTCCalculator.jsx (~2,400 lines)                                      | v93 carry        | **AUDIT 2026-06-10:** file is now **2,402 lines** (was ~2,100) and lives at `src/pages/WTCCalculator.jsx` (imported by `ProposalDetail.jsx:9`), not as a component. Same rule: only when feature work here gets painful. |
| R3  | T3   | Open   | RLS simplification — use `tenant_id` directly instead of JOIN-based scoping   | v93 carry        | Mass policy rewrite; defer until F7 forces it.                                                        |
| R7  | T4   | Open   | [Lev 2 · Eff 1h] Subcon Home code-review hardening (6 low/trivial, non-blocking) | /code-review of feat/subcon-command-home, 2026-09-06 | **Repo: sales-command.** From the T5 code-review of the executive-home build (0 ship-blockers; 2 HIGH finder flags refuted as false-positives against canonical code). Six optional items: (1) `billRes` truthiness doesn't guard a partial/error-shaped `loadBillingSurfaceData` return — identical to canonical `built = surface ? … : null`, not introduced here; (2) array-index `key={i}` on the Needs-Attention + What's-Happening lists (static per-render, no reordered stateful children); (3) activity dedup-by-text can drop two distinct invoices when both have null `job_name` (rare cosmetic feed omission — dedup on text+timestamp to fix); (4) 60s sessionStorage cache can show stale numbers on fast re-nav (by design — the cache is the point); (5) `snap.error == null` throws a generic message on the degrade path (allSettled catches it → source flips to skeleton); (6) "Past due 30+ days" label vs strict `< -30` (i.e. ≥31d) — label imprecision, not wrong data. All Low/Trivial hardening; none gate ship. |
| R4  | T3   | Open   | Extract `pickBillingContact` to `src/lib/contacts.js` + `supabase/functions/_shared` twin | Loop #28 audit 2026-05-28 | Currently lives only in `ContactBillingPicker.jsx:23-29`. Edge fns that need to resolve Billing Contact (`send-invoice`, `send-pay-app`, `qb-create-job`, `qb-record-payment`) reimplement the lookup inconsistently — some role-only, some `is_billing_contact`-only, some neither (fall back to `customers.billing_email`). Single shared helper closes the asymmetry permanently. Includes: (1) `src/lib/contacts.js` for client, (2) `supabase/functions/_shared/contacts.ts` for edge fns, (3) sweep edge fns to call the helper. Pairs with F31. ~1h. |
| R5  | T4   | Open   | Screen-refresh code-review hardening (2 low-sev) | /code-review of feat/screen-refresh-dash-retire, 2026-08-22 | **Repo: sales-command.** Two low-severity items filed from the code-review (0 ship-blockers; the 2 should-fix — sold-dedupe order + OWED_STAGES dup — were fixed on-branch). Remaining: **(a)** `dedupeBids` (`followUp.js`) keys on `call_log_id::customer_id ?? ""`, so two sister bids that BOTH have a null `customer_id` collapse into one → undercount. Sisters carry distinct `customer_id` by design; null is an anomaly, so ~0 rows today — harden the key (e.g. fall back to proposal_id when customer_id is null) when convenient. **(b)** `markRecipientReceived` (`ProposalDetail.jsx`) writes `viewed_at` with no error surfacing — a silent no-op if RLS blocks the update (only the UI badge fails to flip; no data corruption). Matches the sibling `toggleSigner` pattern; add `.select()` + surface on empty/error if that pattern gets hardened generally. |
| R6  | T4   | Open   | Lock-at-sold code-review hardening (3 low/trivial) | /code-review of feat/lock-at-sold, 2026-08-24 | **Repo: sales-command.** Filed from the T5 code-review of the lock-at-sold enforcement build (0 ship-blockers; the one sub-Tier-2 nit — `saveSowOnly` silent-success — was fixed on-branch, commit `83024ce`). Three items the limiter passed as file-don't-fix: **(a)** the unlock guard (fresh status fetch + committed block + billing-schedule confirm) is duplicated at `WTCCalculator.jsx` `handleLock` and `ProposalDetail.jsx` `toggleWtcLock` — extract a shared `guardUnlock(supabase, proposalId)` helper (`src/lib/`) both call. DRY; identical + correct today, so no behavior risk. **(b)** On a committed proposal the SummaryTab "Save" button routes to `handleSave`, which early-returns silently — no "✓ Saved", no feedback (SOW-tab save is the intended committed path). Give it a no-op affordance: hide/disable the Summary Save when committed, or a "pricing locked" toast. Hardening only, no data risk. **(c)** Backfill `backfill_relock_committed_wtcs.mjs` Sent-total loop refetches all WTCs per proposal (N+1) — batch it. Trivial; INFRA, one-shot at the deploy gate. All low/trivial, no data risk. **(d)** Deploy-time deviation 2026-08-24: the §4.3 backfill was run as a direct SQL `UPDATE proposal_wtc SET locked=true` (22 rows: 7 Sent / 1 Signed / 14 Sold) via the Supabase SQL editor — the node script's login path was blocked, so only the safety-critical `locked` flag was set. **NOT written:** `locked_line_total` snapshots (stay null, same as pre-ship — no regression) and the Sent-only `proposals.total` recompute. Optional follow-up: run `scripts/backfill_relock_committed_wtcs.mjs` (or a SQL equivalent) to populate the H6 signing-page price snapshots on those 22 rows. Cosmetic, not blocking. |

### Cleanup / Ops

| ID  | Tier | Status | Item                                                                                            | Source     | Notes                                                                                                                            |
|-----|------|--------|-------------------------------------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------------------------------------|
| O11 | T2   | Closed 2026-08-07 (merge `9c396d8`, command-suite-db main) | [Lev 7 · Eff ~2h] Migration rehearsal reports "Safe to push" against a drifted baseline — the gate in front of EVERY migration | Split 2026-08-07 out of F44 (T&M round-2 audit) | **Repo: command-suite-db.** `scripts/rehearse.sh` fingerprints the **loaded baseline**, not prod, and the baseline's `EXPECT_TABLES=49` matches its own 49 tables — so the drift (prod has ≥52: `job_assets`, `ar_week_decisions`, `ar_job_billing` all post-date the baseline) cancels out and the run prints **green**. Only `--against-prod` sees it, and that is a read-only drift check, not the refresh. Two consequences: (1) a rehearsal that passes proves less than it appears to; (2) post-candidate re-runs check only `check_anon_exposure`, so a `CREATE OR REPLACE` that silently drops `SECURITY DEFINER`, `SET search_path`, a WHERE guard, `ORDER BY`, or a nested key applies clean, moves zero fingerprint counts, and **passes vacuously**. **Scope:** refresh the baseline `--against-prod` and re-derive `EXPECT_*` (carries a mandatory hand-edit — `REVOKE SELECT ON invoices FROM anon` — that `db dump` cannot emit); add a behavioural check for function replacements (seed a fixture proposal + token, assert a `pg_get_functiondef` diff plus `SET ROLE anon` calls incl. the expired-token and zero-WTC cases), wired into `npm run db:push` the way `from_scratch_powersync.sh` is. **Why it is its own item:** this guards every migration the suite pushes, not just the T&M work it was found under. **Not caused by any one plan.** — **CLOSED 2026-08-07.** Baseline re-dumped from prod (drift was 49->51 tables, incl. `materials` DROPPED in prod but still in the 07-31 copy); `EXPECT_*` re-measured; hand-edited REVOKE re-applied. `--against-prod` now runs on EVERY rehearsal (14d age gate as the offline fallback), and a permanent canary (`scripts/rehearse/canary/`) proves the gate can still fail on every run — two must-fail migrations plus a must-pass control, so a red canary from a broken container is distinguishable from a working gate. Proven by running: the same broken migration returns "Safe to push" on the old baseline and exit 1 on the new one. Six live migrations re-rehearsed from scratch — all clean, nothing already shipped was unverified. |
| O1  | T3   | Open   | Delete SC Staging Supabase project (`zaeevatlpkrcmivhhrph`)                                     | v91        | Served its purpose; clutter on dashboard.                                                                                        |
| O2  | T3   | Open   | HDSP bookkeeper journal entry — move $41,734.78 from "AR Retention" to Construction in Progress-Retent (or Retention Receivable), then deactivate AR Retention | v93        | Not a code task. Three retention accounts exist; bookkeeper context unclear (likely intentional split between in-progress vs ready-to-release). |
| O3  | T2   | Closed 2026-05-20 | H5 Migration B — drop `mark_proposal_signed(text)` 1-arg compat wrapper + `proposal_signatures_public_insert_token` policy | Filed 2026-05-10 (H5 build) | **Blocked by: 48h timer + pg_stat_statements verification.** H5 Migration A deployed to prod 2026-05-11 02:09 UTC; Vercel + edge fn live. The 1-arg `mark_proposal_signed` compat wrapper (delegates to 5-arg with NULL signer fields) AND the legacy `proposal_signatures_public_insert_token` policy remain in place so customer browsers with cached old JS continue to sign successfully through the rollout window. Earliest start: 2026-05-13 02:10 UTC. Before applying Migration B: query `pg_stat_statements` (or Supabase function logs) to confirm zero traffic to the 1-arg form since 2026-05-11 02:09. If non-zero, defer further until cache rotation completes. Migration B itself: drop the 1-arg form + the policy, run `NOTIFY pgrst, 'reload schema'`, smoke a signing flow end-to-end. Closes H5 fully. ~15 min when picked up. |
| O4  | T3   | Open   | Create canonical `docs/runbooks/rls-deploy-gates.md` (6-gate deploy runbook)                    | Audit 2026-05-11 | `CLAUDE_RLS.md:65-66` references this file; it doesn't exist. Canonical 6-gate content currently buried in `docs/handoffs/SC_Handoff_v83.txt:64-89` (origin postmortem of 2026-04-26 incident). Extract → standalone runbook so planning agents and future audits have a non-vapor reference. Inlined version in `~/sch-command/docs/planning/JOBS_IA_REFACTOR.md:152-168` is a usable source. ~20 min. |
| O6  | T2   | Resolved 2026-07-14 (→ MIG-1) | Migration history not replayable from zero — two 2026-04-16 migrations in dependency-violating timestamp order | Found 2026-05-12 (Migration 1a scratch bootstrap) | **ORPHANED then RE-HOMED.** This item stayed in sales-command's backlog when the migrations moved to `command-suite-db` (2026-06-29 consolidation); it was independently rediscovered there 2026-07-14 as **MIG-1** (same failing pair). Ownership now lives with the migrations: `command-suite-db/docs/BACKLOG.md` § MIG-1 + `docs/plans/mig1_rehearsal_path.md`. A credential-free rehearsal harness (`scripts/rehearse.sh`) shipped 2026-07-14, closing the "unrehearsed to prod" danger; the from-zero replay / baseline-squash stays open under MIG-1. Closing this SC copy to kill the duplicate. _Original detail:_ `supabase db push` against a fresh scratch fails on the first ALTER because `20260416132135_billing_schedule_multi_docs.sql` (ALTER billing_schedule) is timestamped BEFORE `20260416175646_billing_schedule_and_archive_links.sql` (CREATE billing_schedule). Prod works because the history was applied/repaired non-chronologically at the time. Impact: every future scratch project (audit smoke, debugging, onboarding a new dev machine) requires dump-and-restore workaround instead of clean `db push` from zero. **Blocks: future scratch-based smoke workflows.** Fix candidates: (a) rename the ALTER migration to a timestamp AFTER the CREATE (e.g., `20260416180000_billing_schedule_multi_docs.sql`), then `supabase migration repair` on prod to point the renamed timestamp at the already-applied state; (b) consolidate both migrations into a single forward-only migration replacing both, and mark old ones repaired. (a) is lower-risk — pure timestamp rename + ledger repair, no DDL change. Either way: must apply repair to prod before the renamed migration goes live or `db push` from anywhere will diverge. ~half-day with careful prod ledger work. |
| O7  | T1   | Resolved 2026-06-29 | Multi-repo migration coordination — shared Supabase project, no timestamp coordination | Found 2026-05-12 (Migration 1a collision with sch-command planning) | **RESOLVED 2026-06-29 — migrations consolidated into the dedicated `command-suite-db` repo (single source of truth = the single shared ledger); app repos no longer own or push migrations, so cross-repo timestamp coordination is structurally gone. Supersedes the remaining Phase-4 AR-wrapper work (AR doesn't push migrations either). Plan + 4-round audit: `docs/plans/shared_migrations_consolidation.md`.** _Original:_ **Phase 1 + Phase 1.5 shipped 2026-05-18** to SC + SCH + FC (merged to main, all 3 repos): `npm run db:push` runs `scripts/check-migration-collision.mjs` (shape-validate `/^\d{14}_.+\.sql$/` then slug-extraction `[15:-4]`, per CLI source review of `apps/cli-go/pkg/migration/file.go::NewMigrationFromFile`) before `supabase db push`. SC real-push acceptance green (47/47 ledger match, 0 collisions). SCH dry-run green, real-push gated on O8 ledger repair (`supabase migration repair --status applied 20260512120000 20260512120100`). FC dry-run green (empty migrations dir no-op). Phase 1.5 canonical-reference pointers to `docs/plans/o7_migration_coordination.md` added in SCH + FC `CLAUDE.md`; AR skipped (no `CLAUDE.md`). Audit ratification: 12 Accept / 2 Partial / 0 Reject (partials addressed pre-merge). Plan doc: `docs/plans/o7_migration_coordination.md`. Loops #12 (plan) + #13 (build) closed in `~/erd-loop/LOG.md`. **Remaining:** Phase 4 — AR-Command-Center wrapper install (gated on AR's first `supabase/` bootstrap). Hardening: see O9 (nightly cross-repo ledger drift-check cron). |
| O8  | T2   | Resolved 2026-05-12 | Two ledger rows reserved via repair without local DDL existing                                  | Found 2026-05-12 (Migration 1a scratch bootstrap) | `supabase_migrations.schema_migrations` on prod has two rows with `has_statements=false`: `20260512120000_jobs_material_status_additive` and `20260512120100_job_wtcs_create`. Neither corresponds to a file in `~/sch-command/supabase/migrations/` (only `20260503190000_daily_log_update_policy.sql` is there). Origin: probably a sch-command session pre-reserved the timestamps via `supabase migration repair --status applied` during Jobs IA planning on 2026-05-11. **Risk:** when sch-command eventually writes the actual DDL for these migrations, `supabase db push` will see them in the ledger and skip — the DDL never runs. Resolutions: (a) delete these ledger rows so the eventual `db push` applies cleanly, or (b) confirm the rows are intentional placeholders with a documented unblock plan. Cross-repo coordination with whoever owns sch-command's Jobs IA implementation. **Blocks:** Jobs IA implementation phase (if sch-command tries to push the actual migrations, they'll silently no-op). ~15 min to delete the rows once intent is confirmed; longer if there's a planning doc that needs to be reconciled. **Resolution: reverted via `supabase migration repair --status reverted 20260512120000 20260512120100` during Migration 1a prod apply 2026-05-12.** Justification: both rows had `has_statements=false`, no local files existed in `~/sch-command/supabase/migrations/` AT TIME OF CHECK, the reservation purpose was moot. POST-REVERT DISCOVERY: sch-command's actual Jobs IA DDL had been applied to prod via dashboard/db-query bypass; their git remote 2a286e9 now carries migration files for those timestamps that need ledger reconciliation before their next `db push`. Cross-repo breadcrumb dropped in sch-command's latest handoff. If sch-command needs the placeholders re-asserted, `supabase migration repair --status applied <version>` re-creates them. |
| O9  | T3   | Closed 2026-06-29 (obsolete) | Schedule cron — nightly cross-repo ledger drift check + alert (O7 hardening)                    | Filed 2026-05-18 (O7 audit-side gap analysis) | **CLOSED 2026-06-29 — obsoleted by the O7 consolidation. With one migrations home (`command-suite-db`) and one ledger, the cross-repo drift class this cron was meant to detect can no longer occur; there is nothing to diff across repos. No cron needed.** _Original:_ O7 wrapper catches collisions on `npm run db:push` in SC+SCH+FC but does NOT cover bypass paths: raw `supabase db push`, dashboard SQL editor DDL, `--skip-collision-check` override, or AR-Command-Center (no `supabase/` yet). A nightly cron that queries the prod ledger + diffs against each repo's `supabase/migrations/` files would surface drift before it bites. Output: alert on any version-only or version+slug mismatch. Implementation candidates: (a) GitHub Action cron in sales-command checking out siblings via deploy keys; (b) standalone Mac mini cron. (a) easier (no machine dependency). Pushes happy-path coverage ~70% → ~95% by surfacing what the wrapper can't enforce. Plan reference: `docs/plans/o7_migration_coordination.md`. **Blocked by:** O7 Phase 4 (AR-Command-Center `supabase/` bootstrap) — otherwise AR is excluded from the check. ~half-day once unblocked. |
| O10 | T2   | Resolved 2026-06-29 | `check-migration-safety.sh` Check B + bare `supabase db push` BLOCK legitimate SC migrations on sibling-repo ledger entries | Found 2026-06-01 (Loop #30 retention-release migration push) | **RESOLVED 2026-06-29 by the O7 consolidation. SC no longer pushes migrations, so Check B's sibling-ledger false-positive can't fire here. All pushes now run from `command-suite-db`, whose `supabase/migrations/` is a verified bijection with the ledger (0 strays) — Check B passes cleanly there. The `db query -f` + `repair --status applied` workaround is retired.** _Original:_ **Related: O7, O8, O9.** Now that sch-command has applied migrations to the shared prod ledger, every NEW sales-command migration is blocked from the normal push path: (1) `scripts/check-migration-safety.sh` Check B does `git log --all` only within the CURRENT repo (`:74`), so the 5 sch-command ledger entries (`20260503190000`, `20260512120000`, `20260512120100`, `20260528120000`, `20260528133000`) match no file on any SC branch → it exits 1 and wrongly suggests `repair --status reverted` (the 2026-05-18-incident footgun); (2) `npm run db:push` chains `safety && collision && push`, so it halts at (1); (3) bare `supabase db push` ALSO refuses ("Remote migration versions not found in local migrations directory") and suggests the same dangerous reverted-repair. **Workaround used Loop #30 (worked, clean):** apply the additive DDL via the Supabase SQL editor, then `supabase migration repair --status applied <ts>` to record it; collision check then shows ledger aligned. **Fix candidate:** teach Check B to also scan sibling Command Suite repos (`~/sch-command`, `~/field-command`, `~/AR-Command-Center`) for `${remote_ts}*` before flagging an entry as divergent — mirrors the all-5-found verification done by hand this loop. Does not fix the bare-CLI refusal (that's a supabase CLI behavior), but unblocks `npm run db:push` and removes the false reverted-repair prompt. ~1–2h. |
| B54 | T1   | Closed 2026-08-05 | [Sev 7 · Like 10 · Eff 1h] Internal Approve notified nobody — sold jobs never reached the sales rep | Found 2026-08-05 (Chris: "sales is not getting any notification") | **Closed 2026-08-05 on `fix/sales-notifications`.** `ProposalDetail.handleInternalApprove` flipped status, stamped `approved_by`/`approval_reason`, and kicked `qb-create-job` — with no email anywhere on the path. Only a customer e-signature (`proposal-signed`) ever mailed the rep, and **21 of the last 25 prod approvals went through Internal Approve**, so nearly every sold job notified no one. **REVISED SAME DAY — notification moved into the DB.** First cut invoked `proposal-approved` from the browser. Then prod logs for a live signature at 23:04:47 showed the real defect: `.865` browser sends the preflight for `proposal-signed`, `.896` browser calls the `mark_proposal_signed` RPC **directly**, `.930` proposal is Sold with "Proposal Accepted" on screen — **the function's POST never happens**. The signing page's fallback commits the signature itself whenever the edge call doesn't land, so any email inside that function is skipped while the DB says Sold. No hardening inside the function can fix that; the function isn't running. Final shape: trigger `trg_notify_proposal_approved` on `proposals` fires on a transition INTO Sold/Signed (archive imports excluded), posts to `proposal-approved` (now x-cron-secret gated, DB-only caller), which reads the rep and the approval route from the row. Covers e-signature, the signing-page fallback, and Internal Approve with one mechanism. Migration `command-suite-db/20260805233000`. Verified in prod: `{"emailed":true,"to":"chris@hdspnv.com"}`. |
| B55 | T1   | Closed 2026-08-05 | [Sev 6 · Like 10 · Eff half-day] `call_log.follow_up` never sent anything — the wizard promised a reminder that did not exist | Found 2026-08-05 (Chris: follow-up must email the rep) | **Closed 2026-08-05 on `fix/sales-notifications`.** The New Inquiry wizard read "A 30-day reminder will be sent to the sales rep if this job's status hasn't changed" — no cron, no function, nothing read the column except the Call Log list's overdue colouring. Fix: nightly `follow-up-reminders` edge fn + `follow-up-reminders-daily` pg_cron job (14:00 UTC ≈ 07:00 Pacific), one digest per rep, driven by new `call_log.follow_up_reminded_for` so a missed night catches up and a pushed date re-arms. Migration `command-suite-db/20260805223000`. **Backfill decision: the 77 already-overdue follow-ups were stamped as reminded** so day one didn't dump months of stale items; the one-line UPDATE to release them is in the migration header. Wizard copy corrected to match reality. |
| B56 | T2   | Closed 2026-08-05 | [Sev 6 · Like 10 · Eff 1h] Orphan-user security alarm had failed on EVERY run since 2026-06-09 | Found 2026-08-05 (incidental — same cron plumbing as B55) | **Closed 2026-08-05 on `fix/sales-notifications`.** `check-orphan-users-daily` errored `unrecognized configuration parameter "app.settings.supabase_url"` on every single run — the custom `app.settings.*` GUCs it read were never set and *cannot* be on this managed project (same superuser block documented in migration `20260609120100`, which moved the cron secret to Vault but left url/service-key on the GUC mechanism as out of scope). The alarm has therefore never once run. Fix: rescheduled with a literal project URL and no service-role header, with `check-orphan-users` redeployed `--no-verify-jwt` — `20260609120100` already recorded that the platform JWT gate "admits ANY authenticated tenant user", so x-cron-secret was always the real gate and is now the only one asserted. Same migration as B55. |

### By-design (not bugs — kept here so we stop re-litigating)

- In-app invoice display ignores `show_cents` (intentional).

---

## How to use this file

1. **Every session starts by reading this file** (enforced via `CLAUDE.md`).
2. **Add new items** the moment you discover them — don't trust memory.
3. **Mark `In Progress` when starting** so a parallel session won't double-up.
4. **Move `Done` rows to the Completed Log** before closing the session.
5. **Each row's `Source` is non-negotiable** — it's how we trace why an item exists when context fades.

---

## Completed Log

Append rows as they finish — newest first. Once the log gets long, archive
older entries to a per-version handoff and trim here.

| Date       | ID  | Item                                                                                                                          | Where done         |
|------------|-----|-------------------------------------------------------------------------------------------------------------------------------|--------------------|
| 2026-09-16 | IDENT-3 | Crew Scheduler active person list consumes Team-linked `crew.archived`. Legacy unlinked rows stay visible. Existing assignments unchanged. | [PR #63](https://github.com/chris7berger-droid/subcon-command/pull/63), merge `d8c9eb7`; handoff v280 |
| 2026-09-16 | IDENT-2 | Team modal **Available on Crew Schedule** creates/links/unarchives or archives the linked `crew` row. No bulk backfill, no Chris link, no assignment identity writes. | [PR #62](https://github.com/chris7berger-droid/sales-command/pull/62), merge `922b520`; handoff v279 |
| 2026-09-13 | B123 | Crew Schedule weekday/date label restyle (capacity pills + stacked Job/Mo–Sa headings) and ~25% larger free/off badges. Chris accepted preview. | [PR #56](https://github.com/chris7berger-droid/sales-command/pull/56), merge `d12cfef`; handoff v276 |
| 2026-09-13 | B122 | Scrapped: weekly-text crew grid / drop With: line. Chris rejected. Do not rebuild. | [PR #55](https://github.com/chris7berger-droid/sales-command/pull/55) closed unmerged; handoff v275 |
| 2026-09-13 | B121 | Weekly crew texts omit empty days (no date, no “No work assigned”). Chris accepted preview. | [PR #54](https://github.com/chris7berger-droid/sales-command/pull/54), merge `ac0b74b`; handoff v274 |
| 2026-09-13 | B120 | Weekly crew texts use the Crew Schedule lead (trip lead if set, else job Lead). Confirm with office only when the board has no lead. Chris accepted preview. | [PR #53](https://github.com/chris7berger-droid/sales-command/pull/53), merge `48342cf`; handoff v273 |
| 2026-09-11 | B119 | Crew Schedule Job / Mo–Sa date bar stays put while job rows scroll. All Stages row removed; crew cards and week strip sit under Weekly Crew Capacity. Larger day/date labels on capacity cards and the board header. Chris accepted preview. No data writes. | [PR #51](https://github.com/chris7berger-droid/sales-command/pull/51), merge `ea41bb9`; handoff v268 |
| 2026-09-10 | — | Added CLAUDE.md Workflow Rule 9: inspect existing code before estimating, count remaining work and verification, define the endpoint, name uncertainties, and calibrate against observed agent elapsed time. | f342be8; user-requested session instruction; PR49 |
| 2026-09-08 | F61 | **CODEX — Done:** One weekly text per crew member with day/trip-specific jobs, addresses, leads and coworkers; exact preview/copy, previous/next person, and editable 6:30 AM shop default overridden by saved scheduler times. Approved by Chris, merged and verified live. B107/B108 printing and F60 Sunday scheduling UI remain deferred. | [PR #41](https://github.com/chris7berger-droid/sales-command/pull/41), merge `0a98826`; approved app `d1141b8`; handoff v243 |
| 2026-09-08 | B109 | **CODEX — Done:** Crew Schedule header follows the board week; trip-based Jobs Starting / Ending / Needing Crew, exact-trip links, historical requirement wording, and steady week loading. Chris accepted the final preview and authorized merge. Integrated released B110; production deployment verified. | [Codex PR #39](https://github.com/chris7berger-droid/sales-command/pull/39), merge `31069ca`; final app commit `1acc13c`; handoff v241 |
| 2026-09-08 | B110 | Crew Schedule preserves overlapping trips as separate rows with their own dates, staffing, editor and UUID-linked crew assignments. Sibling-safe removals; ambiguous legacy days visible once. User accepted preview; merged and production verified. B109 subsequently integrated this release and merged in PR #39. | [Codex] PR #40, merge `29af790`; handoff v238 |
| 2026-09-08 | B104 | **CODEX — Done:** Scheduling crew lead choices and cream modal controls. Accepted by Chris; merged and production verified. | [Codex PR #38](https://github.com/chris7berger-droid/sales-command/pull/38), merge `555019c`; implementation `a338f36`; handoff v235 |
| 2026-09-08 | B105 | **CODEX — Done:** Prevent allocation creation on hidden combined/deleted jobs; show saved allocations across Crew Schedule/Calendar/Daily. Earlier authorized job7215 data repair documented; do not rerun. Frontend save guard is not an atomic concurrent-Combine DB invariant. | Codex PR #38, merge `555019c`; implementation `5846bf3`; handoff v235 |
| 2026-09-08 | B106 | **CODEX — Done:** Date-specific staffing targets/leads, unknown vs zero, blank inheritance and valid crew-count creation. B107/B108 printing issues remain separately deferred. | Codex PR #38, merge `555019c`; implementations `0483c1d`, `4952f0b`; handoff v235 |
| 2026-09-08 | MOBZ-2 | **CODEX — Done:** Saved full-span trips plus conserved crew history; parent-date coverage; shared trip editing, inline Crew Schedule fields, trip titles in Schedule/Calendar, Open job, Build Schedule Create/Edit, and header/picker polish. Chris accepted preview and authorized merge; production verified. | Codex PR #38, merge `555019c`; final app code `97275ff`; handoff v235 supersedes v223-v234 preview status |
| 2026-09-01 | —   | Sent-invoice action button read "Send / Resend" even though a sent invoice can't be sent or resent (correcting it is a pull-back). Now reads **Send** while New and **Review/Open** once sent, so people aren't afraid to open it and see what was done. One-line label change in `Invoices.jsx`; pay-app "Preview" path unchanged. | main @ `b143ac6` |
| 2026-08-18 | —   | Deleting a pay-app **invoice** from the Invoices screen left the pay app row behind — the ghost still counted in New Pay App numbering (showed #2 on job 10019 after Pay App 1's invoice was deleted) and its `pay_app_lines` fed prior-billed math (Prior % locked at 100%, Gross This Billing $0). The 2026-05-05 cascade fix (`dcbee9f`) covered the **pay-app-side** delete only (PayAppDetailModal removes pay app + soft-deletes invoice); the invoice-side paths (`Invoices.jsx` `handleDelete` non-QB delete, `handleVoidConfirm` void+delete) never touched the pay app. Now both call `deleteLinkedPayApp()`: hard-delete the linked `billing_schedule_pay_apps` row (lines cascade via FK), unlock the schedule to `draft` when no pay apps remain (mirrors modal delete), alert instead of failing silently if the pay-app delete 0-rows (RLS), and `handleDelete` is gated on `loading` so a click before `linkedPayApp` resolves can't skip the cascade. Confirm text names the pay app. **Prod data repaired:** ghost Pay App 1 rows deleted on job 10019 (invoice 10162) and TEST job 10141 (invoice 10070), schedules reset to draft. Also on 10019: SOV Grind & Seal line synced 8,793 → 9,111 (+$318 travel added to the WTC after the Jul 6 SOV entry) and `contract_sum` 13,422 → 13,740, so Job Detail / pay app modal now match the proposal summary — note the proposal-summary vs Job-Detail "Contract" mismatch is by design (live WTC math vs SOV as canonical for pay-app jobs); the SOV was stale, there's no auto-sync (see B32 for the adjacent draft-pay-app snapshot drift). `vite build` green. | main (this commit) |
| 2026-08-11 | F45 | **Won't Do** — Revise-invoice-in-place (keep same number, edit + resend). Chris: "I'm not gonna edit invoices in place. I don't want to deal with that." Logged here so it isn't re-filed. The T&M delete-and-rebuild flow (Pull Back voids+deletes → recreate) covers the correction need; a same-number in-place edit is explicitly not wanted. | Decision only — no code |
| 2026-08-25 | F45↩ | **Reversed + shipped** (supersedes the 2026-08-11 "Won't Do" above). Chris re-requested exactly this for **regular** (non-pay-app, non-T&M) invoices: pull back #567, edit the amount + retention, resend, **keep #567** — grounded in QB behavior ("a voided invoice reopens when you type data into it"). Root cause: the synced Pull Back ran a **two-row void+replacement** in `handleVoidConfirm` (`Invoices.jsx`) — voided #567 in QB, marked the SC row `voided_at`, and **inserted a new invoice at the next free number** as the correction, so 567 could never be kept. Fix (frontend only): synced regular Pull Back is now a **same-number reissue** — void #567 in QB (unchanged), but keep the SAME SC row + its `qb_invoice_id` and reset it to an editable New draft (no `voided_at`, no new row). `syncedLock` split into `numberLock` (freezes the invoice # whenever a QB record exists) and `moneyLock` (`qb_invoice_id && !isNew` — freezes dollars only while the invoice is still live/Sent, so a pulled-back New invoice unlocks amount/retention). On resend, `qb-sync-invoice`'s `isUpdate` branch full-replaces #567's lines → **reopens the voided QB invoice, same DocNumber**. QB PrivateNote (stamped by `qb-void-invoice`) carries the audit trail; the separate SC "voided twin" row is no longer created for this case. **Pay-app + T&M pull-back paths unchanged.** New edit-mode banner explains the reopen. `vite build` green. **Preview-smoke against a QB test job before merge** — the QB-reopen-on-update behavior is the one thing that needs a live confirm. | branch `fix/invoice-same-number-reissue` (preview) |
| 2026-08-07 | —   | Invoices sent after 5pm Pacific were stamped with **tomorrow's** date and dropped out of the date filter — in Sales Command and in QuickBooks. Chris sent #10154 (job 10040) at 5:51pm and it vanished from a FROM 08/03 → TO 08/07 filter. `invoices.sent_at` is a Postgres **date** column, but every send path wrote `new Date().toISOString()` — a UTC instant, which Postgres casts to the UTC date. 5:51pm Pacific Aug 7 is `2026-08-08T00:51Z`, so the row stored 2026-08-08. Invisible for months because the display and filter paths disagreed on timezone: `fmtD()` appends `T00:00:00` **local** to a bare date string, so the row printed "Aug 8" — a day late but not obviously broken — while the filter compared the raw string. Proven in data before any edit: 10153/10154 stored Aug 8; 10151/10152, sent the same afternoon before 5pm, stored Aug 7. Fixed at the shared helper rather than the call sites — `utils.tod()` now returns the local wall-clock date (`toLocaleDateString("en-CA")`), which also corrects `over()` and Home's "due today" counts, both already wrong the same way; CallLog's hand-rolled duplicate of the same expression deleted in favor of the import. Two more defects shared the root cause: **aging read a day early** across the whole list (subtracting a local instant from a date string parsed at UTC midnight — 10150, due Aug 7, showed "1d overdue" on Aug 7), now via a new `dayDiff()`; and the **pay-app path** (`send-pay-app` edge fn, which runs in UTC, plus `NewPayAppModal`'s printed `invoice_date`) carried the same stamp. Column types were read from `information_schema` first: on `invoices` only `sent_at`/`due_date` are `date` — `paid_at`/`voided_at`/`deleted_at` are timestamptz and correctly keep `toISOString()`; `sent_at` is a date on `invoices` alone, so `proposals`/`proposal_recipients`/`invoice_recipients` never had this bug. `due_date` untouched (manual date-picker input, never through `toISOString()`). **Prod data repaired:** 10153 + 10154 reset to 2026-08-07 in Supabase, and — because `qb-sync-invoice:365` sets QB `TxnDate` from `sent_at` — the same two corrected in QuickBooks (148261, 148262), verified by re-reading QB. **Open:** invoices sent after 5pm on earlier days are still a day late and are **undetectable** — the DB stores only the date, not the send instant. `vite build` green; edge fn v19 verified by reading deployed source back. | branch `fix/invoice-sent-date-tz`, fast-forwarded to main @ `c648dc3`; `send-pay-app` redeployed; handoff v178 |
| 2026-08-06 | —   | Invoice header: the Invoice # / Job # / Due Date block drifted sideways with the date, the whole company block was blank, and JOB # printed the job name. **Four fixes, one screen.** (1) **Drift** — the block sat in a `space-between` flex row at auto width, so its width (driven by the longest value, usually the due date) pushed its left edge horizontally; a different date moved the whole block. Now the middle cell of a `1fr / 200px / 1fr` grid, anchored to the page centerline, growing downward only. The identical bug was fixed on the **proposal** PDF header 2026-07-27 (`15fdcc2`) and never mirrored here — third time a per-surface header fix hasn't been swept across surfaces. (2) **Blank company header** — no company name, tagline, phone, website, license, and the logo falling back to the bundled default. Data and RLS were both fine: `tenant_config`'s SELECT policy is `id = get_user_tenant_id()` for `authenticated` only, `Login.jsx:15` reads the company name on mount while still **anonymous**, and `config.js` cached `{...DEFAULTS, ...null}` at module level for the rest of the session — poisoning every later consumer (invoice preview, proposal PDF, WTC calculator). Refreshing straight onto an authenticated route never mounts Login, so the first read succeeds — which is why it looked intermittent. Now only a real row is cached, the cache clears on every auth transition, and `TenantConfigProvider` (which sits above the auth gate) re-reads on auth change. (3) **Mailing address** — company address/city/state/zip added to the top-right contact block on all three surfaces so customers know where to mail a check; needed a DB change for the customer-facing page (see Where done). (4) **JOB # printed the job name** — `invoices.job_id` stores the whole display string ("6897 - Plenium Builders Virginia Palmer Elementary - Polish"), so a field labeled JOB # wrapped to two ragged lines; now the leading segment only, matching the proposal PDF's convention. The generated PDF still prefers the customer's own `subcontractor_job_no` when set, so screen and PDF can differ on jobs that have one — closing that needs `job_number`/`subcontractor_job_no` exposed to anon, i.e. a migration plus an anon-exposure allowlist change, deliberately not taken for a cosmetic field. All three surfaces (preview modal, public page, generated PDF) kept in sync; PDF's Bill To column now wraps at a fixed width so it can't run under the centered block. `vite build` green. | branch `fix/invoice-header-anchor`, merged to main @ `ec2a758` + follow-up `2326f27`; migration `20260806140000_public_tenant_config_mailing_address.sql` in `command-suite-db` @ `93451df` (pushed to prod) |
| 2026-08-06 | —   | Raw Postgres errors no longer reach the screen, and Job Detail notices when its job is gone. Surfaced twice in one session: the job-delete FK failure, then `insert or update on table "proposals" violates foreign key constraint "proposals_call_log_id_fkey"` in the Archive Job Proposal modal — a tab left open on deleted job 3806 while 6653 had been re-imported as 3807, so a fully-typed archive proposal (sold amount, historical billed, description) failed and was lost. NEW `src/lib/dbErrors.js`: `friendlyDbError()` maps both FK directions by constraint name (missing-parent → "the job this proposal belongs to no longer exists…"; blocked-by-child → "an invoice still points at this record…"), plus 23505/23502/42501, from the real `pg_constraint` list — only NO ACTION / RESTRICT constraints appear in the blocked-by table since CASCADE and SET NULL can't surface. Returns **null** for anything unrecognized and `dbErrorText()` falls back to the raw message: a wrong-but-friendly sentence is worse than an ugly accurate one. Applied at the two proven sites (`CallLogDetail.handleDelete`, `ArchiveProposalModal.handleCreate`) — the other ~88 `alert(err.message)` sites across ~20 files were deliberately NOT swept; the helper is there when they're touched. Job Detail also re-checks its row on mount and on tab focus, and replaces itself with a "this job has been deleted" panel + Back button rather than redirecting on a timer (a redirect would yank the screen out from under half-typed work). Only a clean not-found evicts — a network or RLS error must never take down a live page. Scoped to Job Detail by decision; Proposals/Invoices/Customers keep prior behavior but now fail in English. No schema change, `vite build` green. | branch `fix/stale-job-page-and-db-error-messages`, merged to main @ `6d6a3a7` |
| 2026-08-06 | —   | Deleting a job failed with a raw Postgres FK error and left the job half-emptied. `CallLogDetail.handleDelete()` preflighted only active proposals, then ran its cleanup as loose statements: work types deleted → soft-deleted proposals detached → `call_log` delete. Soft-deleted **invoices** still hold `call_log_id`, and unlike proposals that column is NOT NULL, so they can't be detached — the delete died on `invoices_call_log_id_fkey` after the first two steps had already run. Job 6653 was left present but stripped (0 `job_work_types`, proposal `4469e0ff` orphaned) with `alert("Delete failed: update or delete on table \"call_log\" violates foreign key constraint…")` as the only explanation. Now: all blockers preflighted before any mutation (active proposals, active invoices — the latter named by invoice number); soft-deleted invoices are hard-deleted as part of the job delete, called out by number in the confirm since it's irreversible (`invoice_lines` / `invoice_recipients` / `invoice_attachments` all cascade, verified via `pg_constraint`); and a surviving FK error (Schedule/Field's `jobs`, `time_punches`, `daily_production_reports`, `daily_log_entries` all hold `call_log` with NO ACTION) renders as English naming the constraint instead of raw Postgres. Client-side only — a real fix is an atomic RPC, but that's a `command-suite-db` migration, deliberately not taken here. `vite build` green. | branch `fix/import-wizard-existing-customer-billing`, merged to main @ `a9dc3fd` |
| 2026-08-06 | —   | Pulling a History Locker job forward onto an **existing** customer silently discarded the whole Contact & Billing step. `ImportToLiveWizard.handleImport()` only persisted those fields inside the `customerMode === "new"` branch (`:401-437`) — there was no existing-customer counterpart, so billing contact, billing terms, and job contact phone/email never reached the DB. Found on job 6653 (Incline Property Management, imported 2026-08-06): 0 `customer_contacts` rows, `billing_name` null, so invoice creation had no billing contact to resolve. Added the `1b` branch mirroring `NewInquiryWizard.jsx:311-339` — `customers` update + `customer_contacts` Billing Contact insert, skipped when `billingSourceContactId` is set (picker locked from a saved row). Blank contact fields are not written, so an archive import can't wipe data already on the customer. Second bug, same step: `ContactBillingPicker`'s `onBillingChange` patch carries `billingSame: false` (its "billing contact same as job contact" flag), and this wizard spread it straight into form state where `billingSame` is the billing-**address** toggle — so typing a billing contact name flipped `billing_address_same` false and nulled the job's billing address (confirmed on 6653). `onBillingChange` now destructures `billingSame` out. Both `customer_contacts` inserts in this file now set `is_billing_contact: true` alongside `role` (F31 shape, "verify on touch"). No schema change, `vite build` green. | branch `fix/import-wizard-existing-customer-billing`, merged to main @ `a9dc3fd` |
| 2026-08-04 | —   | Work types are custom-only across the app. Settings listed all 84 `work_types` rows, including the 51 seeded system rows (`tenant_id IS NULL`) that can't hold a SOW — reps picking those is how the near-duplicate names ("Clean and Seal" vs "Clean & Seal", "Joint fill" vs "Joint Fill Caulking") got created. New `src/lib/workTypes.js` holds one rule, `selectableWorkTypes(all, keepIds)`: tenant-owned + `active !== false`, plus a `keepIds` escape hatch so anything already tagged on the record being edited stays listed and is never silently dropped on save. Applied to Settings, Job Detail (frozen at mount so unchecking doesn't make a row vanish), New Inquiry, WTC calculator, Archive Proposal, Import to Live; CSV import's name→id map now prefers the tenant row. Filter bars on Proposals + Sales Dash deliberately keep the full catalog — they filter historical records. Also: `remove()` discarded the PostgREST error, so an FK block or RLS no-op looked like the row snapping back with no explanation; now surfaces the reason and `.select()`s to catch the zero-rows RLS case. `work_types.active` was a live but unread column — now the retire switch; TEST (id 89, blocked by 3 `proposal_wtc` rows on soft-deleted proposals, FK is NO ACTION) set inactive in prod via `supabase db query --linked`. No schema change. **Open:** 10 system defaults are on real jobs with no custom twin (Caulking 12 WTCs, Grind and Seal, Joint fill, Patching, Waterproofing, Water Based Epoxy, Joint Fill Caulking Labor, Epoxy, Grind Stain and Seal, Shot Blast) — they're no longer selectable for new work; re-create as custom with SOWs if they're real offerings. | branch `fix/settings-custom-work-types-only` (deleted post-merge), merged to main @ `e2dec86` |
| 2026-08-04 | —   | Job Detail attachments: the upload control was visually indistinguishable from the attached-file chips. Both used the dark-pill / teal-text treatment (`C.dark` bg, `C.teal` text, radius 6), so at a glance "+ Upload Files" scanned as a fourth attached file and Chris couldn't find how to add one. Replaced with the app's dashed-outline action pattern (same family as BillingScheduleSection "+ Add Line" / NewInquiryWizard): transparent fill, `1.5px dashed C.borderStrong`, uppercase muted display text, sized to its own text (not full-bleed), double-height for an easy drag target. Added drag-and-drop — border + fill flash `C.teal` / `C.tealGlow` on drag-over — mirroring the existing `Import/FileUpload.jsx` drop zone. Upload logic extracted to `uploadFiles(files)` so the picker (`handleUpload`) and the drop handler (`handleAttachmentDrop`) share one path; filename sanitizing, per-file failure alert, and the `job-attachments` bucket write are unchanged. No DB/RLS/edge-fn change — prod storage policies verified already allowing insert/read on the bucket. UI-only, `vite build` green. | branch `fix/attachment-dropzone` (deleted post-merge), merged to main @ `c68e122`, handoff v173 |
| 2026-07-13 | —   | Edit a Sent / QB-synced invoice in place (add late PO, keep number). Revealed the Edit button for unpaid Sent-family invoices (Sent / Waiting for Payment / Past Due) so an operator can add a late PO into description/intro WITHOUT a pull-back that mints a new number. The QB-synced-edit machinery already existed (handleSaveEdit + qb-sync-invoice) but was gated to status New. `syncedLock` locks number + discount + retention + per-line % both in the UI and at the write path (preserve stored amounts, never recompute — CLAUDE.md #6/#7); Paid / qb_payment_id blocked; QB resync now awaits + surfaces errors (fnErr.context.json), non-destructive on failure (SC saved, form open for idempotent retry). Reason-for-Edit is a required, pulsing dark/amber callout. /buildvsplan cleared (0 T1, 2 T2 fixed). Live-used + accepted; QB-side artifacts (CustomerMemo PO, `[EDITED]` PrivateNote) worth a QBO spot-check. | branch `feat/edit-sent-qb-invoice-in-place`, merged to main @ `1260c24`, handoff v164 |
| 2026-05-14 | B21 | Public invoice page (`/invoice/:token`) was returning 400 for 100% of customers since 2026-05-02. Root cause: RLS policies `call_log_public_view_token` + viewing branch of `customers_public_read_token` cast `i.job_id::int = call_log.id`; `invoices.job_id` stores descriptive display strings (intentional denormalized copy used by QB sync, PDF gen, etc.), not stringified `call_log.id`, so the cast threw inside the predicate and PostgREST returned 400 for any embed touching call_log/customers. Fix: added `invoices.call_log_id integer NOT NULL` FK as the canonical "which call_log does this invoice belong to" pointer; backfilled from `proposals.call_log_id`; rewrote both policies to use the clean FK. Code writers in `Invoices.jsx:228` + `NewPayAppModal.jsx:180` set `call_log_id` on insert. `job_id` stays as the denormalized display copy (8 readers depend on it). One-off cleanup: hard-deleted orphan invoice 09121 + soft-deleted Draft proposal 3273564d-... that blocked the NOT NULL backfill (both deleted_at 2026-05-11). | branch `fix/invoices-call-log-id-fk`, migration `20260514130000_invoices_call_log_id_fk.sql` |
| 2026-05-09 | C9  | `send-pay-app` no longer trusts body-supplied `recipientEmail`, PDF URLs, or `senderEmail`. Adopts `_shared/tenantAuth.ts`; loads pay app + invoice + customer chain from DB; asserts `payApp.tenant_id === caller.tenantId`, `invoice.tenant_id === caller.tenantId`, AND `payApp.invoice_id === invoiceId` (closes same-tenant cross-row send); requires `invoice.pdf_url` to be persisted before send (clear 400 if missing); soft recipient allowlist built from `customer_contacts.email` ∪ `customers.{billing_email, contact_email, email}` for the pay app's own customer chain; sender email derived from `team_members.email` via `caller.userId`; PDF URLs taken from DB rows only and validated against hostname=Supabase project + path prefix `/storage/v1/object/public/(job-attachments\|signed-proposals)/` (caller URLs ignored entirely); pay-app + invoice writes scoped with `.eq("tenant_id", caller.tenantId)` defense-in-depth. | branch `fix/c9-send-pay-app-and-qb-tenant-scoping` |
| 2026-05-09 | —   | QB function tenant scoping — `qb-create-job`, `qb-link-customer`, `qb-search-customers` switched to `authenticateCaller` (reject service role; these are user-initiated UI flows). `getQBToken(sb)` → `getQBToken(sb, tenantId)` matching `qb-record-payment`. `qb-create-job` and `qb-link-customer` now load `call_log` and assert `call_log.tenant_id === caller.tenantId` before any QB call or DB write; `proposals`/`customers` reads scoped with `.eq("tenant_id", caller.tenantId)`; `customers`/`call_log` writes scoped same. `qb-search-customers` no DB writes — only the `getQBToken(sb, caller.tenantId)` switch matters. Closes the multi-tenant gap that `C4` migration (`qb_connection.tenant_id`) opened the door for. | branch `fix/c9-send-pay-app-and-qb-tenant-scoping` |
| 2026-05-08 | B8  | NewPayAppModal totals no longer go negative when "% To Date" inputs are empty. Per-line `newAmount` now floors at `prior$` (`src/components/NewPayAppModal.jsx:98`), so blank inputs read as "no new claim against this line" rather than "regress cumulative to 0". Preserves the AIA G702 invariant Line 4 − Line 5 = Line 6 ≥ 0. `validate()` still gates submission on `grossThisBilling <= 0`, so persisted invoice/pay-app values are unchanged for any submittable state. Repro on preview: Plenium AGRU job → blank inputs now show L4=$43,091, L5=$43,091, L6/L7/L8=$0 instead of L6=−$43,091. | branch `fix/b8-payapp-negative-totals` |
| 2026-05-04 | B6  | QuickBooks "Connection Failed" 403 on `/qb/callback` — fixed by `verify_jwt = false` on qb-auth in `supabase/config.toml` (gateway was rejecting the OAuth redirect's stale JWT before the function ran) + `waitForSession` retry loop in `QBCallbackPage.jsx` (was firing `exchange()` before gotrue restored the session). Closed retroactively 2026-05-08 after BACKLOG audit confirmed both pieces match the original B6 hypothesis and are live in main. | commit `f68e417` |
| 2026-05-07 | S1  | `public.get_user_tenant_id()` COALESCE fallback removed; body now returns NULL on miss + scoped to `active=true`. Pre-apply orphan gate (one-shot DO block) + permanent `v_orphan_auth_users` steady-state view. Three seed files synced to new prod body. Tests 1–6 passed on scratch (`xuovwlhqztqljyvveicu`, deleted post-test); prod push clean (0 orphans), live smoke green (call_log insert via New Inquiry wizard resolved tenant correctly). | commit `c893aee`, migration `20260509120000_get_user_tenant_id_remove_fallback.sql`, rollback `supabase/rollbacks/20260509120100_…sql`, plan `docs/plans/s1_remove_get_user_tenant_id_fallback.md`, handoff v102 |
| 2026-05-06 | H4  | `proposal_signatures.tenant_id` now derived from parent proposal via `BEFORE INSERT` trigger `trg_proposal_signatures_set_tenant`; column DEFAULT (`get_user_tenant_id()`) dropped. Closes the latent cross-tenant write that would have activated on the second tenant being provisioned. Verified on scratch project with two tenants + live prod signing test (5 most recent signatures all match parent). Token-only anon INSERT policy unchanged. | commit `1660795`, migration `20260508120000_proposal_signatures_tenant_id_trigger.sql` |
| 2026-05-06 | B10 | Unique partial index on `call_log(tenant_id, job_number, COALESCE(co_number,0))` excluding archived + retry guards on PG 23505 in `NewInquiryWizard.jsx` and `ImportToLiveWizard.jsx`. | PR #15 (`92b0696`), migration `20260507130000_call_log_unique_job_number.sql` |
| 2026-05-06 | B9  | Merge Job feature — `merge_call_log()` RPC + `MergeJobModal.jsx` + Merge button on Job Detail. Re-points proposals/invoices/`job_work_types`/CO children, archives loser, admin/manager only, hidden on COs and archived rows. | PR #13 (`74cbbc5`), migration `20260507120000_call_log_merge.sql` |
| 2026-05-06 | —   | Import to Live wizard re-import handling — when an archive collides with its own prior import, Step 7 offers Open Existing Job (navigate) or Replace Existing Import (typed `REPLACE NNNN` confirm, only when existing call_log has zero proposals/invoices). | `feat/billing-contact-flag` → main (`c8af84e`) |
| 2026-05-06 | —   | `is_billing_contact` flag on customer_contacts decouples billing from role. Migration `20260506100000` adds column + backfills role='Billing Contact' rows. New "Use as billing contact" checkbox in Edit Contact modal, BILLING badge on cards, back-compat resolution everywhere (ContactBillingPicker, Invoices.loadContact, PayAppDetailModal, send-invoice edge fn). | `feat/billing-contact-flag` → main (`c8af84e`); send-invoice deployed |
| 2026-05-06 | —   | Smarter customer auto-match in Import to Live wizard — exact → acronym (IVGID ↔ Incline Village General Improvement District) → token-overlap with stopword filter. SearchSelect replaces native `<select>` for the Use Existing dropdown. | PR not opened, merged via `34f926a` |
| 2026-05-06 | —   | Field SOW materials picker empty state — split misleading "✓ All Tab 3 materials added" into two distinct empty messages so zero-materials WTCs point users at Step 3. Copy-only change to src/pages/WTCCalculator.jsx (~line 687). | PR #9 (`d5a72f6`), branch `fix/field-sow-empty-materials-copy` (deleted) |
| 2026-05-05 | H6  | PublicSigningPage no longer exposes pricing internals. Migrations 20260505190200 (proposal_wtc.locked_line_total column) + 20260505190300 (get_public_proposal_view RPC) applied to prod; backfill ran (127 rows written, 0 failed); Vercel preview smoke test passed; PR #8 merged. handleLock writes per-WTC totals at lock time (no SQL recompute, no calc.js drift surface). | PR #8, branch `fix/h6-public-signing-locked-totals` |
| 2026-05-05 | H1/H7 | `SET search_path = public` on get_user_tenant_id, request_signing_token, request_viewing_token. Migration applied via `supabase db push --linked`; verified all 3 proconfig=["search_path=public"] on prod. | Migration `20260505181452`, branch `fix/h1-h7-search-path` |
| 2026-05-05 | —   | Stale remote branch cleanup — 9 claude/* + 3 feature branches deleted (handles former "delete 10 stale branches" cleanup row) | v93 (`ca2e984`)    |
| 2026-05-05 | —   | Pay app system redesign shipped (delete cascade fix, retention to invoice, QB retention to Other Current Asset, retention list view) — replaces former "F6 Pay app system redesign" row pointing at `docs/plans/pay_app_system_redesign.md` | v93                |
| 2026-05-05 | —   | v91 prod smoke test — public signing page renders, signing flow transitions proposal/call_log → Sold                          | Session 2026-05-05 |
| 2026-05-02 | —   | Security audit v91 deploy — all 12 Criticals, anti-pattern policies dropped, edge fn tenant isolation                         | v91 handoff        |

### Reversed / Closed-without-doing (2026-05-12)

Originally filed under active tables; closed when the load-bearing premise was overturned. Kept here in full active-row form so the forensic trail (Tier, Status, Source, Notes) survives — see `docs/AUDIT_LOG.md` 2026-05-12 §5(c) reversal notes for the audit-miss root cause.

| ID  | Tier | Status            | Item                                                                                              | Source                                              | Notes                                                                                                                                                                                                                                                                                                                                                          |
|-----|------|-------------------|---------------------------------------------------------------------------------------------------|-----------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| B17 | T2   | Closed 2026-05-12 | Archive import creating duplicate `(proposal_id, work_type_id)` rows in `proposal_wtc`            | Found 2026-05-12 (V8 pre-flight for Multi-GC §5(c)) | **Closed Not-a-Bug 2026-05-12.** §5(c) resolution overturned: multi-WTC-same-`work_type_id` on one proposal is intentional, encoding sub-area splits / time-phasing / crew assignment. V8 "dup" pattern was sub-area data, not import duplication. See AUDIT_LOG 2026-05-12 §5(c) reversal notes + plan §5(c) Reversal section.                                |
| B18 | T2   | Closed 2026-05-12 | Triage 17 existing duplicate WTC pairs across 14 proposals                                        | Found 2026-05-12 (V8 pre-flight for Multi-GC §5(c)) | **Closed Not-Applicable 2026-05-12.** The 17 "dup" pairs are intentional sub-area encoding, not duplicates. No triage needed. See B17 closure + AUDIT_LOG 2026-05-12.                                                                                                                                                                                          |
| O5  | T2   | Closed 2026-05-12 | Multi-GC Migration 1b — apply `UNIQUE (proposal_id, work_type_id)` on `proposal_wtc`              | Filed 2026-05-12 (Multi-GC §10 step 2 split — V8 returned dups) | **Closed Won't-Do 2026-05-12.** UNIQUE constraint would actively prevent valid use (multi-WTC-same-work_type for sub-areas). §5(c) resolution overturned. Migration 1b will never ship. §10 step 6 sequencing constraint inverted: blocked on §5 sync-identity re-plan (F16), NOT on UNIQUE. See AUDIT_LOG 2026-05-12.                                          |
